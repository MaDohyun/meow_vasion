# BEAM BANDIT 작업 규칙

- `src/core`는 브라우저·React·Three.js에 의존하지 않는 순수 로직으로 유지한다.
- 핵심 동사는 통합 빔 하나다. 납치·스캔·파괴가 별도 조작으로 늘어나지 않게 한다.
- 조작감, 카메라, 팔레트 변경은 플레이 테스트 결과를 근거로 작은 단위로 조정한다.
- 이동 키는 두지 않는다. UFO는 늘 보는 방향으로 전진하고, 플레이어가 정하는 것은 방향뿐이다. 처음 잡은 사람에게 조작을 가르치는 단계를 만들지 않는 것이 이 규칙의 목적이므로 전진·후진·좌우 이동 키를 되살리지 않는다.
- 감속은 과적이 맡는다. 빔을 켜는 것 자체에는 값을 매기지 않는다 — 빈 거리를 원뿔만 열고 지나가는 것과 차 세 대를 물고 지나가는 것이 같은 값일 수는 없다. 값을 매기는 것은 매달린 무게이며, 적재 한계를 넘어선 비율만큼 최고 속도가 내려간다. 브레이크 키를 따로 만들지 않는다.
- 벽은 세우지 말고 밀어낸다. 이동 키가 없는 기체는 후진으로 벽에서 빠져나올 수 없으므로, 충돌은 벽면을 따라 미끄러지게 하고(법선 성분만 제거), 들어온 속도에 비례해 되튀기고(`WALL_BOUNCE`), 정면으로 부딪힌 정도에 비례해 기수를 벽 바깥으로 돌린다(`WALL_DEFLECT_RATE`). 접촉 프레임마다 속도를 일정 비율로 깎는 처리는 다시 넣지 않는다 — 재가속보다 큰 감속은 기체를 벽에 영원히 붙여 놓는다.
- 벽은 사다리이기도 하다. 고도는 전진 속도에 피치를 곱해서 나오므로, 속도를 빼앗는 접촉은 상승 수단까지 함께 빼앗는다. 그래서 벽에 닿아 있는 동안은 보는 방향으로 수직 속도를 직접 준다(`WALL_CLIMB_SPEED`). 건물 사이에 낀 기체가 위를 보면 올라갈 수 있어야 한다.
- 월드는 경계와 좌표 래핑이 없는 34유닛 결정론적 셀 구조로 유지한다. 같은 셀은 저장 없이 좌표 해시로 재생성한다.
- 생성 반경 250, 제거 반경 300의 히스테리시스를 유지하고 활성 풀은 건물 128개·차량 48개를 넘기지 않는다.
- 건물은 약 45~50% 셀에 생성하고 footprint 16~22를 도로 경계에서 4.5유닛 이상 안쪽에 둔다.
- 절차 오브젝트는 고정 InstancedMesh 슬롯을 재사용한다. 실시간 그림자와 메시별 외곽선 대신 blob shadow와 픽셀 텍스처를 사용한다.
- 호숫가 돌·갈대는 무게 1의 빔 소품이다. 그리는 반경(랜드마크 6셀)보다 시뮬레이션 반경(`LAKE_SHORE_PROP_RADIUS_CELLS`, 4셀)을 좁게 두되, 그 차이는 언제나 빔이 닿을 수 있는 가장 넓은 원뿔보다 넓어야 한다. 두 목록은 좌표로 만든 같은 id를 쓰므로 소품 하나가 두 번 그려지거나 먹힌 뒤 되살아나지 않는다.
- 인트로는 프리플라이트 로더의 에셋·셰이더 준비가 완료된 뒤에만 표시한다.
- 게임 런은 생존형이다. HUD에는 경과 시간과 남은 생존 시간이 함께 표시되고, 남은 시간은 매초 감소한다. 충돌·적 공격은 남은 시간을 더 크게 깎고, 보행자·고양이 흡수는 시간을 회복한다. 180초 누적 생존은 클리어, 0초는 실패다.
- 런의 결말은 셋이다. 정찰 완료, 시간은 버텼으나 미션 실패, 그리고 격추다. 판을 일찍 끝내는 것은 격추 하나뿐이다.
- 과적은 느리게·낮게 만들되 죽이지 않는다. 무게가 적재 한계를 넘으면 상승이 끊기고, 기체가 내려가고, 최고 속도가 떨어지고, 경고가 뜬다. 그러나 지면에 닿아도 판이 끝나지 않는다 — 눈으로 보이고 언제든 빔을 놓아 되돌릴 수 있는 압박에 사망 판정까지 붙이지 않는다. 속도 하한(`OVERLOAD_CRUISE_FLOOR`)은 항상 0보다 크게 유지한다. 과적에서 빠져나오는 길이 어딘가로 날아가 삼키는 것이므로 멈춰 세우면 그 길이 막힌다.
- 같은 절차 건물 데이터를 0~250유닛은 풀 디테일, 250~600유닛은 단일 실루엣 InstancedMesh로 재사용하며 독립 스카이라인 데이터는 만들지 않는다.
- 주행 차량은 고정 32슬롯, 보행자는 28슬롯, 고양이는 7슬롯, 적은 종류별 고정 InstancedMesh 풀(후반 총 100개 이상), 적 투사체는 160슬롯, 레이저는 18슬롯, 발사·착탄 FX는 12슬롯을 재사용한다.
- `Q` 레이저는 조준 시점에 판정하는 히트스캔이며 UFO에서 명중점까지 0.14초 동안 보이는 고정 풀 선분으로 그린다. 총구·착탄 FX와 피해도 같은 조준 결과를 사용한다.
- `E` 트랙터 빔의 시각 길이는 포획물 높이와 무관하게 지면 또는 `maxDrop`까지 유지하고, 보행자·고양이만 UFO 근접 시 축소 흡수한다. 자동차는 흡수하지 않는다.
- 도로 차량 폭발음은 승용차·트럭·유조차가 같은 샘플 하나를 공유하고 볼륨만 다르다. 유조차는 연료를 실은 가장 무거운 표적이므로 항상 더 크게 재생한다.
- 유조차는 빔에 닿아도 기폭하지 않는다. 크기·무게 게이트만 통과하면 삼켜지는 평범한 중량 차량이며, 폭파는 레이저의 몫이다.
- 무게는 무엇을 옮길 수 있는지를, 폭은 무엇을 삼킬 수 있는지를 정한다. 두 게이트는 독립이며 흡수는 둘 다 통과해야 한다.
- 빔이 들어올릴 수 없는 무게(`beamLiftScale`가 0인 밴드)는 어떤 종류든 흡수되지 않는다. 소품·차량·군중·기계에 예외를 두지 않으며, 타깃 링이 표시되지 않는 대상은 삼켜지지도 않는다.
- 종류로 빔을 면제받는 것은 없다(`beamImmune`는 전부 false). 하늘의 적도 무게와 폭으로만 판정한다 — 헬기 6, 전투기 10, 공중전함 30이다. 종류로 거부하는 것은 자폭 드론 기뢰 하나뿐이며, 그것은 "너무 무거워서"가 아니라 잡힌 폭탄은 식량이 아니라 터지는 것이기 때문이다.
- 건물에도 예외가 없다. 뉴스 타워도 높이가 매기는 무게(10 또는 11)로 판정한다 — 무엇이냐가 아니라 얼마나 무거우냐가 규칙이다. 속보 자체는 HUD의 띠이므로 타워를 삼켜도 계속 나온다.
- 주유소는 빔에게는 무게 9의 소품이고 레이저에게는 랜드마크다. 같은 id를 쓰며, 삼켜지면 `destroyedLandmarks`에도 들어간다. 빔이 닿아서 폭발하는 것은 이제 도시에 하나도 없다 — 폭파는 레이저의 몫이다.
- 폐허도 무게 5로 치울 수 있다. 폐허는 월드 생성이 아니라 런타임 상태이므로 소품이 될 수 없고, 건물과 같은 방식으로 잡히는 즉시 `ruinedBuildings`와 콜라이더에서 빠져 빔 오브젝트가 된다.
- 무게 사다리는 두 층이다. 성장의 85%까지가 도시 사다리(1~12단), 그 위 15%가 하늘 사다리(12~30단)다. 공중전함은 사다리의 꼭대기이므로 크기 상한에 거의 닿아야 열린다 — 판의 마지막에 할 수 있는 일이지 지나가다 하는 일이 아니다.
- UFO 몸통 지름(`ufoDiameter`)보다 넓은 오브젝트는 흡수되지 않는다. 끌 수 있다는 것이 삼킬 수 있다는 뜻이 되어서는 안 되며, 이런 물건은 매달아 옮기거나 떨구는 대상이다. 현재 수치에서는 무게 사다리가 늘 먼저 걸리므로 단독 발동하지 않는 불변식이다.
- 포인트 라이트는 항상 마운트된 3개 슬롯만 사용하며 기능 토글 시에는 intensity만 바꾼다.
- 기체의 자세는 항공기 순서(`YXZ`)로 합성한다. Three.js 기본값인 `XYZ`는 피치를 요 다음에 월드 X축으로 적용해, 기수가 Z축에서 벗어난 순간 같은 피치가 롤로 보인다. 헤딩·피치·롤을 `rotation.x/y/z`에 직접 넣지 말고 `core/craftPose`의 `craftEuler`를 지난다.
- 적은 시간 웨이브 고정 풀(자폭 드론 기뢰·헬기·전투기·공중전함)을 종류별 단일 InstancedMesh로 유지한다. 드론은 전부 정지형 기뢰이며, 지나가는 드론과 탱크는 사용하지 않는다. 건물 옥상 대공포와 헬륨풍선도 사용하지 않는다.
- 종류별 풀 상한(`ENEMY_CAPS`)은 웨이브 목표보다 크게 둔다. 그 여유분이 공중전함의 격납고이며, 스포너가 채우는 인구와 전함이 띄우는 호위대를 합쳐도 상한을 넘지 않는다.
- 공중전함의 무장은 사방·상하 3층 탄막 하나뿐이다. 캐리어이기도 해서 10초마다 기뢰·헬기·전투기를 자기 주변에서 띄운다.
- 공중전함은 일정 거리 밖으로 벌어지면 플레이어 속도보다 빠르게 붙는다. 한 방향으로 계속 도망쳐도 전함이 시야 밖으로 사라지면 안 된다.
- 피격 연출은 종류를 가리지 않는다. 체력을 깎는 모든 것이 화면 흔들림과 붉은 점멸을 일으킨다. 세기만 `HIT_TRAUMA`가 종류별로 정한다.
- 적의 사격은 하나뿐이다. 느리고 은은한 붉은 구체 탄막이며, 하늘의 모든 총이 같은 것을 쏜다. 조준하는 적은 없다 — 예고선도, 조준 링도, 리드사격도 두지 않는다. 전투기는 사거리 안에 UFO가 있으면 쿨타임마다 지금 있는 자리로 한 발, 공중전함은 사방·상하로 링을 뿌린다. 탄속은 순항 속도(30)의 절반 아래로 유지한다. 탄막은 맞히는 물건이 아니라 읽고 피하는 물건이다.
- 기체와 폭발의 거리는 중심이 아니라 선체 표면으로 잰다. 선체 반지름은 성장에 따라 15를 넘고 기뢰 폭발 반경은 10 고정이므로, 중심 기준으로 재면 다 자란 기체는 자기 선체에 붙어 터진 폭발의 사거리 밖에 있게 되어 아무 피해도 받지 않는다. 무장·기폭 판정에 새 거리 비교를 넣을 때는 `+ hitRadius`를 빠뜨리지 않는다.
- 탄막 한 발은 체력 반 칸이다. 보고 피할 시간이 있었던 피해가 가장 싸다는 규칙(`HEALTH_LOSS`)을 유지한다.
- 정점 색 속성이 없는 기본 지오메트리(`sphereGeometry`, `ringGeometry`, `planeGeometry` 등)에는 `vertexColors`를 켜지 않는다. 셰이더가 없는 속성을 곱해 전부 검게 나오며, 가산 블렌딩과 겹치면 아예 보이지 않는다. 인스턴스별 색은 `setColorAt`만으로 동작한다.
- 크라우드와 적 모델은 파츠별 정점 색을 구운 병합 `BufferGeometry`를 최초 한 번만 만들고 종류별 단일 `InstancedMesh`로 렌더링한다.
- 임무는 고정 순서 다섯 단계 사다리다. 무작위 추첨도, 한 번에 여러 목표도 쓰지 않는다. 수배 별·열·체인 시스템은 여전히 사용하지 않는다.
- 임무 게이지는 판 전체 누적치를 읽는다. 단계가 열릴 때 미터를 0으로 되돌리지 않으며, 이미 채워진 채로 열려 즉시 끝나는 경우를 정상 동작으로 다룬다.
- 흡수 점수와 파괴 점수는 절대 섞지 않는다. 빔으로 삼킨 것만 표본 게이지에, 터뜨린 것만 파괴 게이지에 들어간다. 모든 점수 지급은 `bankAbsorbScore` 또는 `bankDestroyScore` 중 하나를 지난다.
- 임무 한 단계를 끝내면 장군의 무전이 시뮬레이션 전체를 멈춘다(게임 시간 포함). 클릭으로만 해제되며, 마지막 다섯 번째 단계는 결과 화면이 대신하므로 무전이 없다.
- 미스터리 서클은 가속과 터보 완충을 한 번에 준다. 둘 중 하나만 떼어 쓰지 않는다. 서클 통과만으로는 생명력을 회복시키지 않는다.
- 서클 위 부품 아이템은 능력치 여덟 종(레이저·속도·회전력·빔 범위·빔 사거리·빔 흡수 속도·터보 충전·터보 양)을 올린다. 전부 1레벨까지다 - 같은 능력치를 두 번 주는 서클은 없고, 아이템 여덟 개면 사다리가 끝난다. 레벨이 하나뿐이므로 습득 문구에도 레벨 숫자를 넣지 않는다. 전부 최대일 때만 생명력 1.5칸을 회복하고, 생명력도 가득이면 점수를 준다. 회복은 올릴 것이 없어진 판의 대체 보상이지, 다쳤을 때 찾아가는 수단이 아니다.
- 레이저만 1.5배로 크게 잡는다. 공중전함은 빔으로 먹을 수 없어 레이저 외에 답이 없는 유일한 표적이기 때문이다. 나머지 일곱은 15~30% 대에 둔다.
- 빔 범위·사거리·흡수 속도 부품은 기체 크기가 주는 값에 곱해질 뿐 대체하지 않는다. 최대 부품도 크기가 한 판 동안 벌어들이는 폭(`BEAM_APERTURE_MAX`·`BEAM_REACH_MAX`·`BEAM_PULL_MAX`)을 넘지 않는다. 그리는 빔과 실제로 잡는 빔은 항상 같은 값에서 나와야 하므로 `beamRadiusScale`/`beamReachScale` 한 곳만 지난다.
- 초기 로딩을 위해 외부 이미지·음원 의존성을 추가하지 않는다. 에셋 예산은 gzip 후 8MB다.
- BGM은 외부 파일 없이 Web Audio로 절차 합성하며 플레이 중에만 재생하고 결과 화면에서는 정지한다.
- 랭킹은 구글 스프레드시트에 붙인 Apps Script 웹 앱 하나로만 오간다. 이름 다듬기와 순위 정렬 규칙은
  `src/core/leaderboard.ts`와 `google-apps-script/Code.gs`에 같은 내용으로 존재하므로 한쪽만 고치지 않는다.
- 랭킹 등록은 결과 화면에서만 열리며, 한 판은 한 기록이다. 시트가 없으면 로컬에 저장하고 화면에 그렇게 밝힌다.
- 개발자용 입구는 개발 서버에서 늘, 배포된 빌드에서는 주소가 `?dev=1` 또는 `#dev`일 때만 보인다. 로비의 전함 드릴이 그 하나다.
- 드릴로 시작한 판은 `devRun`으로 표시되고 랭킹에 올리지 않는다. 튜토리얼 브리핑도 뜨지 않는다.
- 커밋 메시지는 Codex 작업에 `[codex]`, 사람의 직접 조정에 `[human]` 태그를 사용한다.
