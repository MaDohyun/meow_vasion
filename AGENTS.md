# BEAM BANDIT 작업 규칙

- `src/core`는 브라우저·React·Three.js에 의존하지 않는 순수 로직으로 유지한다.
- 핵심 동사는 통합 빔 하나다. 납치·스캔·파괴가 별도 조작으로 늘어나지 않게 한다.
- 조작감, 카메라, 팔레트 변경은 플레이 테스트 결과를 근거로 작은 단위로 조정한다.
- 월드는 경계와 좌표 래핑이 없는 34유닛 결정론적 셀 구조로 유지한다. 같은 셀은 저장 없이 좌표 해시로 재생성한다.
- 생성 반경 250, 제거 반경 300의 히스테리시스를 유지하고 활성 풀은 건물 96개·차량 48개를 넘기지 않는다.
- 건물은 약 33% 셀에만 생성하고 footprint 16~22를 도로 경계에서 4.5유닛 이상 안쪽에 둔다.
- 절차 오브젝트는 고정 InstancedMesh 슬롯을 재사용한다. 실시간 그림자와 메시별 외곽선 대신 blob shadow와 픽셀 텍스처를 사용한다.
- 인트로는 프리플라이트 로더의 에셋·셰이더 준비가 완료된 뒤에만 표시한다.
- 게임 런은 3분 제한형으로 만들지 않는다. HUD 시간은 경과 시간이고 점수가 매초 감소하며 0이 되는 것만 실패 조건이다. 별 5개는 자동 종료 없이 최고 위험·보상 상태로 유지한다.
- 같은 절차 건물 데이터를 0~250유닛은 풀 디테일, 250~600유닛은 단일 실루엣 InstancedMesh로 재사용하며 독립 스카이라인 데이터는 만들지 않는다.
- 주행 차량은 고정 32슬롯, 보행자는 28슬롯, 고양이는 7슬롯, 레이저는 18슬롯, 발사·착탄 FX는 12슬롯을 재사용하며 `Q` 한 번에 투사체 한 발만 발사한다.
- `Q` 레이저는 조준 시점에 판정하는 히트스캔이며 UFO에서 명중점까지 0.14초 동안 보이는 고정 풀 선분으로 그린다. 총구·착탄 FX와 피해도 같은 조준 결과를 사용한다.
- `E` 트랙터 빔의 시각 길이는 포획물 높이와 무관하게 지면 또는 `maxDrop`까지 유지하고, 보행자·고양이만 UFO 근접 시 축소 흡수한다. 자동차는 흡수하지 않는다.
- 포인트 라이트는 항상 마운트된 3개 슬롯만 사용하며 기능 토글 시에는 intensity만 바꾼다.
- 적은 티어별 고정 풀(병사 8·헬기 3·대공포 12·전투기 3·풍선 1)을 종류별 단일 InstancedMesh로 유지하고, 배경 오브젝트 애니메이션은 최소화한다.
- 크라우드와 적 모델은 파츠별 정점 색을 구운 병합 `BufferGeometry`를 최초 한 번만 만들고 종류별 단일 `InstancedMesh`로 렌더링한다.
- 미션은 실제 95유닛 안의 활성 오브젝트 하나만 가리킨다. 고양이·보행자·자동차 고도 유지·현재 등장한 적 격추만 사용하고 미스터리 서클은 아직 추가하지 않는다.
- 수배 별 점수 배수와 체인 배수를 곱하며, 주변 건물·보행자·차량 밀집도는 틱당 한 번만 캐시해 빔·레이저·파괴 열 상승에 적용한다.
- 초기 로딩을 위해 외부 이미지·음원 의존성을 추가하지 않는다. 에셋 예산은 gzip 후 8MB다.
- 커밋 메시지는 Codex 작업에 `[codex]`, 사람의 직접 조정에 `[human]` 태그를 사용한다.
