# Codex 작업 로그

## 2026-08-17 — 전체 프로젝트 재구성

- 의도: 기존 교차로 프로토타입 대신 드론 적재 배달 사양을 구현한다.
- Codex 구현: 프로젝트 구조, 결정론적 드론 적분, 충돌 반사, 화물 안정도와 낙하/회수, 주문·경제·업그레이드, 절차적 도시, 렌더 후처리, HUD, 모바일 입력, 자동 테스트.
- 사람의 결정: 핵심 리스크/리턴 정의, 2.5축 조작, 밝은 도트풍 아트 방향, D-9 스코프 기준.
- 기술적 개입: 실제 리지드바디 스택 대신 순수 안정도 함수, 떨어진 상자에만 경량 탄도 시뮬레이션을 사용한다.

## 2026-08-18 — BEAM BANDIT으로 게임 루프 전환

- 사람의 결정: 시부야·사이버펑크·배달 컨셉을 버리고, 밝은 소규모 도시에서 속도감 있게 UFO를 조종하는 코미디 아케이드 방향을 선택했다.
- Codex 구현: 통합 빔 미션, 5단계 수배, 체인 배수, 저공 은신/고공 전투기 위험, 터보 상호 배제, 3칸 실드, 경찰·전투기, UFO·빔·납치 대상 모델, 새 HUD와 모바일 조작을 구현했다.
- 협업 판단: 조작은 이동·고도·빔·레이저·터보로 제한하고, 전투기는 최대 3대, 배경 NPC 애니메이션은 정적인 구성으로 축소했다.
- 검증: 브라우저에서 시작 화면과 실제 첫 프레임을 시각 검수하고, TypeScript·단위 테스트·프로덕션 빌드를 통과시켰다.

## 2026-08-18 — 마우스 조향과 무기 입력 개선

- 사람의 결정: 마우스로 방향을 잡고 WASD로 이동하며, 빔/레이저를 각각 `O`/`P`로 분리한다.
- Codex 구현: 마우스 위치 기반 선회·조준, `A/D` 스트레이프, 상시 레이저 발사 피드백, 터보 화염과 HUD 활성 표시를 추가했다.

## 2026-08-18 — 완전한 3D 시선 비행

- 사람의 결정: 좌우 조향을 반전하고, 마우스 상하 피치와 실제 이동 방향을 결합하며 `Space/Q/E`를 터보/레이저/빔으로 배치한다.
- Codex 구현: UFO 피치 상태와 3차원 전진 벡터를 물리에 추가하고, 카메라·기체·레이저가 같은 방향을 보도록 통합했다.

## 2026-08-18 — 상하 반전 및 부스터 우선 입력

- 사람의 결정: 마우스 위는 상승, 아래는 하강으로 반전하고 `Space` 사용 중에는 빔이 절대 나오지 않게 한다.
- Codex 구현: 피치 입력 부호를 반전하고, 부스터가 빔보다 우선하도록 상호 배제 규칙을 강화했다.

## 2026-08-18 — 빔 입력 피드백 보장

- 사람의 피드백: `E`를 눌러도 빔이 보이지 않았다.
- Codex 구현: 고도에 따른 빔 시각 차단을 제거하고 키보드 레이아웃에 관계없이 `E`를 인식하도록 입력을 보강했다. 부스터 중 차단 규칙은 유지했다.

## 2026-08-18 — 고속 원형 미니 행성 확장

- 사람의 결정: UFO 속도를 높이고, 기존의 약 3배 크기를 정사각 격자가 아닌 하나의 둥근 지구로 만든다.
- Codex 구현: 지름 600 원형 행성, 둥근 바다 하부, 중앙+외곽 9개 도시 구역, 90개 건물, 원형 충돌 경계, 외곽 미션 순환, 행성 레이더를 추가했다.
- 속도 조정: 일반 최고 속도 14→24, 터보 22→42, 최대 고도 18→90.

## 2026-08-18 — 군집 트랙터 빔과 토러스 월드

- 사람의 결정: `A/D` 스트레이프를 반전하고, 부스터 중에도 빔을 사용하며, 마우스 포인터로 레이저를 조준한다. 빔은 차량 무리를 끌고 가되 범위 밖 차량은 떨어져야 한다.
- Codex 구현: 도시 차량 54대를 동적 오브젝트로 전환하고, 원뿔 포획·스프링 추종·중력 낙하를 추가했다. 터보 중 빔 반경·높이·응답력을 강화했다.
- 사람의 결정: 원형 경계 대신 어느 방향으로도 끊기지 않는 토러스 래핑 맵을 사용한다.
- Codex 구현: 플레이어의 연속 월드 좌표는 유지하면서 600×600 기본 데이터를 3×3 고정 슬롯으로 재사용한다. 충돌·미션·차량·레이더는 반대편 최단 거리를 사용한다.
- 검증: 좌표 예시, 래핑 거리, 복제본 슬롯, 래핑 충돌, 빔 포획·강화·이탈 낙하를 포함한 25개 테스트와 TypeScript·프로덕션 빌드를 통과시켰다.

## 2026-08-18 — 시야 컬링·차량 인스턴싱·프리플라이트 로더

- 사람의 피드백: 3×3 토러스 렌더링이 너무 무거우므로 시야 안의 오브젝트만 렌더링하고 진입 전 필요한 리소스를 준비한다.
- Codex 구현: 9개 맵 슬롯과 81개 도시 구역을 카메라 프러스텀·렌더 거리로 그룹 컬링했다. 도시 컴포넌트는 타일을 넘을 때만 다시 계산한다.
- Codex 구현: 486개 차량 컴포넌트와 프레임 콜백을 제거하고, 시야 안의 차량만 차체·캐빈·하부·라이트·빔 링의 5개 InstancedMesh로 합쳤다.
- Codex 구현: 절차적 리소스 로드, Three.js 셰이더 컴파일, 빔·카메라 워밍업 단계를 보여주는 프리플라이트 로딩 화면을 추가했다.

## 2026-08-18 — 절차적 셀 스카이라인과 오픈 런

- 사람의 정정: BEAM BANDIT은 작은 3분 레이드 게임이 아니며 현재의 큰 토러스 월드를 유지한다.
- Codex 구현: `BUILDINGS`, `CITY_COLLIDERS`, 미션 스폰은 건드리지 않고 26유닛 월드 셀 해시로 원경 블록을 생성한다. 드론 셀·heading 구역이 바뀔 때만 최대 40개 InstancedMesh 행렬을 갱신한다.
- 런 규칙: 180초 자동 종료를 제거하고 HUD 시간을 경과 시간으로 변경했다. 별 5개 생존 탈출은 유지한다.

## 2026-08-18 — 무한 절차 도시와 경량 풀 렌더링

- 사람의 결정: 고정 토러스 타일을 폐기하고, 어느 방향으로도 경계 없이 이어지는 좌표 해시 기반 절차 도시로 전환한다.
- Codex 구현: 40유닛 셀의 건물·주차 차량·공터·교차로를 결정론적으로 생성하고 생성 90/제거 130유닛 히스테리시스를 적용했다. 활성 상한은 건물 24개, 차량 12개다.
- 충돌·미션: 현재 활성 건물만 동적 AABB로 사용하며, 다음 미션은 완료 시점의 플레이어 주변 절차 건물 옆에 생성한다.
- 렌더링: 3×3 토러스 복제본과 고정 90개 건물을 제거했다. 절차 건물·도로·차량을 고정 InstancedMesh 슬롯으로 합치고, 실시간 shadow map과 건물별 `<Edges>`를 blob shadow·픽셀 텍스처·사인 아틀라스로 교체했다.
- 사람의 추가 결정: 목숨·실드 제한은 추후 설계로 미루고 현재는 제거한다. 충돌·경찰·전투기 피격은 반동, 포로 이탈, 화면 피드백만 남기며 게임오버를 발생시키지 않는다.

## 2026-08-18 — 무한 도시 밀도와 360° 연속성 보강

- 사람의 코드 리뷰: 원점 단위 geometry 기반 InstancedMesh 컬링 때문에 카메라 회전 시 건물 풀이 통째로 사라지고, heading 기반 원경 재생성·좁은 생성 반경 때문에 벌판이 드러나는 문제를 확정했다.
- Codex 구현: 건물 4개 풀의 frustum culling을 끄고, heading과 무관한 72개 360° 스카이라인 링을 300유닛에 배치했다.
- 밀도: 셀 34, 생성/제거 250/300, 건물/차량 96/48, 지면 반경 9셀로 확장했다. 도로·보도에 월드 고정 픽셀 노이즈, 차선, 횡단보도를 넣었다.
- 성능: pointLight를 항상 존재하는 3슬롯으로 고정하고, 활성 월드가 갱신될 때만 콜라이더 배열을 재구축한다.

## 2026-08-19 — 월드 고정 원경과 단발 전투·도로 교통

- 사람의 코드 리뷰: 플레이어 셀마다 원경 72개가 통째로 재추첨되고, 체커보드 건물과 셀보다 큰 footprint가 도로와 비행 공간을 막는 문제를 확정했다.
- Codex 구현: 원경을 55유닛 월드 고정 셀의 280~340 밴드 선택 방식으로 전환했다. 건물은 약 33%, footprint 16~22, 도로 inset 4.5, 저층 7~22/고층 25~60 분포로 바꿨다.
- 전투: 키보드·모바일 공통 rising edge에서만 발사하는 18슬롯 레이저 투사체 풀을 추가했다. 빔 범위를 축소하고 반경 업그레이드 배수와 자동차 질량 2.4를 적용했다.
- 월드 활동: 시야 밖 60~140유닛 도로에서 생성되어 직진·회전하는 16슬롯 주행 차량을 추가하고, 빔 포획 시 주행 AI에서 물리 오브젝트로 전환한다.
- 비행: 일반/터보 속도를 30/54, 가속도를 38, 최대 고도를 130으로 올렸다.

## 2026-08-23 — 모바일 드래그 조준

- 사람의 요청: 모바일에서 화면을 터치한 채 움직이면 그에 따라 조준점이 움직여야 한다.
- 문제: 조준점이 커서 위치를 그대로 쓰는 절대 매핑이라 손가락에는 맞지 않았다. 터치한 자리로 조준점이 순간이동했고, 조이스틱이나 발사 버튼을 누르면 그 위로 끌려갔으며, 손을 떼면 조준이 그대로 남아 있어야 하는데도 마지막 터치 지점에 붙었다.
- Codex 구현: `src/core/aim.ts`에 순수 함수 `absoluteAim`(마우스)과 `dragAim`(터치)을 분리했다. 터치는 상대 조준으로, 손가락이 움직인 만큼 조준점을 밀고 손을 떼도 그 자리에 남는다. 한 손으로 조준하고 다른 손으로 발사할 수 있다.
- 감도: 게인 1.4다. 1:1이면 화면 끝까지 반 화면을 쓸어야 해서 엄지가 닿지 않는다. 1.4면 화면의 약 1/3 스와이프로 중앙에서 가장자리까지 간다.
- 입력 분리: `.mobile-controls`와 버튼 위에서 시작한 터치는 그 컨트롤의 입력이므로 조준을 건드리지 않는다. 조준 드래그는 모바일 입력으로 취급해 마우스 조향 경로로 새지 않게 했다. `.mobile-controls`를 노출하는 미디어 쿼리와 같은 조건에서만 동작하므로 터치스크린 데스크톱은 커서 매핑을 유지한다.

## 2026-08-23 — 빔이 스치기만 해도 터지던 도시 구조물

- 사람의 피드백: 버스정류장이나 전파탑이 빔에 스치기만 했는데 폭발 이펙트가 발생하며 사라졌다.
- 문제 1(소품): 흡수 판정이 "빔 원뿔 안에 있는가, 충분히 가까운가"만 물었다. 들어올릴 수 없는 무게(`beamLiftScale`가 0인 밴드)여도 저공 비행 중 흡수 거리(최대 3.48) 안에 들어오면 그대로 삼켰다. 타깃 링은 들 수 없는 대상에 일부러 표시하지 않으므로, 아무 표시도 없던 정류장이 임팩트 스파크와 함께 사라지는 것으로 보였다.
- 문제 2(랜드마크): 빔이 켜져 있는 동안 전파탑·주유소는 원뿔이 밑동에 닿는 순간 `detonateLandmark`로 폭발했다. 기체 크기·빔 강도와 무관한 판정이라 조준하지 않은 스침 한 번이 곧 철거였다.
- Codex 구현: `beginNearbyBeamObjectAbsorption`에 `gripStrength` 인자를 추가하고, 도시 소품(`worldProp`)은 들어올릴 수 있는 무게일 때만 흡수하도록 했다. 차량·군중·기계는 월드에 고정된 자리가 없으므로 기존 규칙을 유지한다.
- Codex 구현: 전파탑은 빔 접촉 폭발 경로에서 제외했다. 질량 11의 소품이므로 충분히 강한 빔이면 뽑아서 옮기고 삼키며(기존 lifted comms 풀), 폭파는 레이저의 몫이다. 삼켜서 제거한 경우에도 `destroy-comms` 미션 진행과 목표 마커가 갱신된다.
- Codex 구현: 주유소는 들어 옮길 모델이 없으므로 빔 경로를 폭발로 유지하되 `GAS_STATION_BEAM_MASS = 9` 무게 사다리를 통과해야 한다. 파일런(6)보다 무겁고 전파탑(11)보다 가볍다.
- 영향: 초반의 약한 빔으로는 정류장·벤치·가로등·옥상 구조물을 스치듯 먹을 수 없다. 무게 사다리가 실제로 지켜지므로 `absorb-rooftop-structures` 같은 퀘스트는 성장·그립 카드로 빔을 키운 뒤의 목표가 된다.
- 검증: TypeScript, 단위 테스트 279개, 프로덕션 빌드를 통과했다. `test/beam.spec.ts`에 약한 빔의 정류장 스침·강한 빔의 흡수·차량 예외를, `test/city-landmarks.spec.ts`에 주유소 무게 사다리를 추가했다.

## 2026-08-23 — 카드 시스템 제거와 UFO부붐 픽업

- 사람의 결정: 강화 카드 시스템을 통째로 제거한다. 양력은 UFO가 커질수록 1→40으로, 빔 흡수력은 1→12로, 빔 범위도 크기에 따라 자연히 오르게 한다. 쉴드와 회전력 강화 개념은 뺀다. 레이저 위력·속도·터보 충전·터보 양은 미스터리 서클 상공에 상하로 떠 있는 UFO부붐을 먹어 올리고, 능력치가 만땅이면 부붐이 체력을 회복시킨다.
- Codex 구현: `core/upgrades.ts`·`core/shield.ts`와 'upgrade' 페이즈, 카드 오버레이를 삭제했다. `core/size.ts`가 양력(지수 0.75로 초반 가속, 1→40), 정수 흡수력 1→12, 빔 조리개 ×1→×1.75를 전부 크기에서 계산한다. 건물 무게 사다리(8~11)는 흡수력 7~10 구간에서 순서대로 열린다.
- Codex 구현: `core/boons.ts`를 추가했다. 서클 id 해시로 정해지는 부붐 4종(레이저 5·속도 3·터보 충전 3·터보 양 3), 서클당 1회 획득, 만렙 스탯은 배제, 전부 만땅이면 회복 1.5칸, 체력도 가득이면 점수로 전환한다. 획득 판정은 시뮬레이션과 렌더가 같은 `boonHoverY(sessionTime)`을 사용해 그려진 위치 그대로 먹힌다.
- Codex 구현: 터보 기본 지속을 2.5초에서 5초로 되돌렸다(부붐 +1.5초×3 → 최대 9.5초). `world.ts`에 섹터 인덱스 기반 `mysteryCirclesAround`를 추가해 레이더가 미획득 부붐 서클을 금색 사각으로 표시하고, `CityLandmarks`의 인스턴스 풀 하나가 스탯 색으로 물든 부붐 접시를 서클 위에 띄운다.
- 검증: TypeScript, 단위 테스트 320개(부붐·서클 인덱스 신규 스펙 포함), 프로덕션 빌드를 통과했다. `size.spec`은 크기 단독 성장 수치(1→40, 1→12, ×1.75)를, `buildings.spec`은 카드 없는 무게 사다리를, `ballast.spec`은 성장에 따라 넓어지는 빔을 고정한다.

## 2026-08-24 — 랜드마크 레이저 2방 판정과 피격 플래시 전면 적용

- 사람의 결정: 주유소·전파탑도 레이저 두 방 판정으로 하고, 레이저에 맞는 오브젝트는 전부 빨간 피격 반짝임을 보여준다.
- Codex 구현: `LANDMARK_LASER_HITS = 2`와 `damageLandmark` 누적기를 추가했다. 레이저 위력 배율이 그대로 적용되어 만렙 레이저(×2.0)는 다시 한 방이 된다. 빔 경로(무게 사다리 철거·전파탑 견인)는 그대로다.
- Codex 구현: 적 슬롯에 `hurt`(1→0, 건물 플래시와 같은 0.2초 감쇠)를 추가하고 `hitEnemy`가 세팅, `stepEnemies`가 감쇠시킨다. 렌더는 건물과 같은 `BUILDING.LASER_HIT` 색으로 인스턴스 색을 보간한다. 공중전함은 지속 사격 중 실루엣이 사라지지 않게 30%만 물든다.
- Codex 구현: `TransitUtilityPool`에 랜드마크 id→인스턴스 슬롯 맵을 두고, 건물의 `paintLaserHits`와 같은 매 프레임 페인트로 주유소·전파탑을 붉게 밝힌다. setColorAt 최초 호출이 0으로 채운 버퍼를 만들므로 리빌드 때 전 인스턴스를 흰색으로 초기화한다.
- 이미 있던 것: 건물 피격 플래시(`buildingHitFlash`), 자동차·트럭·유조차는 한 방에 폭발·발사되므로 플래시 대상이 아니다.
- 검증: TypeScript, 단위 테스트 323개(랜드마크 2방·적 hurt 감쇠 신규), 프로덕션 빌드 통과.

## 2026-08-24 — 대공포 3초 조준과 7발 탄막

- 사람의 결정: 대공포가 조준할 때 주황색 조준점이 3초간 보이고, 그 뒤 탄막을 발사한다.
- Codex 구현: 대공포 텔레그래프를 0.8초에서 `ANTI_AIR_TELEGRAPH = 3`초로 늘리고, 발사를 단발 미사일에서 `fireAntiAirBarrage`로 바꿨다. 잠긴 조준점에 1발, 사격 방향에 수직인 평면의 반경 7.5 육각 링에 6발 — 합계 7발 커튼이다. 수직 사격에서 기저가 무너지지 않게 up 벡터 폴백을 둔다.
- Codex 구현: 경고 링을 대공포 발밑이 아니라 잠긴 조준점 위치에, 커튼 반경(7.5) 크기의 주황(#ff9a3d) 링으로 그린다. 조준선도 같은 주황으로 맞춰 락과 착탄 지점이 하나의 경고로 읽힌다. 조준 예측은 조준 시작 시점 고정이므로 3초 안에 기수만 틀면 피할 수 있고, 직진 유지가 커튼에 꽂히는 구조다.
- 검증: TypeScript, 단위 테스트 324개(3초 락·7발 커튼·발사 전 무발사 신규), 프로덕션 빌드 통과.

## 2026-08-24 — 대공포 탄막을 링 산탄에서 조준점 연사로 교체

- 사람의 결정: 탄막은 흩뿌리는 방식이 아니라 주황 조준점 한 곳으로 5발이 다다다다 이어 붙어 나가게 한다. 조준점을 안 피하면 연속으로 맞는 구조다.
- Codex 구현: 링 산탄(`fireAntiAirBarrage`)을 제거하고 버스트 방식으로 바꿨다. 락 종료 순간 1발, 이후 `burstLeft`와 0.13초 간격(`ANTI_AIR_BARRAGE_INTERVAL`)으로 4발 — 5발 전부 잠긴 조준점 한 지점으로 같은 궤적을 탄다. 마지막 발 후 재장전 3.8초(`ANTI_AIR_RELOAD`). `burstLeft`는 보스 전용 경로와 분리돼 있어 재사용해도 충돌이 없다.
- Codex 구현: 조준점 링 크기를 커튼 반경(7.5)에서 점 표식(3.4)으로 줄였다. 위치·주황색·조준선은 그대로다.
- 검증: TypeScript, 단위 테스트 324개(스트림 타이밍·단일 궤적·재장전 단정으로 교체), 프로덕션 빌드 통과.

## 2026-08-24 — 대공포 체력 하향, 레이저 맞는 쓰레기통, 헬리콥터 감속

- 사람의 결정: 대공포와 쓰레기통도 피격되게 하고(체력 수치는 위임), 쓰레기통은 레이저에 맞으면 날아가며 쓰레기를 흩뿌린다. 헬리콥터 속도는 반으로 줄인다.
- Codex 구현: 대공포는 원래 레이저 판정이 있었지만 체력 10이 사실상 무적으로 읽혀서 4로 내렸다(레이저 만렙이면 2발). 다음 락이 오기 전에 옥상을 정리하는 플레이가 성립한다.
- Codex 구현: 쓰레기통을 도시 소품 중 유일하게 레이저 구체 판정에 포함했다. 맞으면 `beginTrashBinLaunch`가 자동차 격추와 같은 경로로 날려 보내되 폭발은 큐에 넣지 않고(+30점, 재생성 없음), 빔 운반 중에만 흩날리던 쓰레기 조각 풀을 발사된 쓰레기통에도 붙여 날아가는 내내 쓰레기를 흩뿌린다.
- Codex 구현: 헬리콥터 순항 속도를 15에서 7.5로 반감했다. 소총 사거리에서 자기 예광탄보다 빨리 화면을 가로지르던 기체가 실루엣이 약속하는 배회형 위협으로 읽힌다.
- 검증: TypeScript, 단위 테스트 327개(쓰레기통 발사 3종·대공포 체력 고정 신규), 프로덕션 빌드 통과.

## 2026-08-24 — main 병합: 능력치 개편 브랜치와 적 로스터 개편 결합

- 병합 판단: 카드 제거·부붐 브랜치와 main의 적 개편(드론=기뢰, 헬기=램 매복, 탱크 삭제, 전투기 부채꼴·전투함 링 탄막, 소품 레이저 판정, 미스터리 서클 레이더 아이콘)을 결합했다.
- 충돌 해소: 소품 레이저는 main의 `LASERABLE_PROPS` 구조를 채택하고 쓰레기통만 발사+쓰레기 흩뿌리기 특수 분기로 통합했다. 중장비는 main의 체력형 `registerHeavyVehicleLaserHit`에 부붐 배율을 연결했다. 대공포는 3초 락+5연사와 체력 4를 유지했고, 레이더는 main의 서클 아이콘 렌더에 "부붐을 먹어야 흐려짐" 규칙을 얹었다. 헬기 감속은 main의 매복 추격 상수에 적용해 순찰 3.5·추격 21(순항 30 미만)로 낮추고, 추격이 순항을 따라잡던 스펙 3개를 새 규칙(정지 표적은 3초 내 램, 이동 표적은 이탈)으로 교체했다.
- 정리: 카드 시스템은 병합 후에도 완전 삭제 상태를 유지한다(`upgrades.ts`·카드 오버레이·관련 문자열). 중복이던 `mysteryCirclesAround`는 main의 `mysteryCirclesNear`로 통일했다.
- 검증: TypeScript, 단위 테스트 342개, 프로덕션 빌드 통과.

## 2026-08-24 — 부붐을 상공의 럭키박스로, 주유소 경고·폭발 데미지 제거

- 사람의 피드백: 미스터리 서클 위 부붐이 너무 작아 안 보인다. 마리오 럭키박스 모양의 큰 금색 "?" 블록으로 바꾸고 50~80m 상공에 띄우며 금빛 반짝 파티클을 두른다. 주유소의 빨간 경고 구체 이펙트와 폭발 데미지는 삭제한다.
- Codex 구현: 부붐을 4.6m 노란 "?" 블록으로 교체했다. 면 텍스처(노란 벽돌·모서리 리벳·흰 물음표)는 캔버스로 그려 에셋 예산을 쓰지 않고, 같은 텍스처를 발광 맵으로 써서 밤에도 빛난다. 서클마다 해시로 정해지는 50~80m 고도에서 천천히 회전하며 부유하고, 상자당 46개의 금빛 입자가 황금각 분산 궤도로 돌며 반짝인다(쓰레기 조각처럼 시뮬레이션 없는 시계 기반).
- 설계 귀결: 시작 기체의 상승 한계(~31m)로는 못 닿고 크기 ~1.5부터 50m, ~3.5부터 밴드 전체가 열린다. 멀리서 보이는 보물을 성장으로 따라잡는 구조다. 픽업 판정도 상자 크기에 맞춰 수평 8m·수직 6m로 넓혔다.
- Codex 구현: 주유소의 기뢰형 빨간 경고 구체 풀을 삭제하고, 유조차 폭발이 선체를 깎던 2.5칸 피해를 제거했다(연출·현기증·경고음은 유지).
- 검증: TypeScript, 단위 테스트 344개(50~80m 밴드 스펙 갱신), 프로덕션 빌드 통과. 실제 브라우저 비행으로 55유닛 밖에서 상자와 파티클이 또렷이 보이는 것을 스크린샷으로 확인했다.
## 2026-08-24 — 미스터리 서클 신호를 파티클에서 금빛 역빔으로 교체

- 사람의 결정: 파티클을 흩뿌리는 지금 연출은 잘 보이지 않는다. UFO 트랙터 빔 연출을 뒤집어 미스터리 서클 위에 세우고, 밤에도 잘 보이도록 금빛으로 발광시킨다. 빔 안에서 도는 원(플로우 링)은 흡수하는 느낌을 주는 장치이므로 서클에는 넣지 않고 원뿔대만 세운다. 약간 일렁여서 신비하게.
- Codex 구현: 서클당 106개씩 뿌리던 `MysterySignalPool`을 통째로 걷어내고 `MysteryBeaconPool`로 교체했다. 지면에 뿌리 반경 10.2, 하늘 쪽 입구 반경 18.6, 높이 82의 열린 원뿔대를 세운다. 코어 셸과 1.16배 헤이즈 셸이 같은 지오메트리를 인스턴스 행렬로 나눠 쓰고, 지면에는 반경 17.5의 발광 원반이 깔린다. 서클이 몇 개 보이든 드로우는 3개다.
- Codex 구현: 셸은 전용 `ShaderMaterial`이다. 실루엣(프레넬)에서 가장 진하고 정면에서 옅어 원뿔대가 속이 빈 빛 덩어리로 읽히며, 뒤 건물이 가운데로 비친다. 밝기는 지면에서 가장 세고 위로 갈수록 떨어지며 입구 전에 사라져 윗단이 링으로 잘리지 않는다. 색은 지면 금빛(#ffeeb0)에서 입구 호박색(#ffa32e)으로, 낮에는 호박색 쪽으로 더 치우친다 — 밝은 하늘에 흰빛을 더하면 서치라이트가 되기 때문이다. 알파는 `nightFactor`로 낮 0.55, 밤 1.0까지 오른다.
- Codex 구현: 일렁임은 두 갈래다. 정점에서 높이 제곱에 비례해 기둥이 기울고 숨 쉬듯 아가리가 벌어지며(뿌리는 마크에 고정), 프래그먼트에서 위로 흐르는 넓은 사인 두 개가 서로 어긋난 채 밝기를 흔든다. 링처럼 또렷한 띠는 일부러 피했다 — 그건 트랙터 빔이 "빨아들이는 중"이라고 말하는 방식이다. 셀 해시로 서클마다 위상이 달라 한 구역의 서클들이 같은 박자로 흔들리지 않고, 헤이즈가 코어보다 더 크게 기울어 윤곽이 겹치지 않는다.
- Codex 구현: 부붐을 이미 먹은 서클은 0.55로 어두워진다. 레이더 아이콘과 같은 규칙(지나간 게 아니라 먹은 기준)이지만, 서클 3곳 통과 미션이 남아 있으므로 끄지 않고 낮추기만 한다.
- 검증: TypeScript, 단위 테스트 353개, 프로덕션 빌드 통과. 브라우저에서 야간 원거리·상공·주간 스크린샷으로 육안 확인했고 부붐 획득 후 감광도 실제 런에서 확인했다. 첫 프레임 8초 예산을 요구하는 smoke 1건은 이 소프트웨어 렌더링 환경에서 변경 전에도 동일하게 실패한다.

## 2026-08-24 — 성장 연동 빔 도달·견인 버프, 성장 체력 +2, 럭키박스 3배

- 사람의 피드백: UFO가 커지면 빔이 지면에 닿는 거리와 흡수력(끌어올리는 속도)도 자연히 더 세져야 한다. 체력도 크기에 따라 최대 7(+2)까지. 럭키박스는 부피 3배 더 크게.
- Codex 구현: 빔 도달 `beamReach` ×1→×2.75(일반 30→약 83m, 부스트 47→약 130m)와 견인 `beamPull` ×1→×1.55(스프링에 두 번 곱해져 체감 약 2.4배)를 크기 프로필에 추가했다. 삼킴 판정 창도 3.48m에서 성장 시 최대 약 14m까지 열려, 거대 기체가 잡은 것을 선체 안까지 끌고 들어가서야 먹던 어색함을 없앴다. 스프링 던짐 한계는 부스트 빔 기준(47)으로 클램프해 가벼운 물체가 순간이동하던 부작용을 막았다.
- Codex 구현: 성장 하트를 추가했다. 크기 진행 1/3에서 +1, 2/3에서 +2(최대 7). 새 하트는 가득 찬 채로 생기며(`raiseHealthMax`), 크기가 줄지 않듯 하트 상한도 줄지 않는다.
- Codex 구현: 럭키박스 한 변을 4.6→6.6m(부피 약 3배)로 키우고 반짝 궤도·수직 산포를 상자 실루엣에 맞춰 확장했다.
- 검증: TypeScript, 단위 테스트 356개(성장 하트 2종 신규, 도달·견인·삼킴 창 스펙 갱신, 무거운 짐 견인 창 재고정), 프로덕션 빌드 통과.
## 2026-08-24 — 빔이 들지 못하는 무게는 아무것도 흡수하지 않는다

- 사람의 요청: 흡수 가능한 오브젝트를 전체적으로 보고, UFO 크기보다 작더라도 빔으로 움직일 수 없는 것(양력 1일 때 무게 3짜리 물건)은 흡수하지 못하게 한다.
- 문제: 2026-08-23 수정은 무게 사다리 검사를 도시 소품(`worldProp`)에만 적용하고 차량·군중·기계는 "월드에 고정된 자리가 없다"는 이유로 면제했다. 그런데 자동차 무게는 3이고 시작 기체의 빔 강도는 1이다. 즉 타맥에서 1cm도 들어올릴 수 없는 자동차를 저공으로 스치기만 하면 그대로 삼켰다. 트럭(4)·유조차(5)도 같은 구멍에 있었다. 벤치에는 사다리를 물리고 정작 사다리의 주인공인 자동차에는 면제해 준 셈이라 방향이 뒤집혀 있었다.
- 표시와의 불일치: 타깃 링(`BeamTargetRings`)은 이미 `beamLiftScale <= 0`인 대상에는 종류를 가리지 않고 표시하지 않는다. 아무 표시도 없던 자동차가 임팩트 스파크와 함께 사라지던 것은 시뮬레이션만 규칙을 어기고 있었기 때문이다.
- Codex 구현: `beginNearbyBeamObjectAbsorption`의 무게 검사에서 `object.worldProp` 조건을 제거했다. 고정물이든 유동물이든 밴드를 넘으면 밴드를 넘은 것이고, 흡수되지 않는다. `beginNearbyCrowdAbsorption`에도 선택적 `gripStrength` 인자를 뚫어 두었다(기본값은 기존과 같은 무제한).
- Codex 구현: 주행 차량 포획도 같은 사다리로 막았다. 포획은 자동차를 도로 AI에서 영구히 빼내는 동작인데, 들 수도 삼킬 수도 없는 빔이 그렇게 하면 죽은 차가 도로에 남고 32개 주행 슬롯만 소모된다. 밴드 아래에서는 원뿔이 지붕 위로 지나가고 차는 계속 달린다.
- 영향: 시작 기체(강도 1)가 먹을 수 있는 것은 고양이(1)와 보행자(2)뿐이다. 자동차(3)는 강도 2, 트럭(4)·정류장(5)은 강도 3부터 열린다. 빔 강도는 크기를 따라 오르기만 하므로 거절이 아니라 유예다. 성장 루프가 스스로 조건을 말하게 된다.
- 부수 효과: 유조차가 흡수 대상에서 빠지면서 저공 접근 시 기폭만 남았다. 이 항목은 아래 "유조차를 폭발 대상에서 빼고"에서 기폭 자체를 제거해 해소했다.
- 검증: TypeScript, 단위 테스트 340개, 프로덕션 빌드를 통과했다. `test/beam.spec.ts`의 소품 테스트를 종류 무관으로 넓히고, 시작 기체가 자동차를 거절했다가 강도 2에서 삼키는 케이스와 고양이·보행자가 시작 밴드 안에 남아 있는지를 추가했다.

## 2026-08-24 — 몸통보다 넓은 것은 삼키지 못한다 (크기 게이트 복구)

- 사람의 정정: 앞 항목의 조건은 "UFO 사이즈보다 작고"가 아니라 **"UFO 사이즈보다 크고"**였다. 몸통보다 큰 물건은 흡수하지 못해야 한다.
- 문제: `GameContext`의 `maxAbsorbDiameter`가 `Number.POSITIVE_INFINITY`였다. 크기 게이트 자체가 꺼져 있었고, `isAbsorbable`의 주석만 "UFO 지름의 1/3 이하"라는 실제로 존재하지 않는 규칙을 설명하고 있었다.
- 지금 당장 바뀌는 것은 없다: 강도가 크기에서만 나오는 현재 모델에서는 모든 오브젝트에서 무게 사다리가 몸통 게이트보다 늦게 열린다(자동차는 몸통 0.54·무게 1.13, 정류장은 1.34·3.77). 플레이어가 실제로 체감하는 것은 무게이고 이 게이트는 단독으로 발동하지 않는다. 이건 밸런스가 아니라 불변식이다 — 질량이나 지름을 재조정하거나, 몸통을 키우지 않고 강도만 올리는 것이 생기는 순간 2m짜리 접시가 8.6m 지하철 입구를 삼키는 걸 막는 것이 이 게이트다.
- Codex 구현: `maxAbsorbDiameter = ufoDiameter(game.sizeProfile.size)`로 크기 게이트를 켰다. 규칙이 두 개로 갈린다 — **무게는 무엇을 옮길 수 있는지를, 폭은 무엇을 삼킬 수 있는지를 정한다.**
- 끌기는 건드리지 않았다: 몸통보다 넓어도 빔이 들 수 있으면 그대로 들어 올려 매달고 다닌다. 삼켜지지 않을 뿐이라 `beamBallast`가 무게를 매기고, 옮기거나 빔을 끊어 떨구면 된다. 타깃 링은 "끌 수 있다"는 약속이고 그 약속은 그대로 지켜지므로 링 규칙도 바꾸지 않았다(`BeamTargetRings` 주석에 구분을 남겼다).
- 배경: `isAbsorbable`은 원래 `maxDiameter` 인자를 받고 몸통 규칙을 주석으로 설명하고 있었지만, 호출부가 `Number.POSITIVE_INFINITY`를 넘겨 한 번도 적용된 적이 없었다. 늘 원하던 숫자를 이제 넘겨준다.
- 검증: TypeScript, 단위 테스트 342개, 프로덕션 빌드를 통과했다. `test/beam.spec.ts`에 카드를 두른 시작 기체가 정류장·지하철 입구를 거절했다가 몸통이 8.6m가 되면 삼키는 케이스와, 무게·폭 두 게이트가 서로 독립임을 확인하는 케이스를 추가했다.

## 2026-08-24 — 유조차를 폭발 대상에서 빼고 흡수 대상으로 되돌린다

- 사람의 요청: 이제 빔으로 흡수할 수 있게 되었으니 유조차도 폭발 대상에서 빼고, 흡수 가능하게 하되 폭발하지 않게 한다.
- 문제: `detonateReachedHazard`가 빔에 잡힌 유조차를 3.4m 안으로 끌어들이는 순간 기폭시켰다. 도로에서 가장 값비싼 물건(240점)이 게임이 한 판 내내 가르치는 동사를 벌하는 유일한 오브젝트였다. 크기·무게 게이트가 생기면서 초반에는 아예 삼킬 수 없게 되었으므로, 저공에서 빔을 켠 채 지나가면 흡수 대신 폭발만 남는 상태였다.
- Codex 구현: `detonateReachedHazard`와 `HAZARD_TRIGGER_DISTANCE`, 기폭 래치(`detonated`), 경고 램프(`alarm`)를 제거했다. 유조차는 트럭과 같은 평범한 중량 차량이 되어 크기·무게 게이트만 통과하면 삼켜진다. 레이저로 쏘면 여전히 폭발하며(`damageHazard`, HP 3), `destroy-tanker` 미션은 흡수와 격추를 모두 센다.
- 확인: `alarm`은 이미 어디에서도 렌더링되지 않는 죽은 상태였다(경고 구체는 이전에 제거됨). 플레이어에게 노출되는 문구 중 유조차 폭발을 경고하는 것은 없다.
- 남은 것: 기폭이 사라지면서 `wound(game, 'explosive')`와 `DAZE_TIME`을 세팅하는 유일한 경로가 없어졌다. `daze`는 HUD `data-dazed`와 비행 항력에 걸린 범용 상태라 지우지 않고 남겨 두었다 — 현재 트리거는 없다.
- 검증: TypeScript, 단위 테스트 341개, 프로덕션 빌드를 통과했다. `test/hazards.spec.ts`의 경고·기폭 테스트를 유조차 흡수 테스트로 교체하고, 몸통이 좁거나 빔이 약할 때는 여전히 거절하면서도 폭발하지 않는지를 확인한다.

## 2026-08-24 — 과적 추락에 자기 엔딩을 준다

- 사람의 요청: 추락했을 때는 엔딩을 따로 넣는다. "무게를 감당해서 추락했다"는 뉘앙스여야 한다.
- 문제: 과적 추락은 이미 별개의 규칙으로 존재했지만(`shouldCrashFromOverload` — 빔 온·적재 초과·지면 접촉 셋이 동시에 성립할 때) 결과 화면은 `endRun(game, 'CRUSHED BY THE LOAD', 'downed')`로 격추와 같은 엔딩을 썼다. 화면에는 '지구가 좀 치네?' / '정찰은 망했지만 고양이 얘기는 건졌습니다'가 떴다. 도시에 맞아 떨어진 것과 자기 짐에 눌려 내려앉은 것이 같은 문장을 받은 셈이라, 플레이어는 무엇에 죽었는지도, 무엇을 했어야 했는지도 화면에서 알 수 없었다.
- 왜 합치면 안 되는가: 격추는 도시가 이긴 것이고 과적 추락은 자기 적재가 이긴 것이다. 후자는 `E`를 놓는 순간 언제든 피할 수 있었다 — 규칙 주석이 이미 "releasing E is always an escape"라고 말한다. 그 유일한 대응책이 결과 화면에서 말해지지 않으면 규칙이 존재하지 않는 것과 같다.
- Codex 구현: `RunEnding`에 `'crushed'`를 추가하고 과적 추락 경로만 그쪽으로 돌렸다. `isVictory`는 그대로다(추락은 추락이다). `resultCopy`는 `'crushed'` 분기를 하나 더 갖고, 격추는 지금까지의 화면을 그대로 유지하며 여전히 폴백이다.
- 문구: 한·일·영 세 언어에 `crushedTitle`/`crushedLead`를 넣었다. 무게를 감당하지 못했다는 것을 제목과 본문 양쪽에서 말한다 — '무게에 눌려 추락' / '빔에 매단 짐이 출력을 이겼습니다. 끝내 무게를 감당하지 못한 접시는 짐과 함께 지면에 처박혔습니다.'
- 건드리지 않은 것: 추락 조건, 과적 하강 물리, `overloadWarn` 게이지, 결과 화면의 `victory`/`defeat` 클래스. 바뀐 것은 끝난 뒤에 무엇이라고 불리는지뿐이다.
- 검증: TypeScript, 단위 테스트 357개, 프로덕션 빌드를 통과했다. `test/ending.spec.ts`에 `'crushed'`를 엔딩 목록·승패 판정·언어별 문구 중복 검사에 넣고, 과적 규칙이 실제로 발동하는 입력에서 그 문구가 격추 문구와 다르고 무게를 지목하는지 확인하는 케이스를 추가했다.

## 2026-08-24 — 피격은 종류를 가리지 않고 흔들리고 붉게 점멸한다

- 사람의 요청: UFO는 헬기든 전투기든, 대공포든 전투함이든 맞았을 때 자폭 드론에 맞았을 때처럼 흔들리는 효과와 빨간색으로 반짝이는 피격 효과를 준다. 언제든 맞으면 이 효과가 발생해야 한다.
- 문제: 화면·기체 흔들림(`addShakeTrauma`)을 넘기는 호출부는 드론 기폭과 헬기 램 두 군데뿐이었다. `registerImpact`의 `trauma` 기본값이 0이라, 탄막·대공포 대형탄·함수포·건물 충돌은 체력을 깎으면서도 화면과 선체가 미동도 하지 않았다. 느낄 수 없는 피격은 체력 바를 봐야만 알 수 있는 피격이고, 체력 바는 교전 중에 아무도 보지 않는 곳이다.
- Codex 구현: `core/shake.ts`에 `HIT_TRAUMA`를 두어 피해 종류별로 흔들림 세기를 정한다(건물 스침 0.4 ~ 기뢰 폭발 0.85). `registerImpact`는 `trauma`를 받지 않으면 이 표에서 꺼내 쓰므로, 체력을 깎는 경로는 예외 없이 흔들린다. 명시 인자는 같은 `contact` 피해가 세 갈래로 도착할 때만 쓴다 — 기뢰 기폭, 헬기 램, 그리고 그냥 부딪힌 것.
- Codex 구현: 붉은 플래시를 페이드에서 점멸로 바꿨다. `IMPACT_FLASH_FADE`를 5에서 2.7로 낮춰 플래시가 0.37초 살아 있게 하고, 선체 틴트를 `IMPACT_BLINK_RATE`(초당 약 5회)로 흔들어 그 사이 두 번 깜빡이게 했다. 미스터리 서클의 금빛 점멸이 이미 쓰던 방식이다. 스냅샷 대신 런타임 값을 직접 읽는다 — 스냅샷은 초당 16회 발행이라 점멸을 계단으로 만든다.
- 검증: TypeScript, 단위 테스트, 프로덕션 빌드 통과. `test/shake.spec.ts`에 모든 피해 종류가 실제로 눈에 보이는 흔들림을 만들고 한 방으로는 게이지를 채우지 않는지, 그리고 표의 순서(기뢰 폭발이 최대, 건물 스침이 최소)를 고정하는 테스트를 추가했다.

## 2026-08-24 — 공중전함을 캐리어로: 10초 호위대·3층 탄막·두 포탑 대공 연사·추격

- 사람의 요청: 거대 전함은 나타나면 10초마다 드론을 주변에 스폰하고, 헬기를 한 개씩 주변에 소환하고, 전투기를 플레이어 쪽에 보내고, 탄환을 상하좌우로 더 흩뿌리고, 대공포의 기술도 2개씩 쓴다. 그리고 UFO가 한쪽으로 계속 도망쳐도 전함이 시야에 남도록, 일정 범위 벌어지면 빠르게 근처까지 쫓아온다.
- 호위대: `syncEnemyTiers`가 매 틱 `stepBattleshipEscort`를 돌린다. 전함이 하늘에 있는 동안 10초마다 기뢰 3, 헬기 1, 전투기 1이 선체 주변 반경 30 링에서, 플레이어가 있는 쪽 현측으로 치우쳐 발진한다. 용골 아래 최대 68m까지 흩어 떨어뜨리므로 전함에 접근하는 항로 자체가 기뢰 커튼이 된다. 전투기만 발진 순간 플레이어 쪽으로 기수를 맞춘다 — 전투기는 한 번 날면 조준을 고치지 않는 직선 유닛이므로, 가리키는 것이 곧 공격이다.
- 호위대의 자리: `ENEMY_CAPS`를 웨이브 목표(기뢰 34·헬기 14·전투기 6)보다 크게 올렸다(42·20·10). 상한과 목표가 같으면 전함이 띄울 슬롯이 없다. 이 차이가 곧 격납고이고, 상한은 그대로 총량의 천장이다. 스포너는 여전히 목표까지만 채우므로 정상 인구는 바뀌지 않는다.
- 탄막: 사방 링을 -0.34/0/+0.34 라디안 세 층으로 쌓았다(`BATTLESHIP_ORB_PITCHES`). 평면 링은 공중에서 원반이고, 원반은 이 게임이 가장 쉽게 만들어 둔 동작 — 고도 변경 — 으로 그냥 통과된다. 층마다 방위도 1/3 간격씩 어긋나 위아래로 뚫린 세로 길이 생기지 않는다. 속도는 층마다 같게 기울이기만 하므로 수평 속도는 오히려 줄고, 탄막은 여전히 순항 속도 아래다.
- 대공 연사: 전함이 대공포의 기술을 그대로 빌린다. 3초 조준(`ANTI_AIR_TELEGRAPH`) 뒤 두 포탑이 짝지어 쏘는 대형탄 3연사이고, 짝은 연사가 진행될수록 현측을 따라 걸어간다. 볼리 순환은 이제 링 → 대공 연사 → 함수포다. 조준 링도 대공포와 같은 규칙으로 그렸다 — 하늘에서 만나는 탄이므로 경고는 포탑이 아니라 착탄점에 있어야 한다.
- 추격: 지수 접근은 속도가 아니라 따라붙는 거리다. 목표점이 플레이어와 함께 움직이므로 한 방향으로 날기만 하면 간격이 플레이어 속도만큼 벌어졌고, 전함은 디스폰되지 않은 채 지평선 뒤로 사라져 마지막 웨이브가 빈 하늘이 되었다. 177m(궤도의 1.5배)를 넘으면 `chase`로 넘어가 정거리 유지를 버리고 실제 속도로 붙는다. 속도는 64와 플레이어 속도의 1.45배 중 큰 쪽이라 앞으로 속도 강화가 생겨도 약속이 깨지지 않으며, 궤도의 1.15배까지 좁혀야 풀리는 히스테리시스가 있어 "근처까지" 실제로 도착한다.
- 탄 풀: `ENEMY_MAX_PROJECTILES`를 96에서 160으로 올렸다. 3층 링 한 번이 36발, 한 플러리가 72발이라 96에서는 플러리 뒷부분이 조용히 사라지고 — `fireOrb`는 빈 슬롯이 없으면 그냥 false를 돌려준다 — 같은 순간 대공망과 전투기까지 벙어리가 되었다. 실측 최대 동시 탄은 113발이라 여유가 있다. 렌더 풀 3개도 하드코딩 96에서 상수 참조로 바꿨다.
- 부수 수정: 지면 아래로 내려간 투사체를 즉시 회수한다. 링의 아래층은 일부러 아래를 향하므로, 없으면 매 플러리의 3분의 1이 도시 밑을 8초 동안 날며 슬롯을 물고 있었다.
- 검증: TypeScript, 단위 테스트 362개, 프로덕션 빌드 통과. `test/battleship.spec.ts`에 호위대 발진 주기와 위치·기수, 3층 탄막, 두 포탑 짝사격, 터보로 90초 도주해도 177m 밖으로 못 벗어나는 추격을 추가했다. 웨이브 인구 테스트는 "상한과 목표가 같다"에서 "목표는 바닥, 상한은 천장"으로 고쳤다.

## 2026-08-24 — 로비에 공중전함 드릴 버튼

- 사람의 요청: 전투함을 바로 볼 수 있는 개발자 모드를 로비에 버튼으로 따로 만든다.
- 문제: 마지막 웨이브는 5분을 날아야 오고, 그동안 가장 자주 바뀌는 전투를 한 번 보는 데 5분이 든다.
- Codex 구현: `startBattleshipDrill`은 특수 모드가 아니라 평범한 런타임을 만들어 그 전투가 시작되는 상태로 감아 놓는다. 네 가지가 함께 움직여야 한다 — 시계를 마지막 웨이브 시각으로, 튜토리얼을 해제(첫 고양이를 잡기 전에는 웨이브 스포너가 조용하다), 미션을 3단계로(`startFinalMission`), 그리고 기체를 ×5.2로. 마지막 것은 취향이 아니라 천장 문제다: 시작 기체의 상승 한계는 30m이고 전함은 96m에 선다.
- Codex 구현: 스폰을 드릴 안에서 한 번 돌리고 실제로 놓인 전함의 방위를 읽어 기수를 맞춘다. 스폰 공식을 베껴 오는 것보다 낫다 — 공식이 바뀌어도 드릴은 그대로 동작하고, 뒤통수에 전함을 두고 시작하는 드릴은 드릴이 아니다.
- Codex 구현: 럭키박스는 주지 않는다. 강화 없는 레이저로 64 HP를 잡을 수 있는지가 이 드릴로 확인하려는 밸런스의 어려운 쪽이다.
- 노출 규칙: 개발 서버에서는 늘 보이고, 배포된 빌드에서는 `?dev=1`이나 `#dev`일 때만 보인다. 전함을 실제로 들여다보는 곳은 배포된 빌드인데, 그렇다고 모든 플레이어의 로비에 디버그 버튼을 둘 수는 없다. 버튼은 점선 테두리로 세 개의 진짜 입구 아래 한 줄을 차지한다 — 네 번째 플레이 방식이 아니라 작업실 문으로 읽혀야 한다.
- 격리: 드릴 판은 `devRun`으로 표시된다. 결과 화면은 랭킹 등록 버튼 대신 이유 한 줄을 보여 주고, 장군의 튜토리얼 브리핑도 뜨지 않는다 — 머리 위에 전함이 떠 있는데 빔 사용법을 읽어 주는 것은 잘못된 페이지다.
- 검증: TypeScript, 단위 테스트 364개, 프로덕션 빌드 통과. 실제 브라우저에서 버튼을 눌러 전함이 정면에 뜨는 것, 미션 3·남은 시간 2:00·질량 ×5.20·고도 88m를 스크린샷과 계측으로 확인했다. `startFinalMission`은 `test/missions.spec.ts`에서, 드릴 기체 크기가 전함 고도에 닿는다는 불변식은 `test/battleship.spec.ts`에서 고정한다.
## 2026-08-24 — 하늘의 총을 한 자루로 줄인다: 느린 붉은 구체 탄막

- 사람의 요청: 대공포·전투기·공중전함이 참고 이미지 같은 구체를 은은한 빨간색으로 쏘게 한다. 대공포의 사전 조준은 없앤다. 대공포와 전투기는 쿨타임마다 사거리 안에 UFO가 있으면 한 발씩 쏜다. UFO에 닿으면 피격 연출이 나오고 탄막은 사라진다. 피해는 체력 반 칸 정도. 탄속은 피할 수 있게 느리게.
- 문제 1 — 문법이 둘이었다. 대공포는 3초짜리 주황 락온과 붉은 점멸 빔을 그린 뒤 무거운 포탄 다섯 발을 그 지점에 쏟아부었다. 나머지 하늘은 느린 구체 탄막이었다. 하나는 "경고를 읽고 표시된 자리를 떠나라"고 요구하고 다른 하나는 "공기를 읽고 사이로 지나가라"고 요구한다. 동사가 하나인 게임에 사격 문법이 둘이었고, 예고가 거대해야만 포탄이 공정해지는 구조라 예고 자체가 화면을 잡아먹었다.
- 문제 2 — 탄막이 검게 나오고 있었다. `OrbPool`·`EnemyProjectiles`·`EnemyWarnings`·`EnemyAimLines`·레이저 폭발 풀이 전부 `vertexColors`를 켠 채 정점 색 속성이 없는 기본 지오메트리를 쓰고 있었다. 셰이더가 없는 `color` 속성을 곱해 결과가 검게 나오고, 가산 블렌딩이 걸린 것들은 아예 보이지 않았다. `City.tsx`가 자기 머티리얼 두 곳에 이미 기록해 둔 함정을 투사체 풀 전부가 밟고 있었다. 사람이 "저런 구체를 은은한 빨간색으로"라고 말한 이유가 여기 있었다.
- Codex 구현 (규칙): `EnemyProjectileKind`를 `'orb' | 'boss-beam'` 둘로 줄였다. `rifle`·`shell`·`missile`·`rocket`은 아무것도 쏘지 않는 죽은 항목이었고 `HEALTH_LOSS`·`splashIndex`에도 그대로 남아 있었으므로 같이 걷어냈다. 대공포는 락온·리드·연사·재장전을 전부 잃고 `ANTI_AIR_ORB_INTERVAL`(1.5초)마다 지금 있는 자리로 구체 한 발을 쏜다. 전투기는 시야 원뿔 안에서만 터뜨리던 5발 부채를 잃고 `FIGHTER_ORB_INTERVAL`(1.4초)마다 어느 방향에서든 한 발을 쏜다. 공중전함은 그대로다 — 링 탄막과, 예고선을 그리는 함수포 하나가 이 게임에 남은 유일한 조준 사격이다.
- 사거리와 탄속: `ORB_SPEED`를 16에서 14로, `ANTI_AIR_ORB_SPEED`를 17로 낮췄다. 둘 다 순항 속도(30)의 절반 아래다. 대공포 사거리는 미사일 시절의 145에서 80으로, 전투기는 90에서 70으로 줄였다 — 느린 탄이 그만큼 멀리서 날아오면 도착하기 전에 이미 빠져나간 뒤라 공중에 쓰레기만 남는다. 80미터는 5초 남짓의 비행이고, 그건 보고 답할 수 있는 사격이다.
- 고도 규칙: "28미터 위"라는 평평한 게이트를 "포대 자신보다 아래는 못 쏜다"로 바꿨다. 옥상 포대가 자기 건물을 뚫고 아래를 쏠 수는 없다. 낮은 건물에서는 같은 말이고 높은 건물에서는 임의의 숫자였던 규칙이 사라진다.
- 피해: `HEALTH_LOSS.orb`는 0.5 그대로다. 이제 보스 밖의 모든 총이 이 한 숫자로 값이 매겨진다. 대공포 포탄이 2였으므로 고도의 대가가 4분의 1로 내려간다 — 그게 요청이고, 예고를 없앤 대가이기도 하다.
- 피격 연출: `stepEnemyProjectiles`가 `projectileHit`과 `lastHitPoint`를 남긴다. 명중한 지점에서 붉은 폭발 링과 `'strike'` 화염이 터진다. 피해 무적 시간(1.05초) 안에 들어온 탄에도 연출은 나간다 — 탄이 아무 흔적 없이 사라지면 장갑이 버틴 게 아니라 그냥 통과한 것으로 읽힌다. 화면 플래시·히트스톱·체력은 지금까지처럼 실제로 들어간 피해만 따라간다.
- Codex 구현 (그림): `orbFlareTexture`를 절차 캔버스로 그렸다. 하얗게 타는 중심에서 필라멘트 열한 가닥이 말려 나간다. 불처럼 보이게 하는 것은 세 가지다 — 가닥이 뿌리에서 넓고 끝에서 거의 사라지도록 채워진 리본이라는 것(균일한 선은 철사로 읽힌다), 끝에서 갈고리로 꺾이고 멈춘다는 것(고리로 닫으면 꽈배기가 된다), 그리고 길이를 따라 흰색에서 붉은색으로 식는다는 것. 색은 텍스처에 구웠다 — 스프라이트 전체에 평평한 틴트를 곱하면 중심의 흰색과 가장자리의 붉은색을 동시에 가질 수 없고, 그 그라데이션이 타는 것과 그려 놓은 것의 차이 전부다. 렌더는 카메라를 향한 쿼드에 슬롯별 회전을 곱해 가닥이 천천히 돌아간다.
- `vertexColors` 함정: 문제 2에 해당하는 다섯 풀에서 플래그를 뺐다. 인스턴스별 색이 필요한 폭발 풀은 `setColorAt`만으로 그대로 동작한다(`City.tsx`가 이미 그렇게 기록해 두었다). 대공포가 예고를 잃으면서 `EnemyWarnings`와 `EnemyAimLines`는 공중전함 전용이 되었으므로 색이 하나로 줄어 머티리얼이 직접 들고 있게 했다.
- 검증: TypeScript, 단위 테스트 364개, 프로덕션 빌드를 통과했다. `test/danmaku.spec.ts`를 두 총의 쿨타임·사거리·무리드 사격과 명중 시 소멸·연출 지점으로 다시 썼고, `test/aiming.spec.ts`는 "직선으로 날면 맞는다"에서 "제자리에 있으면 맞고 움직이면 빠져나간다"로 바꿨다 — 리드사격이 사라졌으니 전자는 더 이상 참이 아니다. `test/enemies.spec.ts`의 락온·연사 케이스와 `test/health.spec.ts`의 사라진 피해 종류들도 함께 갱신했다.
- 남긴 것: 공중전함 함수포의 조준·예고·리드는 그대로다. 그 배가 보스로 읽히는 이유이고, 사람이 없애 달라고 한 것은 대공포의 사전 조준이다.

## 2026-08-24 — 호수 물가에 돌과 갈대를 두른다

- 사람의 요청: 물타일이 호수처럼 보이지 않으니 주변에 작은 풀이나 돌을 장식해 달라. 흡수는 못 해도 된다.
- 문제: 물 타일은 직사각형이다. 아무리 잔물결이 흘러도 직선 네 변이 직각으로 만나면 호수가 아니라 수영장으로 읽힌다. 기존 `lakeShoreTreesAround`는 물가가 아니라 이웃 셀 안쪽 9.5유닛에 나무를 놓기 때문에(물가는 도로가 지나간다) 수면 경계 자체는 손대지 않았다.
- Codex 구현: `lakeShoreDecorAround`가 수면 경계선을 따라 돌과 갈대 포기를 결정론적으로 배치한다. 물 밖 1.1유닛(마른 턱)부터 물 안 3.4유닛(얕은 물)까지의 띠 안에서, 변마다 9칸을 잡아 그중 약 5분의 1은 비우고, 절반 가까이는 절반 크기의 짝을 옆에 붙여 무더기로 만든다. 도로 폭(경계에서 4.5)보다 마른 턱이 훨씬 좁으므로 어떤 조각도 차도까지 나가지 않는다.
- 렌더: `City.tsx`의 `LakeShoreDecorPool` 두 개(돌·갈대). 돌은 면이 살아 있는 12면체를 허리까지 묻어 수면선이 가로지르게 하고, 갈대는 두 가지 초록을 구운 6가닥 포기다. 다른 지면 풀과 같이 셀 키가 바뀔 때만 다시 채운다.
- 흡수하지 않는 이유: 요청대로 순수 장식이다. 빔 오브젝트로 만들면 갈대 한 포기가 주차 차량과 같은 무게 사다리에 올라가는 대신 얻는 것이 없다.
- 겸사: 물 메시가 쓰던 여백 4.25가 `WaterPool` 안의 지역 상수였다. `world.ts`의 `LAKE_TILE_MARGIN`과 `lakeWaterRect`로 옮겨 물 메시·`lakeDepthAt`·물가 장식이 같은 수면선을 읽게 했다.
- 검증: TypeScript, 단위 테스트 361개, 프로덕션 빌드를 통과했다. `test/city-landmarks.spec.ts`에 모든 조각이 호수 셀 안에서 수면선 ±띠 안에 있는지와 결정론성·풀 상한을 확인하는 케이스를 추가했다. 브라우저에서 실제 호수(x 85~187, z 289)까지 비행해 물가를 눈으로 확인했다.

## 2026-08-24 — 밤에 금색으로 물들던 적을 제 색으로 되돌리고, 대신 광원을 달았다

- 사람의 요청: 드론·헬기·전투기가 드론의 붉은 쉐이더와 밤의 금색 물듦으로 제 색을 잃는다. 색깔을 가능한 유지하고, 밤에는 광원을 살짝 가져서 알아볼 수 있게 한다. UFO도 마찬가지로 밤이 깊을수록 광원을 더 준다.
- 문제(금색): `entityMaterials`의 적 재질은 종류별 발광색(헬기 `#d9b45b` 금색, 전투기 `#d98958` 주황)을 `emissiveIntensity` 0.04 → 0.74로 올리고 있었다. 발광은 표면색과 무관하게 평평하게 더해지므로, 밤 끝값에서는 조명을 받은 본체보다 몇 배 밝았다. 조종석·로터·꼬리를 구분해 주던 모델 자체의 색이 가장 필요한 시각에 사라지고 헬기는 금색 덩어리, 전투기는 주황 덩어리가 됐다.
- 문제(붉은 드론): `MinePool`이 매 프레임 인스턴스 색을 대략 (0.87, 0.25, 0.28)로 써 넣었다. 인스턴스 색은 정점 색에 곱해지므로 회색 프레임·금색 로터캡·마룬 탄두가 전부 한 덩어리 빨강으로 나왔다. 기폭 셸도 정면 헤이즈가 0.085로 두꺼워 그 위에 빨강을 한 겹 더 얹고 있었다.
- Codex 구현: `rimLight.ts`를 `nightVisibility.ts`로 넓혔다. 밤의 가시성을 세 항으로 나눈다 — (1) **리프트**: `diffuseColor.rgb * uNightLift`, 즉 표면이 *자기 색*을 다시 내보낸다. 색조는 그대로 두고 밝기만 오르므로 밤이 깊어져도 다시 칠해지지 않는다. 자기 밝기에 반비례해 `1 - 0.55 * luma`로 감쇠시키는데, 리프트는 어둠이 삼킨 것을 건지려고 있기 때문이다 — 새까만 꼬리 붐에는 전부 필요하고, 이미 모델에서 가장 밝은 크림색 기수에 같은 값을 주면 음영만 날아가 흰 덩어리가 된다. (2) **림**: 프레넬 가장자리에만 얹히는 종류색. 본체를 덮을 수 없는 자리에 종류 구분을 둔다. (3) **램프**: 지오메트리에 `aLamp`로 태그한 항법등. 세 항 모두 `setNightVisibility(nightFactor)` 하나로 움직인다.
- Codex 구현: 종류색 평면 발광은 0.74 → 0.1(전함 0.07)로 낮추고, 그 예산을 리프트(밤 0.72)와 램프(밤 2.6)로 옮겼다. 램프는 헬기 좌우 항법등·꼬리 비콘·복부 스트로브, 전투기 익단등·애프터버너, 대공포 경고등, 전함 마스트등·현등, 기뢰 신관과 로터 팁으로 달았다. 램프 색을 채도 낮은 흰빛으로 칠한 것은 의도다 — 톤 매핑은 진한 빨강을 진한 빨강으로 굴려 보내므로 그건 그냥 빨간 페인트가 되고, 옅은 색이라야 심지가 하얗게 타면서 포스트 패스의 밝은 영역 필터에 걸려 블룸이 생긴다.
- Codex 구현: 광원 자체는 `EnemyHaloPool` — 적마다 하나씩 카메라를 향하는 가산 빌보드다. 텍스처(`entityHaloTexture`)는 가운데가 비어 있고 반지름 절반에서 가장 밝다. 가운데가 밝으면 그 빛이 곧 선체 위에 덮여 다시 금색 물듦이 되기 때문이다. 밝기는 `nightFactor`의 제곱이라 낮에는 아예 없고 밤 바닥에서 가장 세다. 인스턴스 풀 하나이므로 드로우는 1이고, 씬 광원 개수는 그대로다(광원을 추가하면 씬의 모든 재질이 재컴파일된다).
- Codex 구현: 기뢰는 전용 `mineMaterial`을 버리고 `enemyMaterial.drone`을 함께 쓴다. 인스턴스 색은 평상시 0.86 회백색이라 구운 색이 그대로 나오고, 빨강은 **무장 상태에만** 남겨 신관이 도는 동안만 물든다. 기폭 셸의 정면 헤이즈는 0.085 → 0.042로 얇게, 대신 림을 0.82 → 0.92로 올려 경계는 더 또렷하게 했다. 폭발 반경을 알리는 역할은 그대로다.
- Codex 구현: UFO도 같은 규칙이다. 선체·돔 발광에 `nightFactor` 항(0.34/0.3)을 더해 자기 색을 더 내보내고, `UfoNightGlow` 빌보드가 밤에만 켜지며, 상시 마운트된 포인트 라이트 3슬롯 중 UFO 등의 세기만 `+ nightFactor * 0.62` 오른다. 슬롯 수는 건드리지 않는다.
- 검증: TypeScript, 단위 테스트 366개(`test/night-visibility.spec.ts` 10개 신설 — 램프 곡선, 셰이더 패치 삽입, 금색 물듦 회귀 가드), 프로덕션 빌드 통과. 실제 브라우저에서 전 종류 61기가 뜬 야간 런을 스크린샷으로 확인했고 셰이더 컴파일 오류는 없었다.

## 2026-08-24 — 조준 경고를 몸에 칠하지 않고 빛으로 낸다

- 사람의 지적: 앞 작업 뒤에도 드론과 헬기가 여전히 붉다.
- 문제: 남은 빨강은 밤 발광이 아니라 조준 표시였다. `EnemyPool`이 `enemy.aiming`일 때 인스턴스 색을 `#ff6573`으로 **덮어썼고**, 인스턴스 색은 구운 정점 색에 곱해지므로 모델 전체가 한 덩어리 빨강이 됐다. 헬기는 `aiming = mode === 'chase'`라 한 번 발견되면 추격이 끝날 때까지 계속 참이다. 즉 플레이어를 본 헬기는 그때부터 그냥 빨간 헬기였다. 전함은 "70m 선체가 한 색으로 덮이면 실루엣이 사라진다"는 이유로 이미 이 표시에서 빠져 있었는데, 화면에 훨씬 오래 떠 있는 작은 적들에는 같은 판단이 적용되지 않은 상태였다.
- Codex 구현: 경고를 인스턴스 색에서 빼고 **알럿 채널**로 옮겼다. 풀마다 `aAlert` 인스턴스 속성을 하나 두고(모델은 풀 전체가 공유하고 누가 조준 중인지는 매 프레임 바뀌므로 정점이 아니라 인스턴스다), `nightVisibility`가 그것으로 가장자리와 램프에 빨강을 **더한다**. 곱하지 않으므로 도장은 그대로다 — 대기 중인 헬기는 보라색, 조준 중인 헬기는 붉은 윤곽선을 두른 보라색이다.
- Codex 구현: 배분은 몸통 0.09, 프레넬 림 0.85(지수 2.7), 램프 2.9다. 지수가 세기보다 중요하다 — 헬기 동체는 캡슐이라 완만한 감쇠는 거의 전신을 스치고, 그러면 "가장자리"가 슬그머니 다시 몸통이 된다. 낮/밤 램프를 걸지 않은 것도 의도다. 경고는 정오에도 읽혀야 한다.
- Codex 구현: 피격 점멸은 몸통에 남겼다. 그건 상태가 아니라 순간이고 확실히 보여야 한다. 다만 지속 경고가 알럿 채널로 빠졌으므로 0.75 → 0.5로 낮췄다. 기뢰의 무장 점멸도 같은 이유로 몸통 비중을 0.55로 줄이고 알럿을 함께 켠다.
- Codex 구현: 기폭 셸의 대기 밝기를 `0.1 + breath² * 0.62` → `0.07 + breath² * 0.34`로 낮췄다. 기뢰밭은 하늘에서 가장 큰 빨강 덩어리인데, 예전 진폭에서는 무장하지 않은 기뢰가 "회색 물체에 그려진 경계"가 아니라 그냥 빨간 공으로 읽혔다. 무장 쪽 값은 건드리지 않았다 — 놓치면 안 되는 쪽은 그쪽이다.
- 확인: 레이저 연출은 이 작업에서도, 앞 작업에서도 건드리지 않았다. 메인에 들어온 `[codex] 하늘의 총을 하나로 줄이고 느린 붉은 구체 탄막으로 통일한다`가 적 투사체를 붉은 구체로 바꾼 별개 변경이다.
- 검증: TypeScript, 단위 테스트 393개(알럿 채널이 더하기이고 곱하기가 아님을 확인하는 케이스 포함), 프로덕션 빌드 통과. 브라우저에서 대기/조준 상태를 나란히 세운 임시 벤치로 육안 비교했다.

## 2026-08-24 — 호숫가에 나무를 한 무리씩 심는다

- 사람의 요청: 호수에 나무도 몇 개 심어 달라.
- 문제: `lakeShoreTreesAround`는 이미 있었지만 변마다 한 그루가 후보였고, 거기에 "이웃 셀이 완전히 비어 있어야 한다"는 조건과 18% 솎아내기가 겹쳐 호수당 평균 1.89그루에 그쳤다. 아예 한 그루도 없는 호수도 있었다. 두 그루는 우거진 물가로 읽히지 않는다 — 그냥 물 근처에 서 있는 나무 두 그루다.
- Codex 구현: 변마다 세 자리를 잡는다. 가운데 자리는 잘 비지 않고(8%), 양옆 두 자리는 더 세게 솎아낸다(26%). 깊이도 8.7/12.6/10.4로 어긋나게 두어 세 그루가 일직선으로 서지 않는다. 결과는 호수당 평균 5.53그루, 38개 중 36개가 나무를 갖는다.
- 도로를 침범하지 않는다: 나무는 여전히 이웃 셀 안쪽 8~13유닛이고, 가로 방향으로도 중앙에서 최대 ±10.7까지만 벗어나므로 이웃 셀의 수직 도로대(경계에서 4.5)와 겹치지 않는다. 표본 1593그루 전부가 셀 경계에서 6유닛 이상 안쪽에 있는 것을 확인했다.
- 나무가 없는 호수 두 개는 그대로 둔다. 사방이 건물·주차 차량·다른 랜드마크라 심을 빈 땅이 실제로 없는 경우이고, 건물 부지에 억지로 세우는 쪽이 더 나쁘다.
- 검증: TypeScript, 단위 테스트 395개, 프로덕션 빌드를 통과했다. `test/city-landmarks.spec.ts`에 호수당 평균 그루 수와 나무 사이 최소 간격(2유닛)을 확인하는 케이스를 추가했다. 브라우저에서 호수(x 85~187, z 289)로 날아가 물가에 선 나무 무리를 눈으로 확인했다.

## 2026-08-24 — 공중전함을 레이더에 따로 표시한다

- 사람의 요청: 거대 전함은 레이더에 특수 표시되게 해 달라.
- 문제: 공중전함은 다른 적과 같은 붉은 사각형이었다. 크기만 5px로 조금 컸을 뿐이라 헬기·전투기 점들 사이에 묻혔다. 게다가 이 배는 118에서 선회하고 177까지 추격하는데 레이더 사거리는 170이라, 교전 중 상당한 시간을 아예 화면 밖에서 보냈다 — 플레이어가 유일하게 "돌아서 피할" 대상인데 어디 있는지 알 수 없었다.
- Codex 구현: 마름모 + 링. 사각형만 있는 다이얼에서 유일하게 회전한 표식이고, 링이 3px 점과 혼동될 수 없는 크기를 준다. 색은 공중전함 체력 바 그라디언트의 따뜻한 끝(`#ff8f3d`)이라 머리 위의 바와 같은 배로 읽힌다. 사거리를 벗어나면 테두리로 밀되 속을 비워서 "저쪽 멀리"가 "여기"로 읽히지 않게 했다.
- 겸사: 목표 마커가 쓰던 테두리 클램프를 `clampToRadarRim`으로 빼내 둘이 같은 코드를 쓰게 했다. 클램프를 복사하는 것이 두 표식이 어긋나기 시작하는 방식이다.
- 범례: `radarKeyBoss`를 한·일·영에 넣고, 범례 견본도 다이얼과 같은 마름모로 만들었다. 형태가 먼저고 색이 그다음이다.
- 깜빡이지 않는다: 레이더 주석이 이미 설명하듯, 2px 점의 알파를 흔들면 접점이 사라졌다 나타나는 것으로 읽힌다. 이 표식도 같은 규칙을 따른다.
- 검증: TypeScript, 단위 테스트 395개, 프로덕션 빌드를 통과했다. `test/radar.spec.ts`에 클램프가 사거리 안 접점을 건드리지 않는지, 사거리 밖 접점을 방위각을 유지한 채 테두리로 당기는지, 추격 사거리(177)의 전함이 어느 방향에서든 올바른 쪽 테두리에 붙는지를 확인하는 케이스를 추가했다. 브라우저에서 개발자 전함 드릴(`?dev=1`)로 실제 표시를 눈으로 확인했다.

## 2026-08-24 — 대공포를 삭제하고, 전함의 무장을 탄막 하나로 줄인다

- 사람의 플레이 피드백: "대공포까지 있으면 너무 어렵다. 대공포 지워줘." 이어서 "전함 기술 중에 탄막 흩뿌리는 것 빼고 다 지워도 될 듯. 레이저 조준하고 쏘는 기술 지워."
- 왜 한 항목인가: 앞선 작업이 대공포·전투기·전함의 사격을 같은 붉은 구체로 통일했지만, 전함에는 여전히 조준 무기 둘(함수포 예고 사격·락 후 두 포탑 연사)이 남아 있었다. 후반 하늘은 탄막을 헤쳐 나가면서 동시에 서로 다른 조준 경고를 읽으라고 요구했고, 그중 한눈에 읽히는 쪽은 탄막이다. 남긴 것도 탄막이다.
- 대공포 삭제: `EnemyKind`에서 `'anti-air'`를 빼고 건물 부착 로직(`isAntiAirBuilding`·`syncAntiAirEnemies`·`destroyedAntiAir`·`sourceId`)과 웨이브 4단계('AA NETWORK'), 언어별 뉴스 속보 한 건씩, 옥상 포대 모델·재질·팔레트를 함께 걷어냈다. 웨이브는 다섯 단계가 되고 보스는 180초 그대로다 — 110초 전투기 이후 70초의 소강이 생기는데, 난이도 불만에 대한 답으로는 이득이다.
- 전함 무장 축소: 함수포와 대공 연사를 제거해 `stepBattleshipGuns`는 "링 쏘고, 재장전"이 전부가 됐다. 재장전 간격은 그대로 — 그 틈이 레이저를 쓰는 시간이라는 것이 이 싸움의 리듬이다.
- 발견한 버그(삭제로 함께 사라짐): 대공 연사는 조준 시점의 포탑 위치로 착탄 시각을 풀고 발사 시점의 포탑 위치에서 쏘고 있었다. 3초 동안 함선이 움직인 만큼 도달 시간이 어긋나 직선 비행하는 플레이어를 16m 차이로 빗나갔다 — 정지한 옥상 포대에는 없던 문제이고, 움직이는 함선이 그 기술을 물려받으면서 생겼다. 프로브로 수치를 확인한 직후 "그 기술을 지워 달라"는 요청이 와서 수정 대신 삭제로 끝났다.
- 연쇄 삭제: 조준하는 유닛이 하나도 남지 않아 `interceptTime`·`aimProjectile`·`fireProjectile`·`LEAD_ACCURACY`·`AIM_ERROR_METRES`와 렌더의 조준 링(`EnemyWarnings`)·조준선(`EnemyAimLines`)·비탄막 투사체 풀(`EnemyProjectiles`)을 지웠다. 마지막 셋은 남겨 두면 매 프레임 아무것도 그리지 않는 루프다. `EnemyProjectileKind`도 `'orb'` 하나로 줄었다. `test/aiming.spec.ts`는 통째로 지우되 "발사체는 유도되지 않는다"는 불변식만 `test/danmaku.spec.ts`로 옮겼다 — 조준이 사라져도 탄막이 읽히는 근거는 그것이기 때문이다.
- 남긴 것: 포탑 지오메트리와 `battleshipTurretPoint`는 그대로다. 이제 발사구가 아니라 레이저 명중 판정점이자 모델 파츠다.
- 결과: 하늘에 조준하는 적이 하나도 없다. 공격은 공간(기뢰·헬기 램)이거나 탄막(전투기 단발·전함 3층 링)이고, 느린 탄이 곧 경고다.
- 검증: TypeScript, 단위 테스트 377개, 프로덕션 빌드 통과. 개발자 드릴로 실제 브라우저에서 20초를 돌려 옥상 포대와 붉은 조준탄이 사라지고 탄막·기뢰·전투기만 남은 것을 확인했다(콘솔 에러 없음).

## 2026-08-25 — 손가락이 가리키는 쪽을 UFO가 본다

- 사람의 요청: PC에서 마우스를 움직이면 UFO의 시선이 그쪽으로 가듯이, 모바일에서도 손으로 터치해 움직이면 그쪽을 UFO가 보게 해 달라.
- 문제: 조준 드래그는 이미 있었다. 없던 것은 그 조준이 기체에 닿는 경로다. `readInput`은 터치 HUD가 활성화되면 `mobile.current`를 포인터 입력 위에 통째로 덮어썼고, 스틱이 쉬고 있을 때의 `steer: 0`과 아무도 채우지 않는 `lookPitch: 0`이 그대로 이겼다. 레티클은 손끝을 따라가는데 기체는 정면만 봤다 — 화면 위 스티커였지 시선이 아니었다.
- Codex 구현: 터치 HUD는 자기가 실제로 소유한 축만 말한다. 요는 `steerWithStick`이 정한다 — 엄지가 스틱에 올라가 있는 동안(0이 아닌 동안)은 스틱이, 스틱이 쉬는 순간부터는 조준 드래그가 갖는다. 피치는 HUD에 그것을 정하는 컨트롤이 아예 없으므로 드래그가 통째로 갖는다. 커서와 손가락이 같은 규칙으로 기체를 가리키게 되었다.
- 겸사: 인라인으로 흩어져 있던 요 곡선(데드존 0.08, 지수 1.18)을 `aimSteer`로 `core/aim`에 옮겼다. 수치는 그대로다 — 0.92는 `1 - 데드존`이었으므로 데스크톱 감각은 한 프레임도 달라지지 않는다. 순수 함수가 되면서 부호와 데드존이 테스트로 고정됐다.
- 스틱과 싸우지 않는다: 드래그에 요를 무조건 주면 조준하는 손이 스틱을 든 손과 반대로 기체를 끈다. 그렇다고 스틱의 쉬는 0을 통과시키면 이 버그로 되돌아간다. 우선순위 하나로 둘 다 피한다.
- 레티클이 남는 규칙은 그대로다: 손을 떼도 레티클은 그 자리에 있고, 이제 기체도 계속 그쪽을 본다. 데스크톱에서 커서를 화면 한쪽에 둔 것과 같은 상태이고, 되돌리는 방법도 같다 — 가운데로 끌어오면 된다.
- 검증: TypeScript, 단위 테스트 395개(요의 부호·데드존·완만한 곡선, 그리고 스틱이 쉴 때 드래그가 요를 갖는다는 회귀 가드 포함), 프로덕션 빌드 통과. 폰 뷰포트 스모크에서 CDP 터치로 실제 드래그를 만들어 기체 자세를 읽었다 — 고치기 전에는 heading 3.1416 / pitch 0으로 미동도 없었고, 고친 뒤에는 heading이 돌고 pitch가 0.49까지 들렸다. 자세를 눈이 아니라 숫자로 확인하려고 개발 전용 포즈 프로브(`__BEAM_BANDIT_POSE__`)에 `pitch`를 더했다.

## 2026-08-25 — 처음엔 더 크게, 다 크고 나면 더디게

- 사람의 요청: 처음에 UFO 커지는 폭을 조금 늘리고, 엄청 커지고부터는 점점 사이즈가 잘 안 커지게 해 달라.
- 문제: 성장은 순수 비례였다(`size *= 1 + SIZE_GAIN`). 배수가 일정하다는 말은 한 끼의 절대 증가량이 매번 커진다는 뜻이라, 체감은 요청과 정확히 반대였다 — 첫 30% 구간에 보행자 69명이 들고 마지막 30%는 10명이면 끝났다. 시작은 굼뜨고 끝은 순식간이었다.
- Codex 구현: 기본 이득을 올리고(보행자 0.035 → 0.046, 고양이 0.058 → 0.076) 그 위에 `growthFalloff`를 곱했다. 성장 진행도 `p`에 대해 `0.18 + 0.82 * (1 - p²)` — 지수를 1보다 크게 둔 덕에 곡선이 `p`가 작은 구간에서 평평하다. 즉 초반은 올린 이득을 거의 그대로 받고, 브레이크는 정말로 거대해진 뒤에야 걸린다.
- 결과(보행자만으로 천장까지): 첫 30% 69 → 51끼, 마지막 30% 10 → 19끼, 총 102 → 91끼. 한 판의 길이는 사실상 그대로 두고 곡선의 앞뒤만 뒤집었다.
- 감속이지 정지가 아니다: 천장에서도 여는 속도의 18%가 남고, 절대 증가량은 여전히 작은 기체보다 큰 기체가 많다. 성장이 최고의 재미라는 규칙(`core/size` 서두)을 뒤집지 않으면서 후반의 "이제 잘 안 큰다"만 만든다.
- 적용 범위: 모든 성장이 `growSizeBy` 하나를 지나도록 `growSize`를 그 위에 얹었다. 보행자·고양이뿐 아니라 빔으로 삼킨 차량·건물(`growBy`)도 같은 감속을 받는다.
- 검증: TypeScript, 단위 테스트 402개(구간별 끼니 수·감속의 단조성·천장 도달 가능성 가드 추가), 기존 피딩/체력 밸런스 테스트 통과.

## 2026-08-25 — 3개 언어 게임 텍스트 점검: 사실이 틀린 문구와 개편 전 카피를 걷어낸다

- 사람의 요청: 한국어·일본어·영어 게임 내 텍스트를 전부 확인하고 위화감이 없는지, 세계관이 맞는지 살펴봐 달라.
- 점검 범위: `Strings` 143키 × 3언어 = 429개 문자열을 코드와 대조했다. 문자열끼리만 비교하면 "셋 다 같은 말을 한다"로 끝나므로, 기준은 항상 실제 동작 쪽에 두었다 — 점수는 `bankDestroyScore` 호출값, 키 안내는 `readInput`의 키 맵, 웨이브 속보는 `ENEMY_WAVE_STAGES`, 미션 문구는 `MISSION_ORDER`.
- 결과 요약: 한국어가 5단계 정찰 개편을 반영한 기준이고, 일본어·영어가 그 이전 생존 모드 카피에 묶여 있었다. 판단이 필요 없는 것만 여기서 고치고, 용어 표준(선체/생명, DREADNOUGHT/BATTLESHIP 등)은 사람에게 넘겼다.

### 차량 파괴 콜아웃이 값을 거짓말하고 있었다

- `msgCarLaunched`는 `+50`을 문자열에 박아 두고 있었는데, 이 콜아웃은 두 경로가 공유한다. 승용차(`destroyCar`)는 50이 맞지만 트럭·유조차(`registerHeavyVehicleLaserHit`)는 90·140을 적립하고도 화면에는 `+50`이 떴다. 표적의 값을 가르치는 문구가 세 배 낮은 값을 가르치고 있었으므로, 없느니만 못한 콜아웃이었다.
- 두 함수가 `boolean` 대신 적립한 점수를 돌려주게 하고(파괴 실패는 0), 콜아웃을 `msgVehicleDestroyed: (reward) => ...`로 바꿔 실제 값을 받게 했다. 점수 자체는 `CAR_DESTROY_SCORE`·`TRUCK_DESTROY_SCORE`·`TANKER_DESTROY_SCORE`로 이름을 얻었다 — 콜아웃이 인용하는 숫자가 호출부에 인라인으로 흩어져 있었던 것이 이 버그의 원인이다.
- 이름도 함께 고쳤다. 유조차를 부수고 `자동차 파괴`/`車を破壊`/`CAR`가 뜨는 것은 세 언어 공통 문제였고, 영어만 파괴가 아니라 `CAR LAUNCHED`(발사)라고 말하고 있었다. 셋 다 차량으로 통일했다 — `차량 파괴`/`車両を破壊`/`VEHICLE DESTROYED`.
- 타입이 회귀를 막는다: 문자열이 함수가 되었으므로 이제 숫자를 안 넘기면 컴파일이 깨진다.

### 모바일 조작 버튼 셋만 번역되지 않고 있었다

- `MobileControls`의 `BOOST`/`LASER`/`BEAM`이 하드코딩이었다. 로비에서 한국어나 일본어를 고른 플레이어가 화면에 엄지를 올리는 순간 영어 세 단어를 만났다.
- `touchTurbo`/`touchLaser`/`touchBeam` 키를 새로 두고 연결했다. 조작 카드의 `controlBoost`(`빔 유지 · 흡수`처럼 서술형)를 그대로 쓰지 않은 이유는 이 버튼이 지름 58px 원이기 때문이다 — 카드용 문장과 버튼용 단어는 길이 예산이 다르다.
- 덤으로 영어 용어 하나가 정리됐다. 게임 전체가 터보인데 이 버튼만 `BOOST`였다.

### 개편 전 카피와 방향이 뒤집힌 안내

- `tagline`(ja·en)이 여전히 "가능한 한 오래 살아남아 도시를 파괴하라"였다. 지금 판은 5분 정찰이고 한국어만 그 문장으로 바뀌어 있었다. `start`(en)의 `START SURVIVAL`은 존재하지 않는 모드 이름이라 `START RECON`으로, `retry`(en)의 `RAID AGAIN`은 정찰을 습격으로 부르고 있어 `FLY AGAIN`으로 바꿨다.
- `controlStrafe`(en)가 `RIGHT / LEFT`였다. 이 문구는 `A/D` 키캡 바로 옆에 붙으므로 키 순서와 반대로 읽힌다. `LEFT / RIGHT`.

### 언어별 표기

- 한국어: `E 를`·`Q 를`·`스페이스 를`에서 조사 앞 띄어쓰기를 없앴다(`briefingWaitHint`). `시간내에` → `시간 내에`. `성공했습니다 냐앗호!`에 쉼표.
- 일본어: 전각 `！`로 통일(랭킹 3곳만 반각이었다), 대사 말줄임표 `...` → `……`, 구분자 `・` 4곳을 나머지 16곳과 같은 ` · `로 통일했다. `殿堂` → `名誉の殿堂`(단독으로는 리더보드 제목으로 읽히지 않는다), `重さが満タン` → `積載オーバー`(満タン은 연료탱크 표현이라 무게 게이지에 붙지 않는다).
- 검증: TypeScript, 단위 테스트 401개, 프로덕션 빌드 통과.

## 2026-08-25 — 생명력과 공중전함으로 용어를 통일한다

- 사람의 결정: 하트 다섯 칸은 **생명력**, 마지막 웨이브의 배는 **공중전함**. 나머지는 기본값으로 맡겼다.
- 왜 한 항목인가: 앞선 텍스트 점검이 남긴 것은 문장 문제가 아니라 이름 문제였다. 같은 것을 부르는 말이 여러 개면 플레이어는 그것을 여러 개로 읽는다.

### 생명력

- 게이지에는 이름이 둘 있었다. 위험 안내와 회복 콜아웃은 `선체`/`船体`/`HULL`, 장군의 디브리핑은 `생명력`/`ライフ`/`life`. 같은 픽업이 한쪽에서는 무언가를 수리하고 다른 쪽에서는 다른 무언가를 채우는 것으로 읽혔다. 일본어는 `msgBoonHeal`(`船体を回復`)과 `msgMysteryHeal`(`ライフ全回復`)이 한 판 안에서 엇갈렸다.
- `hull` 키를 지웠다. 렌더된 적이 없는 키였고(`t.life`가 하트의 `aria-label`을 맡는다), 하는 일이라고는 렌더되는 문자열들에 `선체`를 쓸 명분을 준 것뿐이었다.
- 한국어에서 `선체`가 완전히 사라졌다. 게이지를 가리키던 셋은 `생명력`이 되고, 실은 기체를 가리키던 셋(`서클을 통과하면 선체가 가속`, `많이 삼킬수록 선체도 커진다`, `선체가 격추됐다`)은 `기체`가 됐다 — 일본어·영어가 이미 `機体`/`craft`라고 부르던 것들이다. 격추되는 것도 껍데기가 아니라 기체다.
- 근거: 선체는 커지기만 하는 몸이고(`core/size`), 줄었다 차는 것은 `core/health`다. 한 단어가 둘을 겸할 수 없다.

### 공중전함

- 영어에 이름이 넷이었다: `SKY DREADNOUGHT`(bossName)·`Dreadnought`(레이더 범례)·`BATTLESHIP`(개발자 드릴)·`a flying battleship`(웨이브 5 속보 본문). 마지막 둘은 속보 카드 하나 안에서 헤드라인과 본문이 서로 다른 배를 가리켰다.
- `SKY BATTLESHIP`으로 통일했다. 웨이브 라벨 상수(`ENEMY_WAVE_STAGES`)까지 포함한다 — 이 라벨은 웨이브가 바뀔 때 파일럿 말풍선에 실제로 뜬다. 레이더 범례만 `BATTLESHIP`인데, 범례 칸은 112~148px 다이얼의 절반이고 이 짧은 형태가 원래의 `Dreadnought`보다 오히려 좁다.
- 한국어·일본어는 이미 통일돼 있었고 개발자 드릴만 `전함`/`戦艦`으로 샜다. 함께 맞췄다.

### 테스트가 결정을 붙잡는다

- 두 이름 모두 회귀 가드를 붙였다. `everyString`이 디브리핑·속보·튜토리얼까지 재귀로 펼치므로 평평한 라벨만 보는 것이 아니다. 실제로 `msgBoonHeal`을 `HULL RESTORED`로, `bossName`을 `SKY DREADNOUGHT`로 되돌려 두 가드가 모두 붉어지는 것을 확인한 뒤 되돌렸다 — 통과만 확인한 가드는 통과를 보장하지 않는다.

### 기본값으로 처리한 것

- 과적 경보가 방법을 말한다: `shouldCrashFromOverload`는 **빔을 켠 동안에만** 발동하므로 빔을 놓는 것이 유일한 해법인데, 세 언어 모두 `무게를 줄이세요`/`重量を減らせ`/`DROP SOME CARGO`로 상태만 말하고 있었다. `빔을 놓아라!`/`ビームを離せ！`/`RELEASE THE BEAM!`.
- 격추 엔딩 리드가 격추를 말한다. `missionFailedLead`만 이유를 대고 있었다.
- 미션 2의 `(L)`을 `(리터)`/`(リットル)`/`(LITRES)`로. `(Q)`·`(E)` 옆에서 `(L)`은 단위가 아니라 키로 읽힌다.
- 한국어 존댓말 정리: 「작전 명령」 슬립인 `lobbyOrders`가 `완수하세요!`였다(일본어는 이미 `完遂せよ！`). `massHint`의 `빔을 통해서`는 `빔으로`로 — 직전 커밋의 "빔 흡수 중심" 의도는 그대로 뒀다.
- 영어 `TURBO BOOST` → `TURBO`(게임의 나머지 전부가 터보다), 레이더 범례 네 개를 다른 라벨과 같은 대문자로, `EARTH HAD NOTES` → `NOT BAD, EARTH`(원래 것은 영미 업계 관용구라 비원어민에게 읽히지 않고, `지구가 좀 치네?`/`地球、意外とやるな`의 톤과도 어긋난다).
- `howToTitle`만 세 언어 모두 `UFO CONTROLS`로 남아 있었다. 같은 성격의 `tipTitle`은 번역돼 있으므로 함께 번역했다. 웨이브 이름은 규칙대로 영어로 남긴다.
- 삭제된 유닛을 설명하던 키를 걷어냈다: `bandGround`·`bandArmor`·`bandAa`와 각 주석 셋. `대공 미사일 사격권`·`지상군 사격권`이라고 적혀 있었지만 지금 하늘에는 기뢰·헬기·전투기·공중전함뿐이다. 튜토리얼 브리핑이 첫 대사를 가져간 `msgRunStart`도 함께 지웠다.
- README: `하트(선체)` → `하트(생명력)`, 없는 `R` 비상 투하 키 삭제. 매단 짐을 버리는 방법은 `E`를 놓는 것뿐이고 그것이 과적 추락을 피하는 유일한 방법이라는 사실을 대신 적었다.
- 검증: TypeScript, 단위 테스트 403개, 프로덕션 빌드 통과.

## 2026-08-25 — 모바일 조작 버튼 글자를 10px로 올린다

- 사람의 결정: 모바일 버튼 폰트를 10px로.
- 왜 필요했는가: 버튼 라벨이 번역되면서 생긴 후속 문제다. 8px는 라틴 문자 한 단어로는 읽히지만 CJK 글립 하나로는 뭉갠다. `LASER`가 읽히던 자리에서 `レーザー`는 얼룩이었다.
- 넘치지 않는다는 것을 눈이 아니라 숫자로 확인했다. Pixel 5 뷰포트에서 세 언어를 각각 실제로 띄우고 라벨의 렌더 폭을 쟀다 — 가장 넓은 `レーザー`가 40.5px, 버튼 안쪽 여유는 58px에서 테두리 4px씩을 뺀 50px이다. 아홉 개 라벨 전부 한 줄에 들어간다(텍스트 높이 11px = 한 행).
- 검증: TypeScript, 단위 테스트 403개, 프로덕션 빌드 통과.

## 2026-08-24 — 로비 개발자 드릴 버튼이 고양이 뒤에 숨어 있던 것을 고친다

- 사람의 보고: 개발자 모드 버튼이 사라졌다.
- 실제로는 사라진 적이 없었다. `.intro-actions`는 `flex-wrap` 없는 `display:flex`이고, 드릴 버튼에 준 `flex-basis:100%`는 줄바꿈이 허용될 때만 "자기 줄"이 된다. 허용되지 않으니 버튼은 아래로 내려가는 대신 행을 늘려 오른쪽 끝으로 밀렸고, 로비 개편으로 그 자리에 고양이 마스코트가 들어오면서 흐린 점선 스타일이 그림 위에 겹쳐 완전히 읽히지 않게 되었다.
- Codex 구현: `.intro-actions`에 `flex-wrap:wrap`을 넣어 원래 주석이 설명하던 대로 세 입구 아래 한 줄을 차지하게 했다. 배경 불투명도도 0.5에서 0.88로 올렸다 — 뒤에 그림이 있는 곳에 놓이는 버튼이므로 읽히는 것이 우선이고, 점선 테두리와 작은 글자는 그대로라 여전히 네 번째 입구로 오인되지 않는다.
- 검증: 1280×720과 420×780 두 폭에서 버튼이 제 줄에 앉는 것을 스크린샷으로 확인했고, 눌러서 드릴이 실제로 뜨는 것(미션 3·질량 5.2·남은 시간 1:59·콘솔 에러 없음)까지 확인했다. 단위 테스트 390개와 프로덕션 빌드 통과.
## 2026-08-25 — 공중전함이 20초 먼저 뜬다

- 사람의 요청: 거대전함을 20초쯤 더 빠르게 나오게 해 달라.
- 웨이브: 마지막 단계 `SKY DREADNOUGHT`를 180초에서 160초로 당겼다. 대공포를 지우면서 생긴 "110초 전투기 이후 70초의 소강"이 그대로 남아 있었다 — 전투기 웨이브가 새로울 동안 전함이 오고, 판 전체에서 가장 기다리는 싸움이 120초에서 140초로 길어진다. 앞선 세 웨이브(0·30·70·110)는 손대지 않았다.
- 하늘도 같이 당겼다: 낮이 20초 늦게까지 남으면 전함이 석양 속에서 뜬다. `daylight.ts`의 정오~야간 키프레임(0.34·0.42·0.49·0.53·0.58)을 0.33·0.39·0.45·0.48·0.52로 옮겨 오후 한나절이 그 20초를 내놓게 하고, 밤 키프레임은 숫자 대신 웨이브 표에서 읽게 했다.
- 왜 파생값인가: `NIGHTFALL_AT = BATTLESHIP_LAUNCH_SECONDS / DAY_CYCLE_SECONDS`다. 원래 0.66은 "180초"를 손으로 계산해 적어 둔 값이었고, 웨이브가 움직이는 순간 조용히 틀린 값이 된다. 이제 발사 시각과 일몰은 같은 하나의 사실이며, `enemies.ts`는 타입 전용 임포트만 있는 순수 데이터라 `src/core` 규칙도 그대로다.
- 정리: `GameContext`가 웨이브 표를 다시 훑어 보스 단계를 찾던 코드도 같은 상수를 쓴다. 개발자 드릴은 이 상수로 시계를 감으므로 자동으로 160초에서 열린다.
- 검증: TypeScript, 단위 테스트 401개, 하늘 표본으로 140~150초 DUSK / 160초 정확히 NIGHT / 이후 300초까지 단조 어두워짐을 확인했다.

## 2026-08-25 — 쫓아오는 적만 15초씩 뒤로 민다

- 사람의 플레이 피드백: 어렵다는 평이 있어서 헬리콥터와 전투기는 조금 뒤에 나오게 해 달라.
- 웨이브: `HELICOPTERS UP` 70→85초, `FIGHTERS SCRAMBLED` 110→125초. 기뢰(30초)와 공중전함(160초)은 건드리지 않았다. 간격은 30/55/40/35초가 된다.
- 왜 이 둘만인가: 자폭 드론 기뢰는 쫓아오지 않는다. 하늘에 떠 있어도 요구하는 기술은 "들이받지 않기" 하나뿐이라, 빔을 배우는 중인 사람이 같이 있어도 되는 유일한 웨이브다. 그 뒤로는 전부 회피를 요구하므로, 늘릴 가치가 있는 구간은 그 한산한 40초다 — 55초가 됐고, 헬기도 전투기 없이 40초를 갖는다.
- 건드리지 않은 것: 각 웨이브가 정착하는 개체 수(`targets`)는 그대로다. 도착 시각만 밀었으므로 특정 단계의 하늘 밀도는 이전과 완전히 같고, 거기에 도달하는 시점만 늦다. 난이도를 낮추는 손잡이가 둘인데 한 번에 하나만 돌린다.
- 전투기 단계가 35초로 가장 짧아졌지만 속보 배너는 6초라 겹치지 않고(`test/broadcast.spec.ts`가 최소 간격으로 고정), 전함이 뜬 뒤에도 호위대가 10초마다 전투기를 내보내므로 전투기를 볼 시간이 줄지는 않는다.
- 검증: TypeScript, 단위 테스트 401개, 프로덕션 빌드 통과.

## 2026-08-25 — 난이도 4종 조정: 웨이브가 차오르고, 쫓는 적이 줄고, 빔이 바닥에 닿는다

- 사람의 요청: (1) 웨이브가 절반으로 시작해 서서히 차오르게, (2) 헬기 출현 20% 감소, (3) 전투기 탄막 간격 2배, (4) 전투기 출현 20% 감소, (5) 디폴트 빔이 지면까지 닿아 초보자가 중층 건물 높이에서도 흡수할 수 있게.
- 웨이브 램프(`WAVE_RAMP_SECONDS = 20`): 스테이지 경계에서 표의 절반이 뜨고 20초에 걸쳐 나머지가 합류한다. 스포너는 한 틱에 8기를 뿌릴 수 있어서 지금까지 웨이브는 사실상 3초짜리 계단이었다 — 헬기 8대가 한꺼번에 나타나는 그 순간이 "어렵다"의 정체였고, 이건 웨이브 시각을 미뤄서는 고칠 수 없는 다른 문제다. 속보 배너는 여전히 하늘에 실재하는 것을 알린다(경계에 뜨는 절반이 그 뉴스다). 램프는 종류를 가리지 않는다 — 헬기만 부드럽게 들어오고 같은 웨이브의 기뢰가 계단으로 들어올 이유가 없다. 전함만은 예외인데, 분기가 아니라 반올림으로 그렇게 된다(0.5척 → 1척).
- 물량 20% 감소: 헬기 8/11/14 → 6/9/11, 전투기 4/6 → 3/5. 기뢰는 손대지 않았다. 난이도는 하늘이 얼마나 찼는가가 아니라 무엇이 쫓아오는가로 읽히므로, 삭감은 쫓는 쪽에만 넣고 하늘은 보이던 만큼 유지한다.
- 전투기 발사 간격 1.4 → 2.8초: 사람이 "4초"로 기억한 값은 실제로는 1.4초였다(사거리 70m 게이트 때문에 체감이 길었다). 요청의 비율(절반)을 적용했다. 사거리가 게이트이므로 편대의 사격은 플레이어가 그 안에 있는 몇 초에 몰린다 — 개별 연사를 절반으로 줄이는 것이 그 뭉침을 그대로 절반으로 만든다. 탄막의 성질(느린 구체 한 발, 조준·예고 없음)은 그대로다.
- 빔 사거리 30 → 40m(부스트 47 → 60): 개막 기체의 고도 상한이 30.9m인데 기본 빔이 정확히 30m였다. 두 숫자는 서로를 보고 정해진 적이 없고, 그 우연이 실제 피해를 주고 있었다 — 중층 옥상을 넘으려고 자기 고도 범위의 꼭대기까지 올라간 초보자는 빔이 거리를 멈추는 바로 그 높이에서 날고 있었다. 도시가 보이는데 손이 닿지 않는 상태이고, 이 게임의 동사는 그거 하나다.
- 빔에서 건드리지 않은 것: 먼 쪽이 약하다는 규칙. 처음엔 낙차에 비례해 최소 견인 속도를 올려 봤는데(멀리서도 실제로 끌려오게), `test/beam.spec.ts`가 정확히 그것을 막았다 — 30m 아래의 화물이 5m 아래의 화물보다 빨리 올라오면 빔의 근본 규칙이 뒤집힌다. 되돌렸다. 사거리만 늘고, 그 안에서의 감쇠와 평평한 `BEAM_HAUL_FLOOR`는 그대로다. 멀리서 잡으면 여전히 느리다 — 빨리 먹으려면 내려가야 한다는 교육은 유지된다.
- 발견한 커플링: `verticalLimit`이 `Math.min(profile.maxDrop, 47)`을 읽고 있었다. 47이 부스트 빔의 사거리와 같은 숫자였을 때는 "스프링이 튜닝된 최대 원뿔"이라는 주석과 일치했지만, 기본 원뿔을 늘리는 순간 짧은 빔에 견인력을 1/3 더 주는 조용한 부작용이 됐다(깊은 포획이 얕은 포획을 앞지르기 시작했다). `BeamProfile.haulDrop`으로 분리했다 — 사거리는 어디서 멈추는지만 정하고, 견인은 튜닝된 원뿔(30/47)을 계속 본다. 성장한 기체의 견인은 이전과 값까지 동일하다.
- 테스트 수정 둘: 풀려난 차의 낙하 높이를 튕김 도중(1초)이 아니라 안정된 뒤(2초)에 읽는다 — 원래 단언은 "거리로 돌아온다"가 아니라 "빔이 끊긴 순간 얼마나 높았나"를 재고 있었다. 전투기 연사 테스트의 관측 창을 9초 고정에서 `FIGHTER_ORB_INTERVAL * 6`으로 바꿨다 — 발사 수를 세는 것이 아니라 간격을 재는 테스트다.
- 검증: TypeScript, 단위 테스트 403개(램프·사거리 회귀 가드 2건 추가), 프로덕션 빌드 통과. 실제 시뮬레이션으로 램프를 눈으로 확인했다 — 85초 헬기 1→4→5→6, 125초 전투기 1→2→3, 전함은 160초에 한 척 통째로.

## 2026-08-25 — 전투기 물량은 되돌린다

- 사람의 판단: 전투기 발사 속도를 절반으로 늦췄으니 수는 원래대로 나와도 되겠다.
- 되돌린 것: 전투기 웨이브 목표 3/5 → 4/6. 헬기의 20% 삭감(6/9/11)과 웨이브 램프는 그대로다.
- 왜 되돌리는 게 맞나: 한 번의 불만에 두 번 값을 치르고 있었다. 전투기는 총인 만큼이나 하늘을 가로지르는 형체이기도 하다 — 편대를 줄이면 그 교차까지 사라지고, 전투기 웨이브가 "출격"이 아니라 조금 시끄러운 헬기 웨이브로 읽힌다. 들어오는 탄이 많다는 불만에 답하는 손잡이는 발사 간격이고, 하늘이 빽빽하다는 불만에 답하는 손잡이가 물량인데 후자를 말한 사람은 없었다.
- 검증: TypeScript, 단위 테스트 408개, 프로덕션 빌드 통과. 한 방향으로 8km를 직진하는 시뮬레이션으로 후반 하늘이 유지되는 것도 확인했다(헬기 12·전투기 7·기뢰 36 유지, 반경 120m 안에 상시 20~30기).

## 2026-08-25 — 도망 시 재생성 지연을 되돌린다

- 사람의 판단: 필요 없겠다.
- 되돌린 것: 직전 커밋(`fef246b`)이 넣은 `LEFT_BEHIND_SPAWN_PAUSE` 일체 — 거리 이탈당 0.08초 홀드, 0.5초 상한, 그리고 도주/제자리를 비교하던 테스트. 도망쳐도 하늘은 예전처럼 곧바로 다시 채워진다.
- 기록으로 남기는 이유: 손잡이 자체는 동작했고 수치도 재 뒀다(제자리 플레이어는 영향 0, 도주 중에만 4분의 1쯤 얇아짐). 다시 필요해지면 `git revert`로 되살릴 수 있고, 그때 0.25초는 이미 한 번 버려진 값이라는 것도 같이 남는다.
## 2026-08-25 — 이동 키를 없애고 자동 전진으로 간다, 과적 추락 엔딩을 지운다

- 사람의 요청: "WASD 이동 없애고 자동 이동으로 가자, 보는 방향으로. WASD가 익숙하지 않아서 초심자에겐 좋지 않다는 평가가 있었어. 단 빔 흡수 시에는 속도 조금 느리게." 이어서 "빔 쓰면 이제 느려지니 추락을 없애자 — 크기 게이트와 무게별 흡수 속도는 그대로 두고 추락 로직만." 마지막으로 범위를 좁혀서: "추락은 없지만 밑으로 떨구고 느려지는 건 냅두자. 추락 엔딩만 없애는 거지."
- 왜 이동 키가 아무것도 사지 못하고 있었나: 기체는 처음부터 보는 방향으로 날았고, 방향은 레티클이 정하고 있었다. `W`가 한 일은 "지금 보고 있는 쪽으로 가겠다"는 확인 뿐이었다. 초심자에게는 그 확인이 게임을 시작하기 전에 넘어야 하는 벽이었고, 넘고 나면 계속 누르고 있어야 하는 키였다. `S`(후진 4유닛/초)와 `A`/`D`(스트레이프)는 마우스 선회로 대체 가능한 조작이었다.
- Codex 구현: `readInput`의 `throttle`이 상수 1이 됐고 `strafe`는 `DroneInput`에서 통째로 사라졌다. 남은 `throttle`은 입력이 아니라 **월드가 쥐는 손잡이**다 — 호수의 `speedScale`, 빔의 `BEAM_CRUISE_SCALE`, 튜토리얼의 0. 그래서 부호도 없앴다(`clamp(input.throttle, 0, 1)`): 요청할 후진이 없다.
- 감속은 빔이 맡는다: `BEAM_CRUISE_SCALE = 0.75`. 스로틀 키가 없어지면 "보행자 한 명에 원뿔을 맞추려고 속도를 줄이는" 수단도 같이 없어진다. 그 자리를 `E`가 채운다 — 누르면 가라앉듯 느려지고, 그 같은 누름이 삼킨다. 브레이크 키를 따로 만들지 않은 이유이자, 게임이 이미 가르치고 있는 버튼 하나에 기능을 더 얹은 이유다. 4분의 1만 깎은 것은 "빔을 켠 채 거리를 쓸고 지나가는" 것이 여전히 빔의 정상 사용법이어야 하기 때문이다.
- 추락은 엔딩만 지웠다: `shouldCrashFromOverload`와 `src/core/overload.ts`, `RunEnding`의 `'crushed'`, 결과 화면 문구(`crushedTitle`/`crushedLead`)와 장군의 마지막 한마디를 3개 언어에서 걷어냈다. **가라앉는 것은 남겼다** — 적재 한계를 넘으면 상승이 끊기고 기체가 내려가는 `flightInput.vertical` 처리는 그대로고, 경고 배너와 무게 게이지의 과적 표시도 그대로다. 사라진 것은 바닥이다. 눈에 보이고 언제든 빔을 놓아 되돌릴 수 있는 압박에 사망 판정까지 붙일 이유가 없고, 그 판정이 가르치는 것은 가라앉음이 이미 말하고 있었다.
- 문구는 실제 결과에 맞췄다: `overloadAlarm`이 "추락 위험"에서 "고도 하강"으로, `overloadHint`가 "무게가 가득 차면 추락"에서 "고도 하강"으로 갔다. 남은 결말이 셋이 되면서 로비의 개발자 엔딩 프리뷰도 셋이다.
- 매뉴얼 1번 패널을 다시 그렸다: WASD 키캡 넷과 사방 화살표 대신, 이미 지나온 점선 궤적 · 기체 · 그 앞의 레티클이다. 그릴 키가 없으니 그리지 않는다. 조작 카드는 `AUTO / MOUSE / E / Q / SPACE` 다섯 줄이 됐고, HUD의 좁은 카드에서는 같은 뜻의 짧은 문구(`howToMove`)를 쓴다 — 긴 쪽은 두 줄로 접혔다.
- 모바일: 조이스틱은 요만 남겼다. 앞으로 가는 것은 기체가 알아서 하므로 스틱의 세로축은 할 말이 없고, 엄지가 아래쪽에 걸쳐 있을 때 조용히 절반 속도가 되는 스로틀은 아무도 쥐고 있는 줄 모르는 스로틀이다.
- 검증: TypeScript, 단위 테스트 401개, 프로덕션 빌드 통과. `test/balance.spec.ts`의 스트레이프 케이스를 "아무것도 누르지 않아도 보는 쪽으로 간다"와 "빔은 브레이크지 핸드브레이크가 아니다"(감속하되 `open * 0.6` 아래로는 안 내려간다)로 바꿨다. 브라우저 검증도 두 갈래로 했다 — 튜토리얼을 건너뛴 직후 키를 하나도 누르지 않은 채 기체가 그 자리를 떠나는 것(`smoke.spec.ts`), 그리고 빔을 켠 채 지면까지 가라앉혀도(`pose.y` 0.4 = `minHeight`) 과적 경고는 뜨고 결과 화면은 뜨지 않는 것. 스모크 첫 케이스는 이 컨테이너의 소프트웨어 렌더링이 로비 5초 대기를 못 맞춰 실패하는데, 변경 전 커밋에서도 똑같이 실패한다.

## 2026-08-25 — 감속을 빔에서 과적으로 옮기고, 시작 적재 한계를 몸 하나로 올린다

- 사람의 요청: "추락 대신 과적을 이용해서, 많이 흡수하려고 하고 과적이면 속도가 느려지는 건 어때. 빔 사용 시에 계속 느려지는 게 아니라." 이어서 "`LIFT_CAPACITY_MIN`을 2로 해줘."
- 무엇이 잘못 계산되고 있었나: 앞선 작업이 넣은 `BEAM_CRUISE_SCALE`은 **동사에 값을 매기고 있었다**. 빈 거리를 원뿔만 열고 지나가는 패스와 차 세 대를 물고 나오는 패스가 같은 25%를 냈다. 게임이 통째로 가르치는 버튼이 켜져 있다는 이유만으로 요금을 매기면, 배우라고 한 것을 아끼라고 가르치는 셈이다.
- Codex 구현: `BEAM_CRUISE_SCALE`을 삭제하고 `src/core/overload.ts`를 그 자리에 되살렸다 — 이번에는 추락 판정이 아니라 `overloadCruiseScale(hangingWeight, liftCapacity)`다. 빔을 여는 것은 공짜고, 값을 내는 것은 적재 한계를 넘어선 무게다.
- 곡선은 절대 질량이 아니라 **한계 대비 비율**로 잡았다: `floor + (1 - floor) / (1 + 초과/한계)`. 한계는 크기와 함께 자라므로(2 → 40), "정격의 두 배"가 시작 접시와 천장 접시에 같은 뜻이어야 한다. 절대값으로 재면 성장에 매기는 세금으로 읽힌다. 한계에서 정확히 1을 돌려주므로 게이지가 꽉 차는 순간 뚝 걸리는 것이 아니라 선을 넘는 만큼 스며든다.
- 하한 `OVERLOAD_CRUISE_FLOOR = 0.45`: 절반 아래로 떨어지되 절대 멈추지는 않는다. 과적에서 빠져나오는 길이 "어딘가로 날아가서 삼키는 것"이므로 못 움직이게 만들면 그 길을 막는다. 호수(`LAKE_BEAM_SPEED_SCALE`)가 하는 것과 같은 종류의 약속이다.
- 그래서 루프가 스스로를 먹인다: 많이 물수록 느려지고, 느려질수록 다음 것을 물기 쉬워지고, 결국 차량 사이로 반쯤 가라앉은 채 삼킬지 놓을지 정하게 된다. 죽지는 않는다 — 추락 엔딩은 앞 커밋에서 이미 없앴고 가라앉음은 그대로다.
- `LIFT_CAPACITY_MIN` 1 → 2: 보행자 한 명이 질량 2다. 한계가 1이던 동안에는 **새 플레이어가 처음 집어 든 것이 곧바로 과적**이었다 — 경고가 뜨고, 가라앉고, 속도가 떨어진다. 튜토리얼이 가르친 바로 그 행동에 걸리는 한계는 한계를 가르치는 것이 아니라 소음이다. 2에서는 몸 하나가 정확히 만적이고 두 번째가 정격의 두 배다.
- 문구도 따라갔다: `controlBeam`에서 "감속"을 뺐고(빔은 이제 공짜다), `overloadHint`·`overloadAlarm`이 감속과 하강을 함께 말한다.
- 검증: TypeScript, 단위 테스트 401개, 프로덕션 빌드 통과. `test/balance.spec.ts`의 브레이크 케이스를 "동사가 아니라 탐욕에 값을 매긴다"로 바꿔 한계선에서 정확히 1인 것, 크기가 달라도 같은 비율이면 같은 값인 것, 하한이 0보다 큰 것, 단조 감소를 고정했다. 브라우저에서는 세 가지를 실제로 재봤다 — 빔 사거리 밖 고도에서 빔을 켜고 끈 이동 거리가 29.9 대 28.5(비율 0.954, 옛 0.75 세금이었다면 나올 수 없는 값), 경고가 처음 뜬 시점의 무게가 4(= 정격 2의 두 배, 몸 하나로는 안 뜬다), 그리고 과적 상태로 지면(1.50)까지 가라앉은 채 6초에 9.8유닛(무적재 순항의 44%)을 가면서 결과 화면은 뜨지 않는 것.

## 2026-08-25 — 서클 통과의 생명 회복을 걷어내고 부품 능력치를 여덟 종 1레벨로 재편한다

- 사람의 결정: 미스터리 서클로 인한 생명 회복 기믹을 지우고 튜토리얼에서도 체력 회복 문구를 뺀다. 다만 부품 아이템의 회복 대체 보상은 남긴다. 회전력·빔 범위·빔 사거리·빔 흡수 속도를 부품 능력치로 추가한다. 종류가 많으니 전부 1레벨까지만 올라가게 하고, 레이저는 데미지 1.5배로 한다.
- 지운 것은 서클 통과 시의 완전 회복(`healHealth(game.health, game.health.max)`)이다. 서클이 회복 지점인 한 후반 교전의 정답은 "서클 위로 도망친다"가 되고, 서클은 지나가기만 하면 되는 드라이브스루라 대가가 없다. 서클은 가속과 터보 완충만 주는 속도 피트스톱으로 되돌린다. 생명력은 느린 자연 재생과 아래의 대체 보상으로만 돌아온다.
- 부품 아이템의 회복 대체 보상(`BOON_HEAL_PIPS`, 1.5칸)은 사람의 요청으로 남겼다. 능력치가 전부 최대면 올릴 것이 없으므로 회복이 그 판의 대체 보상이 된다. 서클을 지나가는 것과는 값이 다르다 - 서클 하나의 아이템을 통째로 쓰고, 사다리를 이미 끝낸 판에서만 나오므로 "다치면 서클로 도망친다"가 되지 않는다. 생명력까지 가득하면 종전대로 점수(`BOON_FULL_SCORE`)를 준다.
- 능력치 네 종을 추가하고(회전력 `turn-rate`, 빔 범위 `beam-radius`, 빔 사거리 `beam-reach`, 빔 흡수 속도 `beam-pull`) 여덟 종 전부를 1레벨 상한으로 통일했다. 종류가 여덟이나 되는데 몇몇에만 사다리를 두면, 같은 서클이 레이저 1/5을 몇 번씩 건네는 동안 빔도 터보도 출발선 그대로다. 한 번씩만 먹는 쪽이 아이템마다 다른 문장이 되고, 가진 목록만 봐도 아직 없는 게 읽힌다. 사다리 전체는 아이템 여덟 개다.
- 값: 레이저 1.5배(사람 지정), 속도 +15%, 회전력 +15%, 빔 범위 +15%, 빔 사거리 +20%, 빔 흡수 속도 +15%, 터보 충전 +30%, 터보 양 5→7초. 레이저만 크게 잡은 이유는 공중전함이 빔으로 먹히지 않아 레이저 외에 답이 없는 유일한 표적이기 때문이다. 나머지는 조종석에서 즉시 읽히되 판을 다시 짜지 않는 15~30% 대에 뒀다.
- 체감은 배수 그대로가 아닌 것이 둘 있다. 빔 견인력은 haul 스프링에 두 번(스프링 상수와 수직 드라이브) 곱해져 1.15배가 약 1.32배로 오고, 빔 범위는 면적이 제곱으로 늘어 +15%가 한 번 훑는 지면의 1.3배다.
- 레벨이 하나뿐이므로 습득 문구에서 레벨 숫자를 뺐다. 여덟 개 전부에 `Lv.1`만 찍히는 표시는 아무것도 말하지 않는다. `레이저 위력 강화`처럼 능력치 이름만 부르며, 세 언어 모두 함수가 아닌 문자열로 바꿨다.
- 공중전함 테스트가 옛 카드 시절 수치(`1 + 4 * 0.45`)를 손으로 박아 두고 "최대 레이저는 절반 이하"를 검사하고 있었다. 이제 최대가 1.5배이므로 그 문장은 아무도 못 타는 레이저를 묘사한다. `BOON_DEFINITIONS`에서 읽어 오도록 바꾸고 기준을 실제 값(64발 → 43발, 3/4 미만)으로 고쳤다.
- 빔 셋은 크기가 이미 가진 값에 곱할 뿐 대체하지 않는다. 크기는 한 판 동안 조리개를 1.75배, 사거리를 2.75배까지 벌리므로, 부품 최대치는 그 폭의 일부를 어디까지 자란 기체든 그 위에 얹는다. 테스트로 세 부품 모두 `BEAM_*_MAX`를 넘지 않는 것을 고정했다 - 작은 기체가 큰 기체처럼 훑기 시작하면 크기가 말하는 것과 어긋난다.
- 배선에서 한 가지를 조심했다. 그리는 빔은 스냅샷의 `beamRadiusScale`/`beamReachScale`에서 나오고 잡는 빔은 `BeamField`에서 나오는데, 물리 쪽에만 곱하면 보이는 빔과 실제로 잡는 빔이 다시 다른 모양이 된다(렌더 레이어에 그 사고를 설명하는 주석이 이미 있다). 두 스케일을 함수 하나로 모아 스냅샷·물리·스모크 스냅샷이 모두 같은 값을 지나게 했다. 회전력은 기존 속도 부품과 같은 방식으로 `stepDrone`의 요 레버 계수(레벨당 0.15)로 나눠 넘긴다.
- 문구: 세 언어의 `msgMysteryHeal`을 삭제하고, 장군의 첫 임무 브리핑에서 `생명력까지 전부 회복`/`ライフも全回復`/`restores your life completely` 한 마디를 뺐다. 새 부품 네 종의 콜아웃(`msgBoonTurnRate`, `msgBoonBeamRadius`, `msgBoonBeamReach`, `msgBoonBeamPull`)은 세 언어 모두 추가했다. README와 AGENTS.md의 같은 설명도 고쳤다.
- 검증: TypeScript, 단위 테스트 410개, 프로덕션 빌드 통과. 배선은 눈이 아니라 테스트로 고정했다 - `stepDrone`을 60프레임 돌려 요 속도가 정확히 1.15배인지, 원뿔 밖에 세워 둔 차가 부품 하나로 원뿔 안에 들어오는지, 빔 끝 너머의 차가 사거리 안에 들어오는지, 견인이 실제로 빨라지는지 확인한다. 여덟 종이 전부 1레벨이라는 것과 사다리 총 길이가 8이라는 것도 테스트가 지킨다.
## 2026-08-25 — 벽이 기체를 세우지 않고 밀어낸다

- 사람의 보고: "앞으로 안 가는데? 건물 벽에 붙으면 살짝 밀어내는 처리 넣어줘. 멈추는 경우 얼마나 될까."
- 먼저 쟀다. 실제 절차 도시를 생성해 놓고 조작 없이 36개 방향으로 60초씩 직진시킨 결과: **34/36 방향(94%)이 2초 이상 벽에 박혔고, 전체 비행 시간의 71%를 순항의 1/10 미만으로 기어갔으며, 최악은 60초 중 58.1초**였다. 예외 상황이 아니라 판이 굴러가는 기본 방식이었다.
- 원인은 `collideDrone`의 `next.speed *= 0.6`이다. 접촉 프레임마다 40%를 깎는데 재가속은 60Hz에서 프레임당 0.63을 더한다. 고정점이 속도 1 부근이라, 한 번 벽에 코를 박으면 영원히 그 자리에서 밀고 있었다. 스로틀 키가 있던 시절에는 `S`로 후진해 빠져나올 수 있었고, 이동 키를 없애면서 그 탈출구가 같이 사라진 것을 놓쳤다.
- Codex 구현 셋: (1) **미끄러진다** — 속도의 법선 성분만 제거하고 벽을 따라가는 성분은 남긴다. (2) **밀어낸다** — 접촉하면 바깥으로 `WALL_PUSH_OFF`(3)만큼 밀고 표면에서 살짝(0.03) 떼어 놓아, 매 프레임 같은 면을 다시 푸는 상태로 눌러앉지 않는다. (3) **기수를 돌린다** — 얼마나 정면으로 부딪혔는지(`into`)에 비례해 벽 바깥쪽으로 요를 준다. 스쳐 지나가는 각도면 거의 건드리지 않고(일부러 건물 벽을 따라 나는 것은 플레이어가 하는 짓이다), 정면이면 몇 초 안에 코가 빠진다.
- 속도 대가도 각도에 실었다: 고정 40%가 아니라 정면 정도에 비례한 최대 55%다. 스치면 사실상 공짜, 제대로 박으면 예전보다 비싸다. 데미지가 읽는 `impulse`는 손대지 않았다 — 충돌 판정의 세기는 그대로다.
- `WALL_DEFLECT_RATE`는 210°/s로 잡았다. 기체 자신의 최대 선회(185°/s)와 비슷한 값이다. 더 빠르면 벽이 조종을 빼앗는 것으로 읽히고, 150°/s에서는 최악의 경우(40m 벽에 정확히 수직으로, 손 놓고) 5초 뒤에도 19까지밖에 못 올라왔다. 210에서는 4.5초에 순항 30으로 복귀한다. 밀어낸 뒤 접촉이 끊기는 프레임이 있어 실효 각속도가 절반쯤으로 희석되는 것을 이 숫자가 되사는 셈이다.
- 결과: 같은 도시 스윕에서 **2초 이상 정체 34/36 → 0/36, 기어가는 시간 71% → 0.11%, 최악 58.1초 → 0.1초.** 브라우저에서도 조준점을 정중앙에 두고 직진시켜 확인했다 — 17초에 벽에 닿아 초당 이동이 6 → 0.9까지 떨어졌다가 조작 없이 4초 만에 5.7로 회복했다.
- 회귀 가드로 `test/wall-slide.spec.ts`를 남겼다. 실제 절차 도시를 24개 방향으로 쓸어 정체가 0인 것을 고정하고, 합성 벽에서 스침과 정면의 대가 차이, 그리고 "눌러도 정지로 수렴하지 않는다"를 따로 잡는다.
- 겸사: 자동 전진이 실제로 도는지 시뮬레이션 시간 기준으로 재봤다. 30.12 units/시뮬초로 `maxSpeed`(30) 그대로다. 헤드리스 컨테이너가 7.5fps라 벽시계로는 1/8로 보였을 뿐이다. 튜토리얼 중에는 설계대로 기체가 서 있다(`throttle: 0`).
- 검증: TypeScript, 단위 테스트 412개, 프로덕션 빌드 통과.

## 2026-08-25 — 기본 회전력을 올린다

- 사람의 결정: 디폴트 회전력을 조금 높인다.
- 올린 값은 `turnRateHigh`가 핵심이다. 자동 전진으로 바뀌면서 throttle이 1에 고정됐고(호수·과적·튜토리얼만 이 값을 깎는다), 그래서 `speedRatio`는 판 내내 이 구간의 위쪽에 붙어 있다. `turnRateLow`만 올렸다면 실제로 날면서 느끼는 것은 하나도 달라지지 않았을 것이다.
- 105°/s → 120°/s(+14%), 185°/s → 200°/s(+8%). 낮은 쪽도 같이 올린 이유는 그쪽이 월드가 throttle을 가져간 자리 - 물 위, 과적, 튜토리얼 - 이고 하필 거기가 선회가 가장 아쉬운 자리이기 때문이다. 부품 회전력(+15%)이 그 위에 곱해져 최대 138°/s가 된다.
- 두 값의 뜻을 주석으로 적어 뒀다. 이름만 보면 Low/High가 어느 쪽이 실제로 쓰이는지 말하지 않는다.
- 겸사겸사 `test/feeding.spec.ts`의 5초 타임아웃 flake를 고쳤다. 이 파일의 케이스는 씨앗 여섯 개로 25초짜리 도시를 실제로 시뮬레이션하므로 두 개가 4.2초·4.9초까지 가고, 전체 스위트와 나란히 돌면 vitest 기본 5초를 넘겨 터진다. 일이 곧 테스트의 내용이므로 양보할 것은 제한 쪽이다 - `describe`에 30초를 줬다. 이전부터 있던 문제고 이번 변경 때문이 아니다(어서션은 통과하고 시간만 넘겼다).
- 검증: TypeScript, 단위 테스트 412개(연속 3회 통과로 flake 해소 확인), 프로덕션 빌드 통과.

## 2026-08-25 — 연못가의 돌과 갈대, 그리고 하늘 전체를 빔에 올린다

- 사람의 요청: 연못에 있는 돌과 풀도 무게 1로 빨아들일 수 있게 하고, 헬리콥터 6·전투기 10·거대전함 30으로 빨아들일 수 있게 해 달라.
- 문제는 두 곳에 있었다. 호숫가 돌·갈대는 `lakeShoreDecorAround`가 만들고 InstancedMesh 두 개가 그리는 순수 장식이라 빔이 지나가도 텍스처였고, 하늘은 반대로 `beamImmune`이 종류로 면제를 주고 있었다 — "먹을 수 있느냐"가 무게와 폭이 아니라 "그것이 무엇이냐"의 문제였다.
- 호숫가: `lakeShorePropsAround`가 같은 결정론적 목록을 무게 1짜리 `BeamWorldProp`으로 낸다. 변환은 전부 코어에서 굽는다 — 소품 하나를 정적 풀(월드가 세운 자리)과 부양 풀(빔이 가진 뒤)이 나눠 그리므로, 올라가면서 모양이 변하면 교대가 들통난다. 렌더가 스스로 구하는 것은 바위의 기울기와 명암 둘뿐이고 그것도 소품 자신의 요에서 읽는다.
- 무게 1은 시작 기체의 힘 그대로이고 폭(1.6·1.4m)도 시작 선체(2.48m)를 통과한다. 두 게이트가 같은 순간에 열리므로 연못은 갓 태어난 접시가 시체가 아닌 것으로 빔을 연습할 수 있는 유일한 장소가 된다.
- 그리는 반경보다 시뮬레이션 반경을 좁혔다(`LAKE_SHORE_PROP_RADIUS_CELLS` 4셀, 그리기는 6셀). 호수 지구 하나가 273개를 깔고 그 전부가 매 프레임 스텝되면 순수한 낭비다 — 빔이 가장 넓게 퍼지는 경우(천장 고도의 터보 원뿔)가 지면에서 반경 91m이고 4셀은 최악 방향으로도 136m를 보장하므로, 닿을 수 있는 것은 하나도 빠지지 않는다. 두 목록은 좌표로 만든 같은 id를 쓴다.
- 하늘: `beamImmune`은 전부 false가 되고 `isAbsorbable`이 종류로 거부하는 것은 자폭 드론 기뢰 하나만 남았다. 기뢰는 무거워서가 아니라 잡힌 폭탄은 식량이 아니라 터지는 것이기 때문이다. 무게는 헬기 6, 전투기 10, 공중전함 30이다.
- 사다리를 한 층 올렸다. 무게 30은 상한이 12인 사다리로는 아무리 커져도 닿지 않는다. `beamStrengthForSize`는 이제 두 구간이다 — 성장의 85%까지가 도시 사다리(1~12단), 나머지 15%가 하늘 사다리(12~30단). 1에서 30까지 직선 하나로 이으면 시작 접시가 세 끼 만에 자동차를 들고 절반도 못 가 탑을 든다.
- 값싸지 않은 대가는 정직하게 적는다: 압축 때문에 도시 사다리가 조금 빨라졌다. 초고층(무게 11)이 성장의 77%가 아니라 66% 지점에서 열린다. 시작 쪽 사다리가 가르치는 구간은 건드리지 않는다.
- 결과 표(무게가 열리는 크기): 자동차 3 → 4%, 헬기 6 → 27%, 전투기 10 → 58%, 초고층 11 → 66%, 공중전함 30 → 99%. 전함은 사다리의 꼭대기이므로 크기 상한(15)에 거의 닿아야 열린다 — 판의 마지막에 할 수 있는 일이지 지나가다 하는 일이 아니고, 그것이 이 배의 무게가 30인 이유다.
- 빔 타깃 링 풀 상한에 소품 몫을 명시했다. 원래 `+ 8`이 붙어 있었지만 소품은 애초에 상한이 없는 목록이라 그 8은 여유분을 눈감아 준 숫자였다. 가장 빽빽한 지구가 400개 미만이므로 512로 이름을 붙여 두었다.
- 검증: TypeScript, 단위 테스트 408개, 프로덕션 빌드를 통과했다. `test/feeding.spec.ts`의 가장 긴 케이스(스물다섯 초 비행 열두 번)는 기본 5초 타임아웃 바로 아래에 있었고 소품이 늘면서 넘었다 — 무엇을 주장하는 테스트도 아니므로 타임아웃만 20초로 올렸다.
- 브라우저 확인: 스폰에서 가장 가까운 호수(x 71, z 279)까지 실제로 날아가 시작 크기(×0.46) 그대로 빔을 켰다. 매달린 무게가 2 → 6 → 7로 오르내리며 열네 개를 삼켰고 크기는 0.46 → 0.70이 됐다. 먹은 자리의 물가만 비고 나머지 둑은 그대로다 — 정적 풀과 부양 풀이 한 소품을 두 번 그리지도, 먹힌 소품을 되살리지도 않는다.
- 공중전함 흡수는 브라우저로 재현하지 않았다. 열리는 크기가 14.8이고 개발자 드릴이 주는 기체는 ×5.2라 드릴로는 닿지 않는다. 대신 `test/battleship.spec.ts`가 실제 함선 슬롯을 상한 크기 기체의 흡수 창에 넣어 삼켜지는 것과, 빔이 잡은 뒤로는 함선이 조타도 사격도 하지 않는 것을 확인한다.

## 2026-08-25 — 주유소·뉴스타워·폐허, 도시에서 마지막 예외를 걷어낸다

- 사람의 요청 셋: 주유소가 이제 폭파하지 않게 할 것, 뉴스타워도 건물 크기에 따라 중량을 부과해 흡수 가능하게 할 것, 폐허는 무게 4나 5로 먹을 수 있게 할 것(수치는 판단에 맡김).
- 하나로 묶은 이유: 셋 다 같은 문장의 예외였다. "무엇을 옮길 수 있는지는 무게가, 무엇을 삼킬 수 있는지는 폭이 정한다"고 해 놓고, 주유소는 무게를 통과하면 들리는 대신 터졌고, 뉴스타워는 종류로 거부됐고, 폐허는 아예 빔 오브젝트가 아니었다.
- 주유소: 통신탑이 이미 만들어 둔 길을 그대로 간다. 빔에게는 무게 9의 소품이고 레이저에게는 랜드마크이며, 둘은 같은 id(`landmarkId('gas-station', …)`)를 쓴다. 삼켜지면 `destroyedLandmarks`에도 들어가서 레이저 조준 목록이 기체 안에 있는 주유소를 겨누지 않는다. 부양 풀이 없다는 것이 폭파로 남겨 둔 유일한 이유였으므로 그것을 만들었다 — 본체와 붉은 캐노피 띠 두 메시이고, 띠는 본체 행렬에 곱해 붙이므로 실려 가는 동안에도 떨어지지 않는다.
- 이제 빔이 닿아서 터지는 물건은 도시에 하나도 없다. 폭파는 전부 레이저의 몫이다.
- 뉴스타워: `canAbsorbBuilding`의 예외를 지웠다. 높이가 최소 44이므로 무게는 언제나 10 또는 11 — 도시 사다리의 꼭대기이고 판에서 가장 늦게 열리는 건물이다. 화면이 아까워서 남겨 둔 규칙이었지만, 자기가 붙어 있는 블록만 한 기체 앞에서 살아남는 화면은 "덜 재밌다"보다 나쁜 말을 한다: 규칙이 무게가 아니라 정체에 대한 것이라고 말한다. 속보 자체는 HUD의 띠라서 타워를 삼켜도 계속 나온다.
- 폐허: 무게 5로 정했다. 4와 5 중에서 5인 이유는 두 게이트가 같이 열리기 때문이다 — 폐허는 원래 건물 바닥 면적의 90%라 폭이 14~20m이고, 무게 5가 열리는 크기(성장 19%, 크기 3.3)가 선체가 그 폭을 삼킬 수 있게 되는 크기와 거의 같다. 4로 두면 끌 수는 있는데 삼킬 수 없는 구간이 먼저 생긴다.
- 폐허는 소품이 될 수 없다. 월드 생성이 아니라 런타임 상태이고, 플레이어가 건물을 무너뜨렸기 때문에 존재한다 — 스트리밍해 올 결정론적 목록도, 돌려줄 정적 풀도 없다. 그래서 건물과 같은 길을 간다: 잡히는 즉시 `ruinedBuildings`와 콜라이더에서 빠지고 빔 오브젝트가 된다. 그리는 쪽은 언제나 한 곳뿐이라 제자리로 튕겨 돌아가는 일이 생길 수 없다.
- 점수는 60이다. 자기가 부순 건물 자리를 치우는 것은 두 번째 철거가 아니라 정리이고, 그 건물을 무너뜨린 값은 이미 한 번 치렀다.
- 겸사로 잡은 것: 주유소 부양 풀을 통신탑 풀과 똑같이 `active`로만 걸렀더니 아직 제자리에 선 주유소까지 그려서 정적 풀과 두 번 그리고 있었다. 변환이 같아 눈에 띄지는 않지만 화면 안의 주유소마다 그리기가 두 배다. `isWorldPropDisplaced`로 바꿨다 — 벤치·버스정류장·호숫가 풀이 이미 쓰는 조건이다.
- 검증: TypeScript, 단위 테스트 410개, 프로덕션 빌드를 통과했다. `test/buildings.spec.ts`의 "뉴스타워는 절대 먹히지 않는다"는 케이스는 반대 사실을 고정하는 케이스로 바꿨고, 폐허의 두 게이트가 같이 열린다는 것과 주유소의 소품 id가 랜드마크 id와 같다는 것을 확인하는 케이스를 더했다.
- 브라우저 확인(폐허): 개발자 드릴 기체(×5.2, 빔 세기 5 — 정확히 폐허의 단)로 블록 하나를 레이저로 무너뜨리고, 제자리 호버로 빔만 켰다. 매달린 무게가 정확히 5.00으로 8.5초간 유지되다가 삼켜졌고, 성장률 0.1983(지름 환산 15.5m = 원래 부지의 90%)과 획득 점수 +312(기본 60 × 크기 5.2)가 그것이 폐허였음을 확정한다.
- 브라우저 확인(주유소): 스폰 근처의 주유소(-17, -17)로 갔다. 첫 시도는 빔을 켠 채 접근하다가 도착 전에 삼켜 버려 실패했고, 반대 방향에서 키운 뒤 빔을 끄고 접근하는 순서로 다시 했다 — 무게 게이지가 34~36%까지 차고 주유소가 있던 모퉁이가 비었다. 다만 실려 가는 주유소 자체를 화면에서 잡지는 못했다. 크기 10 이상이면 흡수 창이 10m를 넘어 붙잡자마자 삼켜지고, 그보다 작은 기체는 무게 9를 들 수 없다.

## 2026-08-25 — 소품 하나는 언제나 풀 하나가 그린다

- 앞선 작업에서 발견한 것의 후속: 주유소 부양 풀이 아직 제자리에 선 주유소까지 그리고 있었다. 고치면서 세어 보니 같은 실수가 다섯 곳에 있었다 — 옥상 구조물·가로등·쓰레기통·송전탑·통신탑. 전부 `object.active`만 보고 그린다.
- 왜 아무도 못 봤나: 두 복사본이 같은 변환을 쓰므로 화면에서는 하나로 보인다. 값은 매 프레임 인스턴스 두 벌을 채우는 비용으로만 나가고, 지구 안의 해당 소품 수만큼 곱해진다.
- 규칙은 원래 하나였다. 정적 풀은 `isWorldPropHidden`(파괴됐거나 자리를 떴으면 숨김)으로 판정하는데, 부양 풀 쪽에는 그 여집합이 이름조차 없어서 풀마다 각자 다른 근사를 쓰고 있었다 — 여섯 곳은 `isWorldPropDisplaced`, 다섯 곳은 `active`. `isWorldPropLifted`를 만들어 열한 곳이 같은 질문을 하게 했다.
- 변위만으로는 부족하다는 것이 이 함수가 그냥 `isWorldPropDisplaced`가 아닌 이유다. 레이저에 맞아 날아가는 쓰레기통은 맞은 즉시 `destroyedWorldProps`에 들어가 정적 풀에서 빠지지만 아직 움직이지는 않았다 — 변위만 보면 가장 잘 보이는 그 한 프레임에 아무도 그리지 않는다.
- 렌더 쪽은 `useLiftedProp` 훅 하나로 런타임의 파괴 집합을 묶어 넘긴다. 풀마다 `runtime.current.destroyedWorldProps`를 손으로 적어 넣는 것이 바로 열한 곳이 어긋나기 시작하는 방식이다.
- 손대지 않은 것: 쓰레기 조각 풀(`TrashScatterPool`)은 `inBeam || tether || destroying`이라는 자기 조건이 따로 있다. 들려 있는 통에서만 쓰레기가 날려야 하므로 이쪽이 맞다.
- 검증: TypeScript, 단위 테스트 411개, 프로덕션 빌드를 통과했다. `test/world-props.spec.ts`에 두 판정이 정확한 여집합이라는 것을 네 가지 상태(제자리·빔 위·레이저에 맞은 직후·사라짐)로 고정하는 케이스를 더했다.
## 2026-08-25 — 벽을 더 세게 튕기고, 벽을 사다리로 쓰고, 큰 기체에 기뢰가 다시 통한다

- 사람의 요청 셋: "벽에 닿으면 많이 튕겨내줘", "UFO가 커지면 건물 사이에 끼었을 때 올라가기 힘든 문제 해결", "UFO가 많이 클 때 드론에 닿으면 폭파 데미지가 안 들어온다". 이어서 "자동 이동이 됐으니 조준점을 움직이면 그쪽을 보고 이동한다고 튜토리얼에 안내 추가, 일본어·영어까지".
- **되튐**: 고정값 3짜리 밀어내기를 들어온 속도의 몫으로 바꿨다(`WALL_BOUNCE = 0.55`, 하한 `WALL_PUSH_OFF = 8`). 스치면 하한만큼 떨어지고, 순항으로 들이받으면 16 남짓으로 튕겨 나간다. 절반 아래로 잡은 것은 들어온 것보다 멀리 날려 보내지 않기 위해서다.
- **벽은 사다리다**: 고도는 전진 속도 × 피치에서 나온다. 그래서 속도를 빼앗는 접촉은 상승 수단까지 같이 빼앗고, 건물 사이에 낀 기체는 위를 봐도 아무 일이 없었다. 이제 벽에 닿아 있는 동안에는 보는 방향으로 수직 속도를 직접 준다(`WALL_CLIMB_SPEED = 16`). 아래를 보면 내려가는 것도 같다 — 틈에서 아래로 빠져나오고 싶은 기체도 사정은 똑같다.
- **기뢰가 큰 기체를 못 때리던 버그**: 폭발 판정이 기뢰와 기체 **중심** 사이 거리를 폭발 반경(10)과 비교하고 있었다. 선체 반지름은 크기에 따라 최대 15.75까지 자란다. 그래서 다 자란 기체에서는 기뢰가 선체에 닿아 터지는 순간의 중심 거리가 이미 17을 넘어, 자기 몸에 붙어 터진 폭발의 사거리 밖에 있었다. 선체 반지름이 10을 넘는 지점 — 성장 구간의 절반 조금 넘는 곳 — 부터 기뢰는 플레이어를 아예 해칠 수 없었다. 구 대 구 겹침으로 고쳤고(`+ hitRadius`), 같은 이유로 기폭 무장 판정도 선체 기준으로 옮겼다. 무장과 피해가 같은 거리를 쓰지 않으면 경고 구체가 거짓말을 하게 된다.
- **튜토리얼**: 오프닝 다음에 조종 단계를 하나 넣었다 — "이 우주선에 이동 키는 없다, 조준점을 움직이면 기체가 그쪽을 보고 그대로 나아간다, 위를 보면 올라가고 아래를 보면 내려간다". 한·일·영 모두. 겸사겸사 일곱 번째 단계에 남아 있던 "무게 때문에 우주선이 추락한다"를 실제 동작(느려지고 고도가 내려간다)으로 바꿨다 — 추락 엔딩은 이미 없앴는데 장군만 옛말을 하고 있었다.
- 알아낸 것 하나, 고치지 않은 것 하나: `collideDrone`은 크기와 무관하게 반지름 0.5로 판정한다. 다 자란 기체의 선체 반지름은 15.75이므로 물리는 그려지는 것보다 30배 작다. "건물 사이에 낀다"는 감각의 진짜 뿌리가 여기일 가능성이 크지만(그려지는 접시는 건물을 뚫고 지나간다), 판정을 선체에 맞추면 성장한 기체의 난이도가 통째로 바뀌므로 요청 범위 밖으로 두고 보고만 한다.
- 검증: TypeScript, 단위 테스트 422개, 프로덕션 빌드 통과. 되튐·수직 구동·정면 대 스침의 대가 차이를 각각 단위로 고정했고, 수직 구동 테스트는 `WALL_CLIMB_SPEED`를 0으로 되돌리면 실패하는 것까지 확인했다(처음 쓴 "구석에서 빠져나온다" 테스트는 0에서도 통과해서 버렸다 — 기수 편향만으로 빠져나오기 때문이다). 기뢰는 크기 상한에서 기폭·폭발이 성립하는지를 새 케이스로 잡았다. 브라우저에서 새 브리핑 단계가 실제로 읽히는지와, 조작 없이 직진했을 때 벽 접촉이 초당 이동 4.5 → 3으로 한 틱만 내려갔다가 즉시 회복하는 것을 확인했다(정체 0틱).

## 2026-08-25 — 회전력을 160°/s로 올린다: 조종이 손해가 아니게

- 사람의 질문: WASD로 이동을 못 하게 됐으니 회전력을 더 올리는 게 나을까.
- 재 봤다. `test/feeding.spec.ts`의 급식 하네스를 회전력별로 돌려 세 가지 조종을 25초씩 여섯 씨앗 평균으로 비교했다 — 직진만, 가장 가까운 먹이를 향해 조종, 그리고 한 자리에서 계속 선회.

  | °/s | 순항 반경 | 터보 반경 | 직진 | 조종 | 선회 |
  | --- | --- | --- | --- | --- | --- |
  | 105 | 16.4 | 29.5 | 22.0 | 17.0 | 1.0 |
  | 120 | 14.3 | 25.8 | 22.0 | 17.5 | 1.0 |
  | 140 | 12.3 | 22.1 | 22.0 | 23.3 | 1.0 |
  | 160 | 10.7 | 19.3 | 22.0 | 26.3 | 1.0 |
  | 180 | 9.5 | 17.2 | 22.0 | 25.8 | 1.0 |

- 답은 예다. 직전 값 120에서는 **먹이를 향해 조종하는 쪽이 그냥 직진하는 것보다 덜 먹었다**(17.5 대 22.0). 게임에 남은 조종이 그것 하나뿐인데 그걸 쓰는 게 손해였다는 뜻이다. 교차점은 140 근처고, 160에서 여유 있게 넘어서며 그 뒤로는 곡선이 눕는다(180은 되레 낮고 오차 범위다).
- 그래서 105 → 120 → **160**. 순항 선회 반경 약 11, 터보 약 19이고 도시 셀이 34이니 U턴 한 번이 대략 한 블록이다.
- 위쪽 한계는 손맛이 아니라 주차다. 충분히 좁게 돌면 한 자리를 맴도는 것이 항로를 나는 것보다 나아질 수 있고, 그건 군중 규칙이 막으려는 바로 그 행동이다. 그런데 재 보니 어느 값에서도 일어나지 않는다 — 선회는 전 구간에서 25초에 1명이다. 한 번 돌아진 군중은 이미 흩어진 군중이기 때문이다. 그러니 실제 한계는 반경이 비행기처럼 보이느냐지 상한이 아니다.
- 테스트도 같이 조였다. `steered > blind * 0.55`는 "조종이 직진에 뒤처져도 된다"는 허용이었는데, 이제 그 문장은 사실이 아니다. `steered > blind`로 바꿔 조종이 값을 한다는 것을 지키게 했다.
- 옆 케이스 하나(`큰 기체가 흩어진 도시에서도 경쟁력을 유지한다`)가 이 변경에 터졌는데, 원인은 성장 페널티가 아니라 **표본이 모자란 것**이었다. 이 케이스만 평균 둘을 서로 나누므로 양쪽 잡음을 함께 지고, 씨앗 여섯 개로는 비율이 0.81~1.07을 오간다 — 문턱 0.85를 시험하는 게 아니라 동전을 던지는 셈이다. 씨앗 서른 개로 다시 재니 큰 기체도 회전력과 함께 같이 좋아졌고(14.4 → 17.7) 160에서 비율은 0.996, 즉 거의 동률이다. 그래서 문턱을 낮추는 대신 이 케이스의 씨앗을 열넷으로 늘렸다(0.94~1.00으로 안정). 나머지 케이스는 문턱에서 멀리 떨어져 있어 여섯 개로 충분하다.
- 중간에 병합 도중 `git stash`로 확인을 시도했다가 병합 상태(`MERGE_HEAD`)가 풀려 잘못된 비교를 한 번 했다. 되돌려 병합을 다시 하고, main 자체에서 테스트를 돌려 이 케이스가 원래는 통과한다는 것부터 확인한 뒤 원인을 짚었다.
- 손대지 않은 것: 기체 롤 연출(`visualTiltMax` 28°)은 실제 회전 속도가 아니라 `input.steer`와 속도비로만 계산되므로 이번 변경에 영향을 받지 않는다. 더 빨리 도는데 뱅크 각은 그대로라 덜 기울어 보일 수 있는데, 거슬리면 따로 올릴 값이다.
- 검증: TypeScript, 단위 테스트 430개(연속 2회), 프로덕션 빌드 통과. main의 성장 곡선·빔 무게 사다리 개편을 병합한 뒤에도 같은 결론이 나오는지 다시 쟀다 — 병합된 main에서도 120°/s의 조종은 여전히 손해(17.5 대 22.3)였다.

## 2026-08-25 — 기체 자세를 항공기 회전 순서(YXZ)로 합성한다

- 사람의 보고: "마우스 위로 했는데 기체가 앞쪽 밑으로 내려가 있고, 마우스 중앙 위쪽으로 했는데 오른쪽이 조금 들린 모습이다", "건물에 부딪히거나 이러면 이렇게 되네".
- 원인은 오일러 회전 순서다. `DroneScene`의 UFO 그룹이 `rotation.x = -pitch`, `rotation.y = heading`, `rotation.z = visualTilt`를 그대로 써 왔고, Three.js `Euler`의 기본 순서는 `XYZ`다. 이 순서에서 피치는 헤딩이 기체를 이미 돌려놓은 *뒤에* 월드 X축으로 걸린다. 즉 피치가 피치로 보이는 헤딩은 Z축 위(0 또는 π) 뿐이다.
- 두 증상이 정확히 이 식에서 나온다. `XYZ`에서 기수(+Z)의 높이는 `cos(heading) · sin(pitch)`다. 시작 헤딩이 π이므로 `cos(π) = -1`, 마우스를 올려 피치를 +로 주면 화면의 기수는 **아래로** 내려간다. 헤딩이 ±π/2 근처면 `cos`가 0이라 기수 높이는 그대로고 남은 것은 롤뿐이라 **한쪽 림만 들린다**. 벽에 부딪히면 이렇게 되는 것도 같은 이유다 — `WALL_DEFLECT_RATE`(210°/s)가 순식간에 헤딩을 축에서 밀어내므로, 스치고 나면 기체가 기울어진 채로 난다.
- 고친 방식은 순서 하나다. `YXZ` — 월드 상방으로 요, 그다음 기체 자신의 날개축으로 피치, 마지막에 기수축으로 뱅크. 이 순서에서 모델의 기수(+Z)는 시뮬레이션이 적분하는 전진 벡터 `(sin h·cos p, sin p, cos h·cos p)`에 모든 헤딩에서 정확히 얹힌다.
- 각도 합성은 `src/core/craftPose.ts`로 뺐다. `core`는 Three.js에 의존하지 않는 규칙이라 순서 이름과 각도만 돌려주고, 렌더 레이어가 `rotation.set(x, y, z, order)`로 받는다. 추격 카메라가 손으로 다시 쓰던 전진 벡터도 같은 파일의 `craftForward`를 쓰게 해서, 기체가 향한 곳과 카메라가 아는 앞이 두 벌로 갈라지지 않게 했다.
- 검증: `test/craft-pose.spec.ts`가 회귀를 잡는다 — 16개 헤딩에서 기수가 전진 벡터와 일치하는 것, 어느 헤딩에서든 마우스를 올리면 기수가 실제로 들리는 것, 뱅크는 기수를 건드리지 않는 것, 그리고 `stepDrone`을 60프레임 돌린 결과가 상승과 들린 기수를 동시에 내는 것. 옛 `XYZ`로 되돌리면 네 개 중 셋이 깨진다. 브라우저에서도 같은 지점(헤딩 2.9rad, 피치 +0.82rad)을 전후로 찍어 비교했다 — 고치기 전에는 접시가 눈에 띄게 기울어 있고, 고친 뒤에는 진행 방향과 나란하다.
- TypeScript, 단위 테스트 통과(이 작업 단독 424개, main 병합 후 440개), 프로덕션 빌드 통과. 브라우저 스모크는 이 컨테이너의 Playwright 브라우저 빌드가 핀 버전과 달라 실행되지 않아, 같은 크로미움을 직접 띄운 스크린샷 비교로 대신했다.

## 2026-08-25 — 조종 안내에서 "이동 키는 없다"를 뺀다

- 사람의 지시: 조종 단계를 "먼저 조종이다. / 마우스로 조준점을 움직이면 기체가 그쪽을 보고, 보는 방향 그대로 나아간다. / 위를 보면 올라가고 아래를 보면 내려간다. 대원이 정할 것은 방향 하나뿐이다."로.
- 첫 줄에서 "이 기체에 이동 키는 없다 — 늘 스스로 앞으로 날아간다"를 뺐다. 없는 것을 설명하는 문장이었고, 바로 다음 줄이 있는 것을 설명한다. 처음 잡은 사람에게는 없어진 키보다 지금 쥔 마우스가 먼저다.
- 한·일·영 모두 같은 모양으로 줄였다. 강조(`[[ ]]`)는 둘째 줄에 그대로 두었다 — 마크업이지 문구가 아니고, 화면에 나오는 말은 지시하신 그대로다.
- 검증: TypeScript, 단위 테스트 440개, 프로덕션 빌드 통과. 세 언어 각각 브라우저에서 브리핑 두 번째 단계를 실제로 띄워 문구를 읽었다.

## 2026-08-25 — 물에 점수를 붙이고, 물 임무를 300리터로 줄이고, 미션 게이지를 고친다

- 사람의 질문과 요청: "현재 물 흡수 점수가 오르나? 안 오르면 아주 조금씩 스코어·흡수 점수도 오르게 해 달라. 물 흡수 미션 400리터를 200리터로 줄여 달라. 그리고 게이지가 진척도랑 안 맞는 것 같으니 확인해 달라." 이어서 "아냐 물 300리터로 해 줘.", "리터당 1점으로 해 줘."
- 답: 오르지 않았다. `stepLakeAbsorption`이 돌려준 리터는 `totals.water`(미션 2 게이지)로만 들어갔고, `game.score`에도 `totals.absorbScore`에도 한 푼도 닿지 않았다. `game.waterAbsorbed`는 스냅샷에 실리기만 할 뿐 HUD 어디에서도 읽지 않고, `beamBallast`도 물을 세지 않는다. 즉 호수 위에서 빔을 잡고 있는 동안 플레이어에게 돌아오는 것은 속도 저하뿐이었다 — 게임에서 유일하게 손해만 보는 빔 사용이 임무 사다리의 한 칸이었던 셈이다.
- 물 점수: 리터당 1점(`LAKE_SCORE_PER_LITRE`). 50 L/s이므로 초당 50점이다. `bankAbsorbScore`를 지나므로 점수와 표본 게이지 양쪽에 들어간다 — 빔으로 삼킨 것은 표본이라는 규칙 그대로다.
- 처음에는 리터당 0.1점(초당 5점)으로 넣었다가 사람의 요청으로 1점으로 올렸다. 표본 임무 3000점이 호수에서 60초가 된다는 것을 밝히고 올렸다 — 판이 300초이므로 도시를 먹지 않고도 그 칸을 끝낼 수 있다. 그 대가는 호수 위에서 절반 속도로 묶인 채 하늘이 보내는 것을 그대로 맞는 60초다.
- 크기 배수를 곱하지 않는다. 다른 흡수 보상과 다른 유일한 예외인데, 물은 기체가 얼마나 크든 50 L/s로 일정하게 들어오기 때문이다. 배수를 곱하면 크기 상한에서 초당 750점이 되어 판 하나가 20초로 끝난다.
- 프레임당 소수점을 지급하지 않는다. `lakeScorePayout(이전 리터, 다음 리터)`가 양쪽을 내림해 차이만 준다. 60분의 1초는 0.83리터라 반올림하면 프레임마다 1점씩 새고 버리면 영영 사라진다. 나머지는 리터 총량 자체가 들고 있으므로 따로 이월 변수를 두지 않았다.
- 물 임무 400 → 300리터. 50 L/s에서 8초가 6초가 되고, 리터당 1점이므로 이 칸 하나가 300점이다. 이 칸이 가르치는 것은 물의 무게이고 그건 첫 1초에 몸으로 안다. 나머지 시간은 도망칠 수 없는 자리에 가만히 앉아 미터를 기다리는 시간이었다.
- 게이지 버그(실재했다): `.mission-panel > div`가 목표 줄을 꾸미는 규칙이었는데, 게이지도 같은 패널의 직계 div다. 게이지가 `display:grid`와 `24px minmax(0,1fr) auto` 3열 격자를 그대로 물려받아, 채움 막대 `<i>`가 24px짜리 글리프 칸 안에 갇혔다. 실측하면 290px 막대에서 50% 게이지가 12px(4.1%)로 그려졌다 — 100%여도 24px 토막이다. 게다가 `min-height:33px`과 `border-top`까지 따라와 9px 막대가 33px 상자가 되어 있었다.
- 고침: 목표 줄에 `.mission-row` 클래스를 주고 패널 규칙 전부를 그 클래스로 좁혔다. 모바일 오버라이드도 같이 옮겼다. 같은 실측에서 50%가 143px/286px(콘텐츠 폭 기준 정확히 50%)로 나온다.
- 회귀 가드: DOM이 없는 스위트라 픽셀 대신 원본을 검사한다. `.mission-panel > div`가 다시 나타나면 실패하고, `Hud.tsx`가 `mission-row`를 잃어도, `.mission-gauge`가 grid/flex가 되어도 실패한다. 이를 위해 `vitest.config.ts`에 `css: true`를 켰다 — 기본값에서는 스타일시트가 빈 문자열로 스텁되어 `?raw` 가드가 조용히 아무것도 검사하지 않는다.
- 검증: TypeScript, 단위 테스트 405개, 프로덕션 빌드 통과. 게이지는 실제 Chromium에 패널 마크업을 띄워 `getBoundingClientRect`로 고치기 전(12px/290px)과 후(143px/290px)를 숫자로 확인했다.

## 2026-08-25 — 물도 먹을 수 있게 하고, 다 마신 물 타일은 마르게 한다

- 사람의 요청: "물도 먹을 수 있게 해 줘. 그리고 물은 일정량 먹으면 물 타일이 사라지게 해 줘. 값은 추천해 달라."
- 왜 이 두 가지가 한 항목인가: 물에 점수를 붙인 직후(리터당 1점, 초당 50점) 호수는 무한 자원이 됐다. 한 자리에 앉아 있으면 표본 임무가 60초에 끝난다. 타일이 마른다는 규칙이 그 무한을 예산으로 바꾼다 — 그래서 성장과 고갈은 같은 결정의 양면이다.
- 타일 용량 **250리터**. 50 L/s에서 5초다. 두 가지가 이 값에 들어 있다. 물 임무가 300리터이므로 한 타일에 서서 끝낼 수 없고, 한 칸 비우고 옆으로 옮겨야 한다 — "가만히 있으면 굶는다"는 이 저장소의 규칙을 그것을 가장 유혹하는 지형에 적용한 것이다. 그리고 호수 하나는 2~4타일이므로 아무리 오래 앉아 있어도 물 한 덩이가 줄 수 있는 최대치는 500~1000점으로 묶인다.
- 타일 하나를 비우면 **+12%** 성장. 리터가 아니라 타일에 붙였다. 물은 빔이 연속으로 받아들이는 유일한 것이라, 리터마다 주면 5초 내내 매 프레임 크기 펄스가 터진다 — 반응이 아니라 스트로브다. 타일을 비우는 것이 "삼키는" 순간이고, 다른 모든 흡수와 같은 방식으로 한 번에 팝 하고 들어온다.
- +12%의 근거: 고양이가 +5.8%, `absorbBeamObject`가 줄 수 있는 최대(초고층)가 +20%다. 타일은 도망칠 수 없는 자리에서 절반 속도로 5초를 묶이는 대가이므로 초고층 쪽에 가깝게 뒀다. 평균 3타일 호수가 약 +40%로 복리되는데, 같은 15초를 도시에서 먹으면(초당 0.8명, +3.5%씩) +51%다. 호수는 안전해 보이는 느린 선택이지 더 나은 선택이 아니다 — 이 부등식은 테스트로 고정했다.
- 지형은 건드리지 않는다. `lakeClusterForCell`은 그대로다. 마름은 지형 변경이 아니라 오버레이다 — 셀은 여전히 호수 셀이라 분지에 건물이 서지 않고 군중·차량도 들어오지 않으며 물가의 나무와 자갈도 제자리에 남는다. 사라지는 것은 물뿐이고, 남는 것은 물이 있던 바닥으로 읽힌다.
- 마른 타일은 그냥 물이 아니다. 런타임이 깊이를 0으로 넘긴다. 그 사실 하나로 빔이 아무것도 가져가지 않고, 항력이 풀리고, 미션이 세지 않는다 — 아래쪽에 특수 케이스를 세 군데 두는 대신이다.
- 셀 단위로 마른다. 호수 통째가 아니라 한 칸씩이라, 발밑에 구멍이 생기고 조종사가 거기서 빠져나와야 한다. 물 한 덩이가 통째로 깜빡 사라지는 것보다 이쪽이 읽힌다.
- 물 메시와 레이더가 같이 안다. `lakesRevision`을 재빌드 키에 넣어, 기체가 셀을 넘지 않아도 물이 실제로 사라지는 프레임에 메시가 다시 만들어진다. 레이더도 마른 타일을 파랗게 칠하지 않는다 — 마른 분지로 조종사를 다시 보내는 표식이기 때문이다.
- 장군의 무전을 고쳤다. "임무가 아니면 물 위의 빔은 피해라"는 이제 틀린 조언이다. 한 칸을 끝까지 비우면 물이 마르고 선체가 자란다는 것, 한 칸이 250리터라는 것, 그리고 마시는 동안 도망칠 수 없는 자리에 있다는 것을 세 언어로 말하게 했다. 대사의 250은 `LAKE_CELL_CAPACITY`와 어긋날 수 있는 유일한 숫자라 테스트로 묶었다.
- 겸사: `test/feeding.spec.ts`의 느린 두 케이스에 20초 타임아웃을 줬다. 실제 런타임을 수백 초 돌리는 테스트라 vitest 기본값 5초에 3.9~4.7초로 붙어 있었고, 부하가 걸린 머신에서 무작위로 터졌다(이번 작업 전에도 터진다). 밸런스를 보는 테스트지 속도를 보는 테스트가 아니다.
- 검증: TypeScript, 단위 테스트 409개, 프로덕션 빌드 통과. 생성된 실제 호수(4타일)를 프레임 단위로 돌려 1000리터·1000점·20초·+57%가 나오고 그 뒤로는 아무것도 나오지 않는 것을 확인했다. 이어서 실제 브라우저에서 3타일 호수(x -119, z -493)에 기체를 세우고 빔을 잡아, 타일이 정확히 250리터에서 화면에서 사라지고 `lakesRevision`이 오르고 크기가 0.46 → 0.5152(정확히 +12%)가 되고 항력이 풀리는 것(`anchored: false`)을 스크린샷과 수치로 확인했다. 레이더도 그 자리에 구멍이 난 채로 그려졌고, 물가의 자갈과 갈대는 남았다.

## 2026-08-25 — 물 타일 용량을 굴리고, 물 점수를 조정하고, 장군의 대사를 바꾼다

- 사람의 요청: 무전 대사를 직접 써서 보내 주고, "물 스코어를 적절히 올려 달라", "물 최대치는 300~500리터 랜덤으로" 해 달라.
- 대사: 사람이 쓴 네 줄을 거의 그대로 넣었다. 문장 끝 마침표만 다른 대사들과 맞췄다. 새 대사는 물을 "소중한 지구의 연구 데이터"로 팔고 보이면 적극 흡수하라고 하며, 한 칸을 다 비우면 물이 말라 없어진다는 경고로 끝난다. 성장 이야기와 "옆 칸으로 옮겨라"는 사람이 뺐고, 용량이 이제 타일마다 다르므로 옳은 판단이다.
- 타일 용량 250 고정 → **300~500 랜덤**. 고정값은 모든 타일을 똑같은 5초짜리 물탱크로 만들었다. 굴리면 위에서 봐서는 어느 칸이 깊은지 알 수 없고, 호수는 재는 것이 아니라 따라 날아가 보는 것이 된다.
- 랜덤이지만 `Math.random`이 아니다. 셀 좌표에서 파생한다(`seedForWorldCell`, 이 모듈 전용 솔트). 지구를 떠났다 돌아오면 같은 호수여야 하고, 다시 굴리는 용량은 절반쯤 마신 타일을 조용히 다시 채운다. 저장하지 않는 이 월드의 규칙 그대로다.
- 하한이 300인 이유: 물 임무 목표와 같다. 세상에서 가장 얕은 타일도 그 칸 하나로 임무를 끝낼 수 있다. 이전(250)에는 반드시 두 칸이 필요했는데, 그 성질은 사람이 대사에서 뺐으므로 함께 놓아 주었다.
- 성장은 이제 리터당(`LAKE_DRAIN_SIZE_GAIN_PER_LITRE = 0.0004`)이되, 여전히 타일이 마르는 순간에 한 번에 지급한다. 깊은 타일이 더 오래 걸릴 뿐 아니라 실제로 더 큰 끼니가 된다. 범위로는 +12%~+20%이고, 아래는 고양이(+5.8%) 위, 위는 초고층(+20%)에 정확히 닿는다.
- 초당으로 환산하면 용량과 무관하게 늘 약 1.9%다. 도시를 먹는 것이 초당 2.8%이므로 호수는 언제나 도시의 3분의 2다. 테스트는 이제 타일 개수가 아니라 초당 성장률로 이 부등식을 고정한다 — 시간을 맞추지 않은 비교는 깊은 타일이 더 크다는 사실을 성장률로 착각한다.
- 점수는 리터당 2점으로 올렸다가 사람의 플레이 피드백("점수 너무 많다")으로 **1점**(초당 50점)으로 되돌렸다. 요율이 요율일 수 있는 것은 물이 요율이 아니라 예산이기 때문이다 — 타일은 다시 차지 않으므로 물 한 덩이는 한 번 줄 것을 주고 영영 마른 분지가 된다. 2점에서는 세상에서 가장 부유한 호수 하나가 표본 임무(3000점)를 통째로 끝냈다. 우회할 가치가 있는 길이 한 단계를 건너뛰는 길이어서는 안 된다 — 이제 이론상 최대 호수(4타일 × 500리터)도 2000점이라 표본 임무 아래로 묶이고, 그 부등식을 테스트로 고정했다.
- API를 키 문자열에서 셀 좌표로 바꿨다. 용량이 좌표에서 나오므로 `lakeCellRemaining(state, key)`로는 답할 수 없다. 호출자 셋(런타임·물 메시·레이더)은 모두 이미 셀 좌표를 들고 있었다.
- i18n 테스트를 뒤집었다. 전에는 대사가 용량을 인용하는지 확인했는데, 이제는 300~500 어느 숫자도 인용하지 않는지 확인한다. 타일마다 다른 값을 대사가 말하면 한 칸에 대해서만 맞고 나머지에 대해서는 거짓말이 된다.
- 실측(생성된 호수 150개): 타일 300~500리터(평균 395) = 395점 / 7.9초. 호수 평균 3.1타일 = 1226리터 / 1226점 / 25초 / +59%. 가장 부유한 호수는 4타일 1859리터 1859점 37초 +98%다. 성장은 그대로 두었다 — 너무 많았던 것은 점수였다.
- 검증: TypeScript, 단위 테스트 410개, 프로덕션 빌드 통과. 실제 브라우저에서 셀 (-4,-15)에 기체를 세우고 빔을 잡아, 그 타일이 굴린 용량인 352리터에서 정확히 마르고 점수 704(=352×2), 크기 0.46 → 0.524768(=0.46×(1+352×0.0004))이 되는 것을 확인했다. 화면에서 그 칸만 사라지고 옆 칸은 물로 남았으며, 레이더에도 같은 자리에 구멍이 났다.

## 2026-08-25 — main 병합: 물 흡수 개편을 용어 정리·과적 개편과 합친다

- main이 그동안 37커밋을 앞서 갔고, 겹치는 파일이 아홉이라 리베이스 대신 병합했다. 여섯 커밋을 하나씩 재생하면 같은 충돌을 여섯 번 푸는 일이 된다.
- 장군은 이제 UFO라고 하지 않는다. main이 장군의 목소리에서 UFO·우주선을 은퇴시키고 기체로 통일했고 테스트로 고정해 두었다. 새 물 무전의 "UFO의 속도가 대폭 저하된다"도 "기체의 속도가"로 맞췄다 — 사람이 써 준 문장이지만, 같은 인물이 한 화면에서 두 이름을 쓰는 쪽이 더 나쁘다.
- 선체도 마찬가지다. main이 선체/船体/hull을 전부 은퇴시켰으므로 `msgLakeDrained`의 "선체 성장"은 "기체 성장"이 됐다(일·영도 같이).
- `msgCarLaunched`가 `msgVehicleDestroyed`로 바뀌어 있었다. 물 고갈 알림은 그 옆에 그대로 붙는다.
- 과적 추락이 사라졌다. main이 `shouldCrashFromOverload`를 속도 하한(`OVERLOAD_CRUISE_FLOOR`)으로 대체했으므로, 그 함수를 확인하던 `test/recon-systems.spec.ts`의 케이스도 main을 따라 지웠다.
- `test/feeding.spec.ts`의 타임아웃은 main 것을 쓴다. 같은 flake를 양쪽이 각자 고쳤는데, 케이스마다 인자를 붙인 이쪽보다 스위트 전체에 `{ timeout: 30_000 }`을 건 main 쪽이 낫다.
- 검증: TypeScript, 단위 테스트 445개(44파일), 프로덕션 빌드 통과. 병합 뒤 실제 브라우저에서 다시 호수에 세워 타일이 굴린 용량 352리터에서 마르고 `lakesRevision`이 오르고 항력이 풀리는 것을 확인했다 — main이 빔 사거리와 구경을 넓혀 물가 소품까지 함께 빨려 들어오지만(화면의 "대형 오브젝트 흡수 · +6"), 물 자체의 계산은 그대로다.

## 2026-08-25 — main 재병합: 흡수 임무 상향과 겹친 부분을 정리한다

- 푸시 직전에 main이 다시 앞서 갔다(회전력 상향, 기체 자세 YXZ 합성, 흡수 임무 상향 등 여덟 커밋).
- 임무 목표에서 충돌했지만 실제로 겹친 것은 없었다. main은 `absorb-water`를 건드리지 않았고 바로 아래 `absorb-samples` 주석만 다시 썼다 — 물 300리터는 그대로 두고 main의 새 문구를 받았다.
- 그 결과 표본 임무가 3000에서 5000으로, 파괴 임무가 1800에서 3000으로 올랐다. 물이 표본 게이지를 통째로 채우지 못한다는 불변식(이론상 최대 호수 2000점)은 여유가 더 늘어난 채 그대로 성립한다.
- 검증: TypeScript, 단위 테스트 449개, 프로덕션 빌드 통과.
## 2026-08-25 — 기체의 몸을 하나로 만든다: 건물 충돌도 선체 반지름으로

- 사람의 제안: "콜라이더를 늘리는 게 좋지 않아? 그래야 드론도 자연스레 폭파하지."
- 맞는 지적이었고, 앞뒤가 안 맞는 상태였다. 적은 이미 `sizeProfile.hitRadius`로 판정하고 있었다 — 기뢰는 중심이 아니라 선체에 닿아 터진다 — 그런데 건물만 `DRONE_DEFAULTS.radius`(0.5) 고정이었다. 같은 기체가 무엇에 닿느냐에 따라 몸이 두 개였고, 다 자란 30m짜리 접시가 구슬처럼 도시를 통과했다. 그려지는 것과 도시가 느끼는 것이 다른 물건이었다.
- `collideDrone`에 선체 반지름을 받게 하고 `GameContext`가 `sizeProfile.hitRadius`를 넘긴다. 시작 크기의 선체는 0.48이라 초반 감각은 그대로다(옛 고정값 0.5).
- 그래서 성장이 하늘을 산다. 실제 도시를 크기·고도별로 쓸어 잰 값(조작 없이 직진, 40초, 12방향):

  | 크기 | 선체 | 편하게 나는 고도 | 거리 (8 / 40 / 80) | 빔 사거리 |
  | --- | --- | --- | --- | --- |
  | ×2 | 2.1 | 20 이상 | 639 / 843 / 885 | 47 |
  | ×5 | 5.3 | 40 이상 | 312 / 803 / 885 | 62 |
  | ×10 | 10.5 | 60 이상 | 12 / 653 / 885 | 86 |
  | ×15 | 15.8 | 80 이상 | 10 / 526 / 871 | 110 |

  거리는 40초 동안 실제로 나아간 유닛이다. 큰 기체가 지면 근처에서 10~12에 그치는 것은 멈춰서가 아니라(속도는 멀쩡하다) 블록 안에서 튕겨 다니기 때문이다. 나가는 길은 위다 — 코를 들면 어느 크기든 4초 안에 지붕 위로 빠져나온다.
- 굶지 않는다: 빔 사거리도 같은 곡선을 탄다. ×15가 순항하는 100에서 사거리 110이면 원뿔이 거리에 닿는다. 커지면 높이 날고, 높이 나는 만큼 빔이 길어져서 먹이는 계속 닿는다.
- 대가는 정직하게 오른다. 건물 접촉 피해는 크기와 함께 늘어난다 — 조작 없이 지면 높이로 직진했을 때 ×2가 분당 44회, ×15가 130회다(실제로는 `damageCooldown` 1.05초가 한 번 더 막아 분당 57회가 상한). 조향하는 플레이어에게는 대부분 스침이라 법선 속도가 작고 임계값 2.5를 넘지 않지만, "큰 기체로 거리를 훑는" 플레이는 이제 성립하지 않는다. 그것이 이 변경이 사려는 규칙이다.
- 검증: TypeScript, 단위 테스트 441개, 프로덕션 빌드 통과. `test/wall-slide.spec.ts`에 "기체의 몸은 하나다"를 고정했다 — 벽면에서 10m 떨어진 지점이 시작 크기에서는 안 닿고 상한 크기에서는 닿는다. 브라우저에서 개발자 드릴(×5.20)로 실제 비행과 상승을 확인했다(콘솔 에러 없음).


## 2026-08-25 — 물 위에서 더 느려지고, 호수는 한 칸을 비우면 통째로 마른다

- 사람의 요청: 물 위에서 빔 흡수 시 이동속도를 20% 더 줄이고, 붙어 있는 물 타일은 한 칸이 마르면 전부 사라지게 해 달라.
- 속도: `LAKE_BEAM_SPEED_SCALE` 0.5 → **0.4**. 이동속도에서 다시 20%를 뺀 값이다(0.5 × 0.8). 물 위 최고 속도가 절반에서 5분의 2로 내려갔다.
- 여전히 최고 속도 상한이지 프레임 감쇠가 아니다. 예전에 속도 자체를 매 프레임 곱하던 버전은 60Hz에서 기체를 제자리에 못 박았고, 그건 물이 무겁다가 아니라 빔이 고장 났다로 읽혔다. 지금도 가속·선회·횡이동은 그대로이고 상한만 낮다 — 아무리 느려져도 호수를 빠져나갈 수는 있다.
- 호수는 이제 덩어리로 마른다. 한 칸을 바닥까지 마시면 그 칸에 붙은 타일 전부가 함께 사라진다. 물은 타일에 담겨 있는 게 아니라 호수에 담겨 있고, 한 칸씩 비면 같은 수면 높이의 웅덩이가 체스판처럼 남는데 그건 물이 마르는 모습이 아니다.
- 대신 판 만큼만 받는다. 점수와 성장은 실제로 퍼 올린 한 칸 분(300~500리터)이고, 나머지 칸은 값 없이 사라진다. 규칙의 대가이고 방향은 이쪽이 맞다 — 타일이 공짜로 없어지는 것이지 공짜로 값을 치르는 것이 아니다.
- 그래서 호수 한 덩이의 가치가 평균 1226점에서 **395점**으로 내려갔다. 물은 이제 지나는 길에 한 번 마시는 것이지 순회할 자원이 아니다.
- 상태를 둘로 나눴다. `drawn`은 빔 아래 타일을 얼마나 팠는지, `dryClusters`는 어느 호수가 없어졌는지다. 한 칸을 비우면 호수가 없어지므로 둘째는 첫째에서 유도되지 않는다.
- 대사와 알림을 고쳤다. "한 칸을 끝까지 비우면 그 호수 전체가 말라 없어지고 만다"로, 알림은 "호수 한 칸 고갈"에서 "호수 고갈"로(일·영도 같이). 한 칸이 마르는 것과 호수가 마르는 것은 다른 경고다.
- 검증: TypeScript, 단위 테스트 449개, 프로덕션 빌드 통과. 드레인 테스트는 이제 생성된 실제 호수를 써서, 퍼 올린 칸 하나가 바닥나는 순간 붙어 있는 칸들이 한 방울도 팔리지 않은 채(`drawn`에 없는 채) 전부 마르는지 확인한다. 브라우저에서도 3타일 호수(`lake:-1:-3`)에 기체를 세워 352리터를 퍼자 세 칸이 동시에 사라지고 레이더에서도 물이 통째로 없어지는 것을 확인했다.
## 2026-08-25 — 빔은 기뢰를 끌어오지 않고 불만 붙인다

- 사람의 걱정: "콜라이더를 늘리면 드론까지 흡수되는 상황이 많아질 텐데, 드론 흡수는 포기할까?" 이어서 "드론 흡수는 포기 못해. 대신 드론은 빔에 닿으면 0.3초 후에 폭파하거나 하자." 그리고 "0.3초가 맞아? 시간은 추천 좀."
- 먼저 재봤다. 걱정이 맞았고, 콜라이더와는 무관했다. 원뿔이 크기와 함께 자라 상한에서 지면 기준 반지름 57에 이르는데(사거리도 110), 빔이 닿는 것을 끌어오니 후반 지뢰밭 위에서 빔을 켜고 있으면 분당 269개를 물어 와 252개를 선체에 대고 터뜨렸다. 초당 네 번이다. 피해 쿨다운 1.05초가 막아도 분당 28칸이라 **빔을 12초 켜면 죽는다** — 게임의 유일한 동사가 자살 버튼이었다.
- 이 문제를 가리고 있던 것이 바로 앞 커밋에서 고친 버그였다. 중심 기준 거리로 재던 동안에는 다 자란 기체에 폭발 피해가 아예 안 들어갔으므로, 252번 터져도 아프지 않았다. 버그를 고치니 드러났다.
- 처음에는 기뢰를 통째로 빔에서 빼려 했지만(사람도 한 번 동의했다) 곧 뒤집혔다. 최종 규칙은 **원뿔이 견인줄이 아니라 도화선**이다: 기뢰는 `beamImmune`이라 절대 끌려오지 않고, 새 `armMinesInBeam`이 원뿔 안의 기뢰에 `DRONE_MINE_FUSE`를 걸어 제자리에서 터지게 한다. 위협은 하나도 포기하지 않았다 — 근접 점화도, 선체 접촉 기폭도 그대로다.
- 퓨즈는 0.3초를 그대로 쓴다. 새 상수를 만들지 않은 것이 요점이다: 기뢰는 어떻게 불이 붙든 정확히 0.3초를 준다. 순항 30으로 계산한 탈출 거리를 놓고 보면 0.3초에서만 "자기 폭발 반경 안쪽 높이로 날면서 쓸면 맞는다"가 남는다(×0.46 고도 8에서 9.8 대 위험 10.5, ×2 고도 12에서 12.0 대 12.1). 0.5초로 늘리면 어떤 크기·어떤 고도에서도 절대 맞지 않아 빔이 만능 무료 지뢰제거기가 되고 기뢰가 위협이기를 그만둔다.
- 그래서 값은 고도가 치른다. 후반 지뢰밭 위에서 빔을 1분 켜고 있을 때 분당 잃는 하트: ×0.46 고도 8이 1.5, ×2 고도 12가 3.5, ×5 고도 40이 1.0, ×10 고도 60이 0, ×15 고도 100이 0이다. 마지막 줄은 149개를 공짜로 쓸어내면서 한 칸도 잃지 않는다는 뜻이고, 고치기 전 같은 자리에서 28칸을 잃던 것과 대비된다. 커지면 하늘이 자리라는 규칙에 지뢰밭까지 붙은 셈이다.
- 검증: TypeScript, 단위 테스트 451개, 프로덕션 빌드 통과. `test/kamikaze.spec.ts`에 "제자리에서 불붙고 절대 끌려오지 않는다"를 고정했다 — 상한 크기의 원뿔 안에 있는 기뢰가 `inBeam`도 `tether`도 갖지 않은 채 자기 호버 진폭 안에서 터지고, 그 자리가 선체에서 폭발 반경 + 선체 반지름보다 멀다는 것까지 확인한다. 헬기·전투기는 여전히 끌려오고 여전히 식사다.
## 2026-08-25 — 성장 상승폭을 4분의 1 깎는다

- 사람의 요청: "ufo 너무 빨리 커진다 커지는 상승폭 좀만 낮춰줘."
- 한 끼당 성장률(`SIZE_GAIN`)을 약 24% 내렸다. 보행자 0.046 → **0.035**, 고양이 0.076 → **0.058**이다. 둘의 비율(고양이가 1.65배)은 그대로 두어 빠르고 잘 도망치는 표적을 쫓을 이유는 건드리지 않았다.
- 빔 오브젝트 흡수도 같은 폭으로 따라 내렸다: `0.012 + 지름 × 0.012` → **`0.009 + 지름 × 0.009`**. 상한 0.2는 손대지 않았으므로 가장 큰 타워 한 채가 주는 값은 예전 그대로다 — 깎인 것은 판마다 몇십 번 있는 평범한 식사이지, 판당 한 번 있는 큰 식사가 아니다.
- 곡선의 모양은 하나도 바꾸지 않았다. `GROWTH_FALLOFF_MIN`(0.18)도 `GROWTH_FALLOFF_EXPONENT`(2)도 `SIZE_MAX`(15)도 그대로다. 요청이 "상승폭을 낮춰 달라"이지 "천장을 낮춰 달라"가 아니므로, 도달점은 두고 도달 속도만 늦추는 쪽이 맞다.
- 그래서 상한까지 필요한 보행자 수가 **95끼에서 124끼로** 늘었다. 구간별로는 첫 30% 구간이 53 → 70끼, 마지막 30% 구간이 20 → 26끼다. 후반 감속(`growthFalloff`)이 하던 일은 그대로 하고, 전체가 한 단계 느려졌을 뿐이다.
- 실제 판으로 환산하면(`test/feeding.spec.ts`가 재는 조종 시 25초당 27.7끼 기준) 60초 시점 크기가 **×8.05에서 ×4.35로**, 상한 도달이 86초에서 **112초로** 밀렸다. 5분짜리 판에서 1분 만에 다 자라 있던 것이 2분 가까이로 늘어난 셈이다.
- 남는 사실 하나: 잘 먹는 플레이어는 여전히 판의 절반 이전에 천장에 닿는다. 이번 요청은 "좀만"이었으므로 상승폭만 손봤다. 더 늦추려면 `SIZE_MAX`를 올리거나 `GROWTH_FALLOFF_MIN`을 내려 후반 감속을 더 세게 무는 쪽이 다음 손잡이다.
- 호수는 일부러 그대로 뒀다(`LAKE_DRAIN_SIZE_GAIN_PER_LITRE` 0.0004). 물의 설계 의도는 "도시의 3분의 2 속도"인데 도시 쪽이 초당 3.7%에서 2.8%로 내려오면서 물이 초당 1.9%로 오히려 의도한 비율(0.51 → 0.66)에 다시 맞았다. 물은 여전히 도시보다 느리다.
- 검증: TypeScript, 단위 테스트 456개, 프로덕션 빌드 통과. `test/size.spec.ts`의 튜닝 가드는 새 곡선에 맞춰 상한만 옮겼다(첫 구간 60 → 80끼, 상한 도달 120 → 140끼). 하한(마지막 구간 15끼 초과)은 그대로다 — 마지막 구간이 요식행위가 되지 않아야 한다는 규칙은 이번 변경과 무관하다.
## 2026-08-25 — 성장 곡선을 "끼니 수"가 아니라 "초심자의 5분"에 맞춘다

- 사람의 요청: 천장(`SIZE_MAX` 15, 지름 81m)이 있다는 것을 확인한 뒤 — "제한은 5분 동안 빡세게 할 경우 도달할 수 있을 정도로 지수 함수 설정해주고 `GROWTH_FALLOFF_MIN`를 조금 더 낮춰줘", "전체적인 성장 속도는 조금 낮춰서 초심자가 힘내면 3~4분에 max치의 60프로 도달할 수 있는 정도로".
- 기준점을 끼니 수가 아니라 **분**으로 다시 잡았다. 초심자가 힘낼 때의 섭취를 **초당 0.6명**으로 놓는다 — `test/feeding.spec.ts`가 재는 조종 봇(초당 1.107명)의 절반을 조금 넘는 값이며, 사람은 먹는 동시에 피하고 조준하고 임무를 읽기 때문이다.
- `SIZE_GAIN` 보행자 0.035 → **0.026**, 고양이 0.058 → **0.043**. 빔 오브젝트도 같은 비율로 `0.009 + 지름 × 0.009` → **`0.007 + 지름 × 0.007`**(상한 0.2는 그대로).
- `GROWTH_FALLOFF_MIN` 0.18 → **0.07**. 천장 부근 한 끼의 값이 개막의 18%에서 7%로 내려간다.
- 결과(초당 0.6명 기준): **60% 지점 207초(3.5분), 천장 307초(5.1분)**. 요청한 두 숫자가 같은 곡선 위에 함께 놓인다.
- 구간별로는 첫 30%가 70 → **93끼**, 마지막 30%가 26 → **51끼**, 천장까지가 124 → **184끼**다. 후반이 전반보다 비싸진 것이 이번 변경의 핵심이다.
- `GROWTH_FALLOFF_MIN`이 유일한 손잡이인 이유도 적어 뒀다. 비례 성장은 범위를 배수로 재는데 마지막 40% 구간은 배수로 치면 3분의 1배도 안 된다 — 감속이 없으면 천장은 판이 지나가는 지점이지 끝나는 지점이 될 수 없다. 다만 이 값은 비율의 하한이라 절반으로 내려도 등반이 두 배가 되지는 않고(로그 하나만큼만 는다), 더 내리면 "마지막은 오르막이지 벽이 아니다"라는 규칙 쪽이 먼저 깨진다. 그래서 0.07에서 멈췄다.
- 호수도 도시와 같이 내렸다: `LAKE_DRAIN_SIZE_GAIN_PER_LITRE` 0.0004 → **0.0003**. 이 숫자는 언제나 `SIZE_GAIN` 옆에서만 뜻이 있으므로, 도시만 느려지면 물이 조용히 더 좋은 식사가 된다. 초당 1.9% → 1.4%, 도시 2.1% 대비 비율 0.69로 설계 의도인 "도시의 3분의 2"에 그대로 남는다. 타일 하나는 +12~20%에서 +9~15%가 됐고 여전히 고양이보다 크고 가장 큰 타워(0.2)보다 작다.
- 솔직히 남는 것 하나: **봇 수준으로 먹는 숙련자는 여전히 3분 안쪽에 천장에 닿는다**(조종 봇 기준 166초). 초심자와 숙련자의 간격은 실력이고, 한 곡선이 한쪽에는 느리고 다른 쪽에는 빠를 수는 없다. 곡선을 초심자 쪽에 맞춘 이유는 천장을 일찍 만난 초심자는 남은 판을 잃지만, 일찍 만난 숙련자는 이미 판을 즐긴 뒤이기 때문이다.
- 검증: TypeScript, 단위 테스트 457개, 프로덕션 빌드 통과. `test/size.spec.ts`에 이번 기준점을 분 단위로 고정하는 케이스를 새로 넣었다 — 초당 0.6명으로 60%가 180~240초, 천장이 270~340초 안에 들어오는지 본다. 끼니 수 가드는 새 곡선에 맞춰 상한만 옮겼고(첫 구간 80 → 110끼, 천장 140 → 230끼), 마지막 구간 하한은 15 → 30끼로 오히려 올렸다.
## 2026-08-25 — 천장을 없앤다: `SIZE_MATURE`(사다리의 끝)와 `SIZE_MAX`(안전 클램프)를 가른다

- 사람의 요청: "천장 자체는 없애줘. 천장 자체는 진짜 엄청 높은 수치로 두고 5분 안엔 거의 재현 못하도록."
- `SIZE_MAX` 하나가 두 가지 일을 겸하고 있던 것이 문제였다. 하드 클램프이면서 동시에 **모든 능력치 사다리의 100% 기준점**(빔 강도·적재·조리개·사거리·견인·보너스 하트·카메라 리프트·HUD 비율)이었다. 그래서 이 값만 1000으로 올리면 천장은 사라지지만 판 전체가 곡선의 첫 1%에 갇혀 빔 강도가 1에서 벗어나지 못한다.
- 둘로 갈랐다. **`SIZE_MATURE = 15`**는 "다 자란 크기" — 사다리가 전부 끝나는 지점이고, `sizeGrowthProgress`와 HUD 비율이 재는 기준이다. **`SIZE_MAX = 1000`**은 카메라·안개·충돌 반지름이 유한한 값을 받게 하는 안전 클램프일 뿐 도달 대상이 아니다.
- 다 자란 뒤에도 성장은 계속된다. 그 지점에서 감속은 이미 `GROWTH_FALLOFF_MIN`(0.07)에 닿아 있으므로 한 끼가 개막의 7% 값으로 계속 붙는다 — 벽이 아니라 눈에 보이는 포복이고, 마지막 1초의 마지막 한 명까지 기체는 커진다. 선체·점수 배수·시야·흡수 반경은 계속 오르고, 사다리 값만 마지막 단에서 유지된다.
- 이것이 지난번에 적어 둔 "숙련자는 3분에 천장에 닿는다"의 답이기도 하다. 이제 일찍 다 자란 플레이어는 고정 크기로 남은 판을 보내는 대신 계속 자란다.
- 5분 뒤 크기(초당 섭취량별): 초심자 0.6명 **×14.9**, 직선 봇 0.907명 **×17.6**, 조종 봇 1.107명 **×19.7**, 조종+오브젝트 1.4명 **×23.1**, 비현실적인 2.0명 **×32**. 클램프 1000까지는 다 자란 뒤로도 2310끼, 조종 봇 기준 **35분**이 더 필요하다 — 요청대로 5분 안에는 재현되지 않는다.
- 성장 곡선 자체(`SIZE_GAIN` 0.026/0.043, 감속 0.07)는 손대지 않았다. 초심자 기준 60% 지점 207초·다 자란 크기 307초라는 앞 커밋의 기준점이 그대로 유지된다.
- 테스트는 "다 자란 크기"를 뜻하던 `SIZE_MAX`를 전부 `SIZE_MATURE`로 옮겼다(45개 파일 중 8개). 값이 아니라 뜻을 옮긴 것이며, `sizeProfile(SIZE_MATURE)`가 여전히 빔 강도 30·적재 40·조리개 1.75를 준다.
- `test/size.spec.ts`에 "한 판이 닿을 수 있는 천장은 없다"를 새로 고정했다: 다 자란 크기에서 50끼를 더 먹여 매번 커지는지, 고양이만 700끼(5분 두 판 분량)를 먹여도 클램프의 10분의 1에 못 미치는지, 사다리 값들이 `SIZE_MATURE`와 `SIZE_MAX`에서 같은지, 그리고 클램프에서 `growSize`가 NaN 없이 제자리에 머무는지까지 본다.
- `AGENTS.md`에 규칙 두 줄을 넣었다 — 천장 없음과 진행도의 기준, 그리고 곡선을 분으로 맞춘다는 것(그리고 `SIZE_GAIN`을 만질 때 오브젝트·호수도 같은 비율로 따라와야 한다는 것).
- 검증: TypeScript, 단위 테스트 458개, 프로덕션 빌드 통과.
## 2026-08-25 — 천장을 되돌리되 판 끝 너머에 둔다: 지름 150m

- 사람의 정정: "천장은 있어야 하는데 천장을 200m 정도로 두자는 거지." 이어서 "천장 150m로. 그 정도도 큰 거지?"
- 크다. 게임에서 가장 높은 건물이 60m이고 도시 셀 하나가 34유닛이므로, 지름 150m 접시는 **스카이라인 높이의 2.5배**이며 **네 블록**을 덮는다. 다 자란 크기(81m)의 두 배에 조금 못 미친다.
- 앞 커밋의 구조는 그대로 두고 클램프 값만 내렸다. `SIZE_MAX_DIAMETER = 150`을 새로 두고 `SIZE_MAX = SIZE_MAX_DIAMETER / UFO_BASE_DIAMETER`(= ×27.78)로 유도한다. 천장을 미터로 적어 두는 편이 읽기도 고치기도 쉽다 — 요청도 미터로 왔다.
- `SIZE_MATURE`(15, 81m)는 그대로 능력치 사다리의 끝이다. 15 → 27.78 구간은 더 줄 사다리가 없는 성장이며, 선체·점수 배수·시야·흡수 반경만 계속 오른다.
- 천장까지는 다 자란 뒤로 **339끼**가 더 필요하다. 초당 섭취량별 도달 시간: 초심자 0.6명 14.5분, 직선 봇 0.907명 9.6분, 조종 봇 1.107명 7.9분, 조종+오브젝트 1.4명 6.2분, 비현실적인 2.0명 4.4분. 5분 판에서 실제로 닿는 것은 마지막 줄뿐이다.
- 5분 뒤 크기는 앞 커밋과 같다: 초심자 80m, 직선 봇 95m, 조종 봇 106m, 조종+오브젝트 **125m**. 즉 아주 잘한 판이 125m로 끝나면서 150m를 눈앞에 두고 끝난다 — 천장이 판 안에 있지도, 안 보이는 곳에 있지도 않은 자리다. 1000(5400m)은 사실상 없는 것과 같아서 이쪽이 낫다.
- `test/size.spec.ts`의 케이스 이름을 "한 판이 닿을 수 있는 천장은 없다"에서 **"천장은 판 안이 아니라 판 끝 너머에 있다"**로 바꾸고, 지름이 정확히 150m인지·다 자란 크기가 81m인지·한 판 분량(332끼)이 천장의 80% 아래이면서 지름 100m는 넘는지를 본다. 고양이만 332끼를 먹으면 정확히 천장에 닿으므로(고양이는 군중 슬롯 7개뿐이라 현실에서는 불가능한 식단이다) 표본을 보행자로 바꿨다.
- 검증: TypeScript, 단위 테스트 458개, 프로덕션 빌드 통과.
## 2026-08-25 — 감속을 진행도가 아니라 지름에 건다: 20m마다 한 단계

- 사람의 요청: "아직도 너무 초반에 빨리 성장해... 20미터부터 한 단계 느리게 해주고 40미터부터 한 단계, 60미터부터 한 단계 느리게 해줘. 단, 초심자면 5분 뒤에 60m 정도 커져도 돼. 왜냐면 초반에 도시를 훑으며 여러 오브젝트 보는 재미가 사라졌어."
- 진단이 정확했다. 감속을 진행도(`sizeGrowthProgress`)에 걸어 두면 지수가 2인 곡선은 초반 내내 개막 속도를 거의 그대로 유지한다 — 범위의 앞쪽에서는 `p²`이 0에 가깝기 때문이다. 그래서 "후반을 늦춘다"는 손잡이를 아무리 돌려도 **초반은 하나도 느려지지 않았다.** 브레이크를 판의 진행이 아니라 몸집에 걸어야 했다.
- `growthFalloff`를 매끈한 곡선에서 **지름 사다리**로 바꿨다. `GROWTH_STEP = 0.55`, `GROWTH_STEP_DIAMETER = 20` — 지름 20m마다 성장률이 한 단계씩 내려간다(20m ×0.55, 40m ×0.30, 60m ×0.17, …). `GROWTH_FALLOFF_EXPONENT`는 없앴고 `GROWTH_FALLOFF_MIN`은 이제 고른 값이 아니라 천장(150m)에서 사다리가 도착하는 자리(0.55⁷ ≈ 0.015)에서 유도된다.
- 요청은 20/40/60m 세 단계였지만 같은 규칙을 위로도 계속 이어 붙였다(80·100·120·140m). 세 단계에서 멈추면 60m 위가 평평해져, 잘 먹는 판이 4분대에 150m 천장을 뚫는다 — 앞서 세운 "천장은 5분 안에 닿지 않는다"가 깨진다. 규칙을 이어 붙이면 천장까지 최소 7.2분이 걸린다.
- 초심자(초당 0.6명) 기준: **20m 2분 17초 → 40m 3분 38초 → 60m 5분 5초.** 요청한 "5분 뒤 60m"에 정확히 맞는다. 이전 곡선에서는 같은 플레이어가 3분에 이미 40m를 넘고 5분에 81m였다.
- 5분 뒤 지름(섭취 속도별): 초심자 **60m**, 직선 봇 85m, 조종 봇 98m, 조종+오브젝트 111m, 비현실 131m. 아무도 천장에 닿지 않는다.
- 사다리는 미터로는 고르지 않지만 **끼니로는 고르다**: 구간별 82·49·52·67·94·140·215끼. 비례 성장은 커질수록 한 끼가 주는 미터가 늘어나므로, 지름에 단계를 걸어야 "다음 20m"가 앞의 20m와 비슷한 분량의 플레이가 된다. 이게 이번 변경의 핵심이고, 초반이 빨리 지나가던 이유의 정확한 반대다.
- 단계는 플레이어에게 보이는 사건이기도 하다. 접시가 도시의 한 켜를 넘어설 때마다 성장이 눈에 띄게 뻑뻑해진다 — 아무도 볼 수 없는 곡선 위의 한 점에서가 아니라.
- `SIZE_GAIN`(0.026/0.043)과 천장(150m), 다 자란 크기(81m)는 그대로다. 다만 다 자란 크기의 뜻이 바뀌었다: 이제 초심자가 5분에 닿는 자리가 아니라 잘한 판이 3분 49초에 닿는 자리다.
- 검증: TypeScript, 단위 테스트 458개, 프로덕션 빌드 통과. `test/size.spec.ts`의 성장 관련 두 케이스를 새로 썼다 — 하나는 사다리 자체(19.9m까지 1.0, 20.1m에서 0.55, 40.1m에서 0.55², 천장에서 `GROWTH_FALLOFF_MIN`, 단조 감소, 구간별 끼니 35~100끼)를, 다른 하나는 기준점(초심자 5분 뒤 50~70m, 단계 도달 시각 100~160초·190~250초·270초 이상)을 고정한다.
